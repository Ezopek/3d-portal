# Deferred Work

## Deferred from: code review of 41-1-taggroup-entity-tag-membership-category-removal (2026-07-18)

- No ORM↔migration drift guard — the inline comment claims migration 0018 (story 41.2) will create index `uq_tag_group_slug`, but nothing in the test suite verifies ORM metadata against migrations (no `compare_metadata`/autogenerate diff test). If 41.2 names the index differently or omits it, no test fails. 41.2 owns the migration and should add a schema-parity test (`test_migration_0018`).
- `Tag.group_id` FK is unindexed — intentional for 41.1 (AC #2 matches the 0018 sketch: add column + FK, no index) and consistent with the ORM. Group-scoped tag queries ("tags in group X ordered by group_position") don't exist yet; when E42 introduces them, evaluate adding an index on `tag.group_id`.

## Deferred from: story 41.2 create-story (2026-07-18)

- **Destructive category-retirement DDL is owed by the E42 backend cut-over.** Migration `0018_facet_tags` (this story) is **additive-only** — it creates `tag_group` and adds `tag.group_id`/`group_position`, with a reversible `downgrade`. The destructive half of the original `0018` sketch — `op.batch_alter_table("model").drop_index("ix_model_category_id")` + `drop_column("category_id")` and `op.drop_table("category")`, forward-only (`downgrade` raises `NotImplementedError`) — was pulled out and must land in the **same commit/migration as the ORM removal** of `class Category` + `Model.category_id` from `_entities.py` (E42 stories 42.1/42.3/42.5; likely 42.5 or a dedicated cut-over story). Suggested revision id: `0019_drop_category` (`down_revision = "0018_facet_tags"`). Reason for the split: prod applies `alembic upgrade head` against the HEAD-built image on any deploy (`docs/operations.md:234`); a forward-only destructive migration on `main` before the ORM/app `Category` references are gone irrecoverably bricks prod catalog/share (re-deploying the prior image can't recover a dropped table + a raising `downgrade`). Keeps "main deployable at every commit" + ORM↔migration parity throughout E41→E47. **Operator confirmation requested** (41.2 Story Creation Question #1).
- **Full ORM↔migration drift guard still open (carried from 41-1).** `test_migration_0018` pins the `uq_tag_group_slug` name + `group_id`/`group_position` shape, but there is still no `compare_metadata`/autogenerate diff test asserting the whole ORM metadata matches the migration head. Revisit when the E42 cut-over migration lands (good moment to add a single autogenerate-diff parity test covering the finished facet schema).

## Deferred from: story 41.2 dev (2026-07-18)

- **Confirmed landed as additive-only.** Migration `0018_facet_tags` shipped exactly as scoped: `upgrade()` creates `tag_group` + `uq_tag_group_slug` and adds `tag.group_id` (nullable) / `tag.group_position` (NOT NULL, `server_default="0"`) with FK `fk_tag_group_id` → `tag_group.id` `ON DELETE SET NULL`; `downgrade()` is fully reversible. `model`, `model.category_id`, and `category` are untouched (asserted by `test_migration_0018` as the deferral guard). Alembic has a single head (`0018_facet_tags`).
- **Destructive category-retirement DDL remains owed by the E42 backend cut-over** (as detailed in the create-story note above): a new migration — suggested `0019_drop_category` (`down_revision = "0018_facet_tags"`) — doing `op.drop_index("ix_model_category_id")` + `op.batch_alter_table("model").drop_column("category_id")` + `op.drop_table("category")`, forward-only (`downgrade` raises), must land in the SAME commit/migration as the ORM removal of `class Category` + `Model.category_id` from `_entities.py` (E42 stories 42.1/42.3/42.5; likely 42.5 or a dedicated cut-over story). Never place the forward-only drop on `main` ahead of the ORM/app `Category` removal — it bricks prod on any `alembic upgrade head` deploy and is unrecoverable.

## Deferred from: story 44.2 dev repair review (2026-07-20)

- source_spec: `_bmad-output/implementation-artifacts/spec-44-2-filterribbon.md`
  summary: No `CatalogList` integration test covers the full toggle→URL→models-query round-trip (including the `setFilters` `>=2` gate that drops a stranded `tag_match`).
  evidence: There is no `CatalogList.test.tsx` in the tree; the `setFilters` gate at `apps/web/src/modules/catalog/routes/CatalogList.tsx:77` is only reasoned about in a comment. The user-facing invariant ("no stranded `tag_match` below 2 tags") is enforced and now unit-tested at the `validateSearch` layer (TanStack runs it on every navigation) and at `useModels.buildParams`, so the behavior is covered where it actually runs; the missing piece is a router-level integration test. The spec's Verification section defers integration/visual coverage of the facet surface to Epic 47, so add this router-level test when E47 authors the facet-surface integration/visual suite.
  status: PARTIALLY RESOLVED in the 44.3 dev-repair pass (2026-07-20). `apps/web/src/modules/catalog/routes/CatalogList.test.tsx` now mounts the route (real `createMemoryHistory` router + `QueryClientProvider`, stubbed `fetch`) and covers the `andTooNarrow` → Switch-to-OR (`tag_match=any`) round-trip, the single-filter vs. no-filter empty branches, and `untagged` wiring. STILL OPEN: the `setFilters` `>=2` gate specifically driven through the `FilterRibbon` UI (my test exercises the empty-state + sidebar paths, not a FilterRibbon-originated tag toggle), plus pl-PL visual baselines. Fold the remaining FilterRibbon round-trip into the E47 facet-surface suite. Both STILL-OPEN sub-items are now RESOLVED by the E47 47.2 facet-surface suite, `apps/web/tests/visual/facet-filtering.spec.ts` (2026-07-22): "selecting 2 tags one-by-one through the picker reveals the match-mode toggle" drives the `FilterRibbon`-originated `setFilters` `>=2` gate through actual `TagPicker` UI clicks (not a `?tag_ids=...` URL preset), and the suite's six tests each carry a pl-PL `toHaveScreenshot` baseline (desktop-light/desktop-dark, plus mobile-light/mobile-dark for the ribbon/empty-state tests) under the harness's forced `pl-PL` locale, closing the visual-baseline gap.
- source_spec: `_bmad-output/implementation-artifacts/spec-44-2-filterribbon.md`
  summary: `validateSearch` dedupes `tag_ids` case-sensitively while validating them case-insensitively, so a hand-crafted URL repeating one UUID in two letter-cases counts as 2 tags and lets `tag_match=any` persist for a single logical tag.
  evidence: In `apps/web/src/routes/catalog/index.tsx`, `UUID_RE.test` is case-insensitive but the dedupe `seen` set keys on the exact trimmed string. `[uuid, UUID]` (same id, different case) survives as two entries. Pre-existing E43 normalization behavior surfaced incidentally by 44.2's new `>=2` gate; functionally benign (backend treats `any`≡`all` for one logical tag, and canonical DB UUIDs are lowercase). Revisit alongside E43/E47 tag-id normalization hardening (case-fold the dedupe key).

## Deferred from: story 44.3 dev review (2026-07-20)

_(The page-overshoot dead-end originally recorded here was RESOLVED in the 44.3 dev-repair pass — see the spec's Review Triage Log. `CatalogList` now renders a "Back to first page" recovery action whenever `items.length === 0 && total > 0` (page past the end of a non-empty result set), covered by `CatalogList.test.tsx`. No open 44.3-specific deferrals remain.)_

## Deferred from: story 45.1 dev review (2026-07-20)

- source_spec: `_bmad-output/implementation-artifacts/spec-45-1-modelcard-untagged-chip.md`
  summary: The `/dev/components` manual-QA gallery (`apps/web/src/routes/dev/components.tsx`) has no zero-tag `ModelCard` fixture, so the new "Brak tagów"/"No tags" ghost-chip state has no dedicated lightweight inspection surface even though the sibling "no preview" placeholder state is exercised there via `thumbnail_file_id: null` on the existing fixtures.
  evidence: Both `FAKE_MODEL` and `FAKE_MODEL_2` in `apps/web/src/routes/dev/components.tsx` carry non-empty `tags` arrays; `dev.spec.ts`'s visual baseline never renders the new branch. Not required by the 45.1 spec's scope or ACs, but worth adding alongside a future dev-page touch (e.g. when 45.2/45.3 also add new card/detail states).

## Deferred from: story 45.3 dev review (2026-07-20)

- source_spec: `_bmad-output/implementation-artifacts/spec-45-3-edittagssheet-grouped-picker-create-form-cutover.md`
  summary: `EditTagsSheet`'s "+ Create" button visibility (`candidates.length === 0 && query.length > 0`) conflates "search still loading" with "genuinely zero results," so it can flash briefly while `useTags(query)` is still refetching for a query that does have matches.
  evidence: `candidates = (tags.data ?? []).filter(...)` — while `tags.isFetching` is true for a new `query` and `tags.data` still holds the previous (possibly empty) result set, the create button can render even though the in-flight request may return real matches a moment later. Pre-existing behavior, unchanged by 45.3 (only the surrounding `isAdmin &&` guard was added); not part of this story's scope.
- source_spec: `_bmad-output/implementation-artifacts/spec-45-3-edittagssheet-grouped-picker-create-form-cutover.md`
  summary: A whitespace-only search query lets the "+ Create" button render (`query.length > 0`) but `createAndSelect()` silently no-ops once the trimmed slug is empty, with no user-facing feedback that nothing happened.
  evidence: `EditTagsSheet.tsx`'s `createAndSelect()`: `const slug = query.trim()...; if (slug === "") return;` — pre-existing logic, unchanged by 45.3. A user who types only spaces and clicks "+ Create" sees no toast, no error, and no created tag.
- source_spec: `_bmad-output/implementation-artifacts/spec-45-3-edittagssheet-grouped-picker-create-form-cutover.md`
  summary: A selected tag's chip label falls back to `id.slice(0, 6)` instead of its slug if the tag was selected while matching one search query, then the search text changes to something that no longer returns that tag AND it isn't present in `detail.tags` (i.e., a newly created/selected tag not yet saved).
  evidence: `selectedLookup` is rebuilt each render from `tags.data ?? []` (the *current* query's results) plus `detail.tags`; a selected id present in neither collection has no name to display. Pre-existing `selectedLookup` construction, unchanged by 45.3.

## Deferred from: story 45.3 dev-repair review (2026-07-20)

- source_spec: `_bmad-output/implementation-artifacts/spec-45-3-edittagssheet-grouped-picker-create-form-cutover.md`
  summary: `labelOf`'s locale-fallback helper guards against an empty `name_pl` (falls back to `name_en`) but has no equivalent guard for an empty `name_en`, so a group/tag with `name_en === ""` renders a blank section/label with no fallback text.
  evidence: Identical `labelOf` expression (`preferPl && item.name_pl ? item.name_pl : item.name_en`) is independently duplicated verbatim in `FacetSidebar.tsx`, `TagGroupsSection.tsx`, and now `EditTagsSheet.tsx` — pre-existing gap in an already-shipped (45.1/45.2), already-reviewed convention, not introduced or worsened by this story. `name_en` is presumed non-empty by the same backend/DB invariant those sibling components already trust.
- source_spec: `_bmad-output/implementation-artifacts/spec-45-3-edittagssheet-grouped-picker-create-form-cutover.md`
  summary: Grouped candidate sections have no defined ordering within a section — `TagRead.group_position` ("order within the group," per its own doc comment in `api-types.ts`) is never read by the sort/filter logic, so tags within a section render in whatever order `useTags(query)` returned them.
  evidence: Confirmed via repo-wide grep that no sibling component (`FacetSidebar`, `TagGroupsSection`) reads `group_position` either — it's only ever set in test fixtures. Pre-existing architectural gap surfaced incidentally by this story (the first place a bold group header visually implies curated ordering, making it more noticeable), not caused by this diff's logic.

## Deferred from: story 46.1 dev review (2026-07-22)

- source_spec: `_bmad-output/implementation-artifacts/spec-46-1-group-list-counts.md`
  summary: `AdminTabs`'s `<nav role="tablist" aria-label={t("admin.tabs.users")}>` hardcodes the tablist's accessible name to the "Users" tab label regardless of which tab is actually active, and Story 46.1 adds a 6th tab without fixing it.
  evidence: `apps/web/src/modules/admin/AdminTabs.tsx:20` — `aria-label` is a static `t("admin.tabs.users")` call, unconditional on `activeTab`. Pre-existing since the tab component's introduction; 46.1 only appends a new `<Link>`, it doesn't touch the `aria-label` line.
- source_spec: `_bmad-output/implementation-artifacts/spec-46-1-group-list-counts.md`
  summary: `routes/admin/tag-groups.tsx`'s auth-gate branches (`isLoading` → null, `!isAuthenticated` → null, `!isAdmin` → redirect to `/`) have no automated test coverage — `TagGroupsPage.test.tsx` only mounts the bare page component, never the route wrapper.
  evidence: Same gap exists for the sibling `routes/admin/queues.tsx` (no dedicated route-level auth test either), so this is an inherited convention gap, not a novel regression — but it means the 46.1 acceptance criterion "non-admin is redirected to `/`" is only verified by manual/structural inspection, not a test.
- source_spec: `_bmad-output/implementation-artifacts/spec-46-1-group-list-counts.md`
  summary: `TagGroupsPage` has no ARIA live-region or `aria-busy` signal on loading→data or loading→error transitions, so a screen-reader user gets no announcement that content changed after the initial render.
  evidence: The skeleton is correctly `aria-hidden="true"`, but nothing else in the component tree uses `aria-live`/`aria-busy`; `QueuesPage` has the identical gap, so this is a pre-existing app-wide convention gap surfaced incidentally by this new screen, not introduced by it.
- source_spec: `_bmad-output/implementation-artifacts/spec-46-2-rename-merge-move.md`
  summary: `TagGroupsPage.reorder` swaps two adjacent groups' `position` via two PATCHes; a truly atomic single-request reorder still requires a backend endpoint. An equal-position swap (only reachable from corrupt seed data where two groups already share a `position`) remains a visual no-op that still shows a success toast.
  evidence: RESOLVED (dev-repair 2026-07-22) for the partial-failure inconsistency the reviewers flagged: the two PATCHes now run sequentially and, if the second fails after the first landed, a compensating PATCH restores the first group's original `position` so the server never persists two groups sharing a `position`. Covered by `TagGroupsPage.test.tsx` "reorder partial failure rolls back the first PATCH…". The remaining items are narrower and lower-consequence: full single-request atomicity needs an out-of-scope backend endpoint (accepting an ordered id list), and the equal-position no-op only arises from already-corrupt seed data (self-heals on the next successful reorder/refetch).

## Deferred from: story 46.3 dev review (2026-07-22)

- source_spec: `_bmad-output/implementation-artifacts/spec-46-3-duplicate-detection.md`
  summary: Pressing Escape or clicking the dialog backdrop during an in-flight multi-step cluster merge closes the dialog immediately (bypassing the disabled Cancel button's intent) while `submitMergeDuplicates`'s sequential merge loop keeps running in the background; a success/partial-failure toast then fires with no dialog context.
  evidence: `TagGroupsPage.tsx`'s single `onDialogOpenChange = (next) => { if (!next) closeDialog(); }` handler is shared by all five dialog kinds (rename/move/merge/create/merge-duplicates) and does not check `pending` — this gap predates 46.3 (every 46.2 write dialog has the identical bypass), but the consequence is worse here because a cluster merge is several sequential requests rather than one. A proper fix (ignore `onOpenChange(false)` while any mutation for that dialog is pending) belongs at the shared dialog-state level, not patched into one dialog kind alone.
- source_spec: `_bmad-output/implementation-artifacts/spec-46-3-duplicate-detection.md`
  summary: When a concurrent action drains a cluster to zero live candidates while `MergeDuplicatesDialog` is open, the dialog still renders its full chrome (title, description, and the "other tags will be permanently deleted" warning banner) over an empty candidate list, with only the submit button disabled — no explicit "nothing left to merge" state.
  evidence: The spec's own "Cluster shrinks mid-dialog" I/O row only requires "submit disables if fewer than 2 remain," which is met (`apps/web/src/modules/admin/dialogs/MergeDuplicatesDialog.tsx` `canSubmit`); this is a narrow, low-consequence UX polish gap (near-simultaneous concurrent admin actions only) with Cancel always available, not a functional defect.
- source_spec: `_bmad-output/implementation-artifacts/spec-46-3-duplicate-detection.md`
  summary: `findDuplicateClusters` is O(n²) tag-pair comparisons (each with a bounded-but-nonzero Levenshtein call), recomputed via `useMemo` on every `useTagGroups()` data-reference change, including background refetches that return unchanged content — no size ceiling or memo-by-content-hash guard.
  evidence: Consistent with this epic's established convention of not optimizing admin-scale, rarely-queried tag data ahead of need (see the 42.4 sprint-status note: "42.2's `list_tag_groups` still buckets all tags in Python... re-open as fresh triage only if a future hot per-group query materializes"). At the catalog's actual scale (dozens of tags, per the 41.3 starter taxonomy of 8 groups/36 tags), this is not a practical concern; revisit only if the tag catalog grows into the hundreds and a real jank report surfaces.

## Deferred from: story 47.1 dev review (2026-07-22)

- source_spec: `_bmad-output/implementation-artifacts/spec-47-1-i18n.md`
  summary: `docs/design/HANDOFF-tagi-fasetowe.md:55` and `docs/operations.md:450` still reference the now-deleted `CategoryTreeSidebar` component as current/pending, left stale by this story's deletion.
  evidence: Confirmed both live docs still name `CategoryTreeSidebar`; out of 47.1's boundary by design — story 47.3 ("Runbook + docs cutover") is the epic's designated owner for category-referencing doc updates, so folding this into 47.1 would duplicate 47.3's scope rather than fix a 47.1 gap. A third reference exists at `docs/superpowers/specs/2026-05-05-portal-ui-rewrite-design.md` (a dated historical UI-rewrite proposal, not live operational documentation); 47.3 should confirm whether that archived spec is in scope before treating this list as exhaustive.
  status: RESOLVED by story 47.3 (2026-07-22). `docs/operations.md:450` now annotates the `CategoryTreeSidebar` mention "(later replaced by `FacetSidebar` in the E41-E46 facet-tag catalog rebuild — see Initiative 25)"; `docs/design/HANDOFF-tagi-fasetowe.md:55` now annotates its `CategoryTreeSidebar.tsx` table row "(zrobione — usunięty w story 47.1)". The third reference, `docs/superpowers/specs/2026-05-05-portal-ui-rewrite-design.md`, is confirmed OUT OF SCOPE — it is a dated/historical archived UI-rewrite proposal, not live operational documentation, matching this repo's convention of leaving dated/historical docs alone (per AGENTS.md and this story's own spec Boundaries & Constraints). No open items remain on this entry.
- source_spec: `_bmad-output/implementation-artifacts/spec-47-1-i18n.md`
  summary: No automated check detects i18n keys with zero remaining code consumers (orphaned keys); the repo's only i18n test (`apps/web/tests/i18n.test.ts`) checks en/pl parity, not liveness — this story's cleanup (and the pre-existing `catalog.filters.tags` orphan noted but left alone) relied on manual grepping.
  evidence: Verified `apps/web/tests/i18n.test.ts` only asserts key-set parity between locales. This is a systemic tooling gap, not specific to 47.1's change; will keep recurring across the remaining E47 cutover stories (47.3/47.4/47.5) unless addressed once, e.g. a script that diffs locale keys against a repo-wide `t("...")` grep.

## Deferred from: story 47.1 repair pass (2026-07-22)

- source_spec: `_bmad-output/implementation-artifacts/spec-47-1-i18n.md`
  summary: `epic-47-context.md`'s "Determinism requirement" line states "3 consecutive identical pytest + vitest pass counts required before merging any story in this epic," worded broadly enough to appear to apply to every E47 story, but 47.1 (and presumably other non-destructive stories) closed on a single Vitest run per its own spec's Verification section.
  evidence: The rule reads naturally as scoped to the destructive migrations (47.4/47.5, where non-determinism risk is highest), not simple dead-code/i18n chores; not clear enough to act on without re-reading the source planning artifact this was compiled from. Whoever compiles/uses epic context for 47.2+ should confirm the rule's intended scope and reword `epic-47-context.md` line 33 if it's meant to be migration-specific rather than epic-wide.

## Deferred from: story 47.2 dev review (2026-07-22)

- source_spec: `_bmad-output/implementation-artifacts/spec-47-2-visual-specs.md`
  summary: `apps/web/tests/visual/api-stubs.ts`'s `stubSotList` fallback fixtures (`DEFAULT_TAGS`/`DEFAULT_TAG_GROUPS`, extracted from the pre-existing inline literals) are deliberately left unannotated against `TagListItem`/`TagGroupsResponse` because the pre-existing literals never carried the required `group_id`/`group_position` fields those interfaces declare non-optional; a future edit that adds the natural-looking `: TagListItem[]`/`: TagGroupsResponse` type annotation would fail to compile, or worse, force someone to invent field values just to satisfy the type.
  evidence: Confirmed via `apps/web/src/lib/api-types.ts` that `TagRead.group_id`/`group_position` are required (non-optional) fields, and that the original (pre-47.2) inline `/api/tags*` fixture body never included them — this gap pre-dates 47.2's extraction, it's not new. This is one instance of a systemic pattern: hand-authored JSON-literal test fixtures across the visual suite are not type-checked against `api-types.ts` interfaces at all (`JSON.stringify(...)` erases the type), so drift between a fixture and the real API contract is caught only by a live pixel-diff failure, not the compiler. Revisit if a `satisfies TagRead[]`-style repo-wide fixture-typing pass is ever scoped.
- source_spec: `_bmad-output/implementation-artifacts/spec-47-2-visual-specs.md`
  summary: `apps/web/tests/visual/facet-filtering.spec.ts` now defines its own `skipOnMobile` helper, textually identical in shape to `filter-ribbon-selects-open.spec.ts`'s pre-existing `skipOnMobile` and structurally the mirror of `remaining-sheets-open.spec.ts`'s `skipOnDesktop` — three independent copies of the same project-name-sniffing pattern now live across the visual suite instead of one shared export in `helpers.ts`.
  evidence: Confirmed all three implementations via direct read; each does `test.skip(testInfo.project.name.startsWith("mobile-"|"desktop-"), reason)` with only the reason string differing. Not fixed in 47.2 itself because doing so would require editing `filter-ribbon-selects-open.spec.ts` and `remaining-sheets-open.spec.ts`, both explicitly out of this story's boundary (`spec-47-2-visual-specs.md` Never section names sibling specs as not-to-touch); a dedicated small refactor should consolidate all three into `helpers.ts` exports (`skipOnMobile(testInfo, reason?)` / `skipOnDesktop(testInfo, reason?)`).
- source_spec: `_bmad-output/implementation-artifacts/spec-47-2-visual-specs.md`
  summary: The "Deferred from: story 44.2 dev repair review" entry's `status:` line has now been appended to twice (44.3 dev-repair pass, then 47.2) and is a single unbroken multi-hundred-word paragraph on one line — each future amendment makes an already hard-to-scan ledger entry harder to scan rather than converging on a clean, closed record.
  evidence: Directly observed in this file — the entry's `status:` field is one continuous line mixing the original 44.3 note and 47.2's resolution note with no structural separation. This is a pre-existing convention across the whole file (every `status:` update appends inline rather than as a dated sub-bullet), not something 47.2 introduced; a format change (dated sub-bullets under `status:`, or a `resolved:` field once an item fully closes) would need to apply file-wide to stay consistent, which is beyond one story's scope.
- source_spec: `_bmad-output/implementation-artifacts/spec-47-2-visual-specs.md`
  summary: `FilterRibbon`'s `TagPicker` (`apps/web/src/modules/catalog/components/FilterRibbon.tsx`) renders `role="dialog"` without `aria-modal="true"` and traps neither Tab-order focus nor screen-reader context — its only "modal" behavior is a document-level `mousedown`/`Escape` listener pair, which is a hand-rolled approximation, not a conformant modal dialog.
  evidence: Confirmed via direct read of `FilterRibbon.tsx`'s `TagPicker` function — no `aria-modal` attribute, no focus-trap logic, `useEffect(() => inputRef.current?.focus(), [])` only sets initial focus without preventing it from later escaping the dialog via Tab. Pre-existing since the component's original story (44.2), first exercised end-to-end via real clicks by 47.2's "selecting 2 tags one-by-one through the picker" test — surfaced incidentally, not caused by this story. A screenshot test cannot catch the missing `aria-modal`/focus-trap behavior; would need a dedicated accessibility/keyboard-navigation test.
- source_spec: `_bmad-output/implementation-artifacts/spec-47-2-visual-specs.md`
  summary: `stubSotList`'s new `opts.tags` and `opts.tagGroups` override params are independently optional with no guard ensuring a caller supplies both together; a future caller overriding only one gets a fixture where the flat tag list (drives `FilterRibbon` chip labels / `TagPicker` options) and the grouped tree (drives `FacetSidebar`/`TagGroupsSection`) silently disagree.
  evidence: Confirmed via `apps/web/tests/visual/api-stubs.ts`'s new signature `opts: { tags?: TagListItem[]; tagGroups?: TagGroupsResponse } = {}` — no runtime or type-level check ties the two together. Not triggered today: `facet-filtering.spec.ts` (this story's only new caller) always supplies both from the same `RICH_FIXTURE`-derived source. A future partial-override caller would only notice via an unexplained visual diff (chip showing a truncated id while the sidebar shows the real slug, or vice versa), not a loud failure.

## Deferred from: story 47.3 dev review (2026-07-22)

- source_spec: `_bmad-output/implementation-artifacts/spec-47-3-runbook-docs-cutover.md`
  summary: No test exercises the exact "pre-47.3 nested category tree + `--prune-deleted`" migration scenario end-to-end — only the pre-existing generic prune test (`test_hydrate_prune_deleted_removes_local_files_not_in_master`, arbitrary stale key) and this story's new flat-layout test (fresh run, no old-scheme fixture) exist independently.
  evidence: Confirmed no test seeds an old `<category-slug>/<subcategory-slug>/<model-slug>-<suffix>/` path in the state file and then asserts the post-flattening run downloads to the new flat path while leaving/pruning the old one per `docs/operations.md`'s migration note. Low priority: the state-cache-key-mismatch-triggers-redownload behavior is a direct, obvious consequence of the key composition (`model_dir_rel` is part of the state dict key), not subtle logic requiring dedicated regression coverage; recreating the pre-47.3 nested-path code just to test the transition would require reintroducing deleted code. Revisit only if the migration note's claims are ever disputed in practice.

## Deferred from: story 49.4 code review + re-review (2026-07-27)

- source_spec: `_bmad-output/implementation-artifacts/49-4-tag-aware-free-text-search.md`
  summary: The `q` **model-name** branch (`func.lower(Model.name_en/name_pl/slug)` in `list_models`) still case-folds ASCII only under SQLite, so a model literally named `Żarówka Model` is unreachable by `żarówka`, `Żarówka` and `ŻARÓWKA` alike. Story 49.4 fixed only the **tag-name** branch it introduced, by folding it through the `portal_lower` shim (`app/core/db/session.py`).
  evidence: Reproduced on a throwaway `tempfile.mkdtemp` SQLite DB by the native re-review and re-confirmed by the 49.4 repair pass: `q='Żarówka'` → 0 rows against a model named `Żarówka Model`, while a tag named `Łazienka` now matches all three casings after the repair. The asymmetry is deliberate and bounded: widening the model-name branch changes the meaning of a filter 49.4 was not chartered to touch, and would need its own RED evidence and non-regression sweep over every shipped `q=` assertion. The mechanism and the fix shape are already in-tree — `unicode_lower(session)` is dialect-aware and needs one call-site change per column. `GET /api/tags?q=` (`list_tags`) has the same untouched ASCII-only folding on `slug`/`name_en`/`name_pl`. Whoever picks this up should decide the three call sites together, and note that PostgreSQL needs no shim at all.
- source_spec: `_bmad-output/implementation-artifacts/49-4-tag-aware-free-text-search.md`
  summary: `q` input hygiene on `GET /api/models`: LIKE metacharacters are not escaped (`q='42_m'` matches `Gridfinity 42mm bin`; `q='%bin'` matches; `q='%'` and `q='_'` return the whole catalogue), `q` is never `strip()`ed, and the parameter carries no `max_length` (`router.py`, `service.py` `q` block).
  evidence: Pre-existing on the shipped model-name branch; `q` is a bound parameter, so there is no injection surface — this is contract precision, not security. One recorded rationale needed correcting: the first review deferred it saying "the new disjunct cannot widen the wildcard cases at all". True for `%` and `_` (they already matched everything), **false for whitespace** — the re-review reproduced `q=' '` returning a model whose own `name_en` and `slug` contain no space, matched purely through its tag name (`Cable Tray`). The seeded taxonomy carries multi-word names (`Plant pots`, `Figurki ruchome`) and `apps/web/src/modules/catalog/hooks/useModels.ts` forwards untrimmed input. Disposition unchanged (low, deferred); only the reasoning was wrong.
- source_spec: `_bmad-output/implementation-artifacts/49-4-tag-aware-free-text-search.md`
  summary: `list_models` has no unique sort tiebreaker — `recent` (`created_at desc`) and `name_asc` each emit a single ORDER BY key, so offset/limit paging over tied keys may repeat or skip rows; and no relevance ranking accompanies the widened `q` result set.
  evidence: Pre-existing (`_apply_sort`); story 49.4 touches neither. Ranking is an explicit non-goal of 49.4 (§10), but widening `q` to tag names makes multi-row results normal, which makes the tie hazard likelier to bite, and `tests/test_sot_models_tag_search.py` now asserts exact order and exact page windows on top of an unguaranteed ordering. The re-review could not force a break under SQLite (rowid fallback) — the *guarantee* is absent, not the current behaviour. A `(sort_key, id)` composite ORDER BY is the obvious fix and belongs with whatever story next touches sorting.
- source_spec: `_bmad-output/implementation-artifacts/49-4-tag-aware-free-text-search.md`
  summary: `tests/test_sot_models_tag_search.py` seeds 49 models, 35 tags, 3 browse categories and 2 tag groups into the session-scoped shared test DB (corrected 2026-07-27 from a stale `47 models, 35 tags` — final-review finding M-2; re-measured independently by the M-1..M-7 repair pass with a read-only session-end plugin counting `ts-%` rows, and unchanged by that pass's own added guard, which runs against its own throwaway SQLite file), and several shipped modules index shared windows without a structural guard (`test_sot_models_list.py` indexes a default `limit=50` `recent` page; `test_sot_tags.py` / `test_sot_tag_groups.py` index slug-ordered `limit=200` windows).
  evidence: Currently safe and proven at three scales (module alone, adverse collection order against the ten AC-20 witnesses, full suite), but safety rests on unenforced accidents: `ts-*` slugs sort after the asserted `tag-*`/`tg-*` slugs, and the `seeded_listing` fixture always produces the newest rows. The same class already bit once — `test_sot_tags.py::test_get_tags_returns_seeded_tags` asserted membership over the **default 50-row** window with only 3 slots of headroom, which 49.4's repair pass fixed by giving it the explicit `limit=200` window the rest of that module already uses (with a length assertion that now fails loudly rather than probabilistically if the window ever saturates). The general hazard — shared-DB budget coupling with no structural guard — remains: a per-module DB scope, or a repo-wide convention that shared-window assertions must be `q=`-scoped, is the real fix and is bigger than any one story.
- source_spec: `_bmad-output/implementation-artifacts/49-4-tag-aware-free-text-search.md`
  summary: The `apps/api` suite has pre-existing order coupling in the auth/admin-users area, unrelated to story 49.4 (R-12).
  evidence: Reverse module order yields `7 failed, 3 skipped, 39 errors` (`test_auth.py`, `test_auth_deactivated_user.py`, `test_admin_users_list.py`, `test_admin_users_mutations.py`, `test_admin_users_2fa_overrides.py`, `test_hydrate_local_tree.py`) **identically with and without** story 49.4's module — control run `7 failed, 1781 passed` vs `7 failed, 1799 passed`, a delta of exactly that module's tests. Measured by the native re-review; zero attribution to 49.4. Recorded so it is not rediscovered as a 49.4 regression, and so whoever fixes the coupling knows a control run already exists.

## Deferred from: story 49.4 final code review (2026-07-27)

- source_spec: `_bmad-output/implementation-artifacts/49-4-tag-aware-free-text-search.md`
  summary: The tag disjunct's subquery selects `FROM model_tag JOIN tag`, so the `portal_lower` shim fires once per **assignment row × each of the two tag-name columns × each of the two SQL statements that carry the subquery** — the `total` count (`service.py`) and the page statement — rather than once per tag. That is a Python callback in the inner loop of every `GET /api/models?q=` request, at roughly `2 × 2 × <model_tag rows>` per request before SQLite's short-circuiting of the inner `or_`. A semantically identical tag-first shape, `ModelTag.tag_id.in_(select(Tag.id).where(or_(...)))`, evaluates the callback once per *tag* instead. *(Mechanism corrected 2026-07-27 — fourth-review finding N-5: the original entry recorded only "once per assignment row per column", omitting the ×2 for the two statements. Disposition unchanged: still deferred, still no AC violated, tag-first rewrite still the recommendation.)*
  evidence: Benchmarked by the final native code review on a throwaway `tempfile.mkdtemp` SQLite DB at 2 000 models / 60 tags / 8 000 assignments, best-of-5 — end-to-end `list_models(q=…)` **33.5 ms**; the tag subquery alone **12.9 ms** as shipped, **7.2 ms** with SQLite's built-in `lower()` (so the shim itself costs ≈1.8×), and **1.4 ms** for the tag-first `IN` shape. `EXPLAIN QUERY PLAN` confirms `SCAN model_tag USING COVERING INDEX ix_model_tag_tag_model`. **Read those three numbers as a SINGLE-statement comparison, which is what they measured.** The end-to-end per-request cost is higher because the subquery rides in both the count and the page statement; the "≈9×" ratio the entry originally derived from the 12.9 ms / 1.4 ms pair is therefore not restated here as an end-to-end per-request speedup, and the N-1..N-7 repair pass did **not** re-benchmark end-to-end — inventing a corrected multiplier from unmeasured data would repeat the defect. The fourth native code review measured the mechanism directly instead: **1560** `portal_lower` invocations for one `list_models(q=…)` call on a 400-row `model_tag` fixture with 20 tags / 100 models (against 800 for rows×columns and 1600 for rows×columns×2 statements, the shortfall being SQLite short-circuiting the inner `or_` once `name_pl` matches), and an independent layer measured **80 000** on a 20 000-link fixture, exactly `2 × 2 × 20 000`. **No acceptance criterion is violated:** NFR26-PERF-1 is framed as SQL *statement count* (`architecture.md` § Initiative 26) and story 49.4 §10 makes latency and ranking explicit non-goals, so AC-15…AC-18 are correct and are structurally blind to per-statement cost. The rewrite is a production change outside 49.4's frozen delta and needs its own RED evidence plus a non-regression sweep; it belongs with whatever story next touches `list_models`' query shape, alongside the model-name-branch folding entry above.
- source_spec: `_bmad-output/implementation-artifacts/49-4-tag-aware-free-text-search.md`
  summary: `portal_lower` / `unicode_lower` are documented as "FULL-UNICODE case-folding" (`app/core/db/session.py`, echoed in `list_models`' `- q:` docstring and in the `GET /api/models` OpenAPI description) but implement Python `str.lower()`, which is case *mapping*, not case *folding*; and neither side applies Unicode normalization, so a decomposed (NFD) tag name is invisible to a composed (NFC) query and vice versa.
  evidence: `'Straße'.lower() == 'straße'` while `'Straße'.casefold() == 'strasse'`, so `strasse` does not match a tag named `Straße`. The NFD/NFC gap was reproduced by the final native code review against the shipped seed name `Ogród` (`app/core/db/seed.py`): a tag stored as `Ogród` is returned for the NFD query and not for the NFC one. Harmless for the Polish corpus story 49.4 exists for — no Polish letter has a multi-character casefold and the seed data is NFC — so this is a claim-versus-code precision gap today, not a live defect. Switching to `casefold()` would change matching semantics on both the pattern and the column and needs its own evidence; normalization is a larger contract question that also touches the untouched model-name branch and `list_tags`. Whoever picks this up should decide the wording and the behaviour together.
- source_spec: `_bmad-output/implementation-artifacts/49-4-tag-aware-free-text-search.md`
  summary: `unicode_lower(session)`'s non-SQLite branch (`return func.lower`) has zero test coverage and has never been executed against a live PostgreSQL; its correctness rests on an unenforced precondition (PostgreSQL's `lower()` only folds non-ASCII under a non-C collation). Relatedly, two shipped contract sentences are SQLite-specific: the OpenAPI text "the model-name half still folds ASCII only" and `list_models`' docstring "the model-name columns keep the shipped ASCII-only `lower()`" are both **false on PostgreSQL**, where the helper returns `func.lower` for the tag branch too and both halves fold identically.
  evidence: `grep -rn unicode_lower apps/api/tests` matches only a prose mention in `tests/test_sot_models_tag_search.py`; story 49.4 §15.2 discloses honestly that the branch "has not been executed against a live PostgreSQL" because this repo has no Postgres in test or dev. PostgreSQL is nevertheless the documented migration target (`docs/architecture.md`, `docs/project-overview.md` — "flip `DATABASE_URL`"). The right home for the collation precondition is a migration checklist item (assert a non-C `lc_ctype`, or re-run the diacritic witnesses against the new engine) rather than a runtime assertion bolted onto `session.py`; the two contract sentences should be qualified with "under SQLite" whenever the wording is next touched.
- source_spec: `_bmad-output/implementation-artifacts/49-4-tag-aware-free-text-search.md`
  summary: **Residual of final-review patch item M-1, recorded 2026-07-27 by the M-1..M-7 repair pass** (appended to this block rather than under a new heading, so story 49.4 keeps exactly the two `## Deferred from` headings its two prior passes added). `list_models(q=...)` depends on the canonical engine factory. `portal_lower` is registered on an engine-instance `connect` listener inside `create_engine_for_url` (`app/core/db/session.py`), so a `Session` bound to a bare `create_engine("sqlite:///...")` raises `OperationalError: no such function: portal_lower` out of the tag disjunct instead of degrading to an ASCII-only match. The repair pass closed the finding by **documenting** the dependency at the registration site and **pinning its positive half** with one guard; it deliberately did NOT move the registration to the class-level `@event.listens_for(Engine, "connect")` event, which is the durable fix. **Scope note added 2026-07-27 (fourth-review finding N-6):** a *second* coupling from the same helper — `unicode_lower`'s argument-less `session.get_bind()` raising `UnboundExecutionError` on a `Session(binds={...})` bound per mapper — was **closed in code** by the N-1..N-7 repair pass (`session.get_bind(Tag)`, pinned by `tests/test_sot_models_tag_search.py::test_per_mapper_bound_session_still_folds_tag_names`) and is therefore **not** part of this residual. What stays open below is only the bare-`create_engine` case.
  evidence: Reproduced independently by the final native code review and again by this repair pass on a throwaway `tempfile.mkdtemp` SQLite DB — bare `create_engine(...)` + `init_schema` + `list_models(s, q="kabel")` → `OperationalError: no such function: portal_lower`, while the same URL through `create_engine_for_url` returns normally. **Latent, not live:** `list_models` has exactly one caller (`app/modules/sot/router.py:210`) and every shipped engine path (`get_engine()`, `apps/api/scripts/*`, `workers/render/render/worker.py`) goes through the factory. The **six** in-tree raw-`create_engine` sites — `tests/test_orm_migration_parity.py:62`, `tests/test_browse_category_entity.py:27`, `tests/test_refresh_token_model.py:21`, `tests/test_cleanup_refresh_tokens.py:15`, `tests/test_migration_0020.py:331`, `tests/test_tag_group_entity.py:20` (this last one omitted from story §15.2's enumeration) — plus `migrations/env.py`, which is **not** one of them because it builds its engine via `engine_from_config` rather than `create_engine`, are schema/entity/migration tests and none of them calls `list_models`; verified by `grep -l list_models tests/*.py migrations/env.py`. *(Count corrected 2026-07-27 from a self-contradicting "seven" — fourth-review finding N-4: the original clause counted `env.py` toward "seven raw-`create_engine` sites" in the same sentence that says it is not one. Re-derived by the N-1..N-7 repair pass with `grep -rn 'create_engine(' tests/ migrations/ scripts/ app/ | grep -v create_engine_for_url` → exactly the six above, plus `app/core/db/session.py:55`, the factory itself. The safety conclusion is unaffected.)* The shipped guard is `tests/test_sot_models_tag_search.py::test_factory_built_engine_resolves_the_unicode_lower_shim` (runs against its own throwaway SQLite file, seeds nothing into the shared DB); it was proved load-bearing by a hash-restored mutation removing the `dbapi_conn.create_function(...)` call, under which it fails. What stays open: the class-level `Engine` "connect" registration, guarded by `isinstance(dbapi_conn, sqlite3.Connection)`, which would make `portal_lower` available on every SQLite engine in the process. That is a broadening of global SQL-surface semantics and was deliberately out of scope for a record-fidelity repair pass; it belongs with whatever story first needs `list_models` on a non-factory engine, and should be decided together with the tag-first subquery rewrite recorded above.

## Deferred from: code review of 49-5-admin-category-governance (2026-07-28)

Written by the Story 49.5 post-review repair pass on behalf of the native `bmad-code-review` (CR) pass of the same date, whose own write scope was restricted to the story artifact and `sprint-status.yaml` and which therefore disclosed this ledger append as owed (its disclosed deviation #2). Eight `defer` findings, verbatim in substance from that review's §8. The six `patch` findings and the one `decision-needed` finding are NOT here — they were repaired or arbitrated in the repair pass and are recorded in the story's Review Findings section.

- source_spec: `_bmad-output/implementation-artifacts/49-5-admin-category-governance.md`
  summary: The depth-2 ceiling is an unlocked read-then-write check on both the POST and PATCH paths (`admin_service.py`, `_validate_parent_is_root` + the `reparent_exceeds_depth` children probe), so two interleaved reparents can produce a persistent depth-3 chain.
  evidence: Confirmed by two independent review layers via a forced in-process interleave. Deferred because it is inherent to the ratified design rather than a deviation from it: Decision AX mandates service-layer-only enforcement ("a nullable self-FK cannot express 'at most two levels' in SQLite") and story §3.9/§8.6 explicitly scope out any concurrency mechanism, naming optimistic concurrency as a future trigger. Closing it means building that mechanism. Note that the repair pass DID close the adjacent delete-path race (an `IntegrityError` safety net now re-derives the conflict source after rollback, children first) — that fix covers deletion only and does not constrain reparenting, which has no FK to violate.
- source_spec: `_bmad-output/implementation-artifacts/49-5-admin-category-governance.md`
  summary: The replace-set audit `before`/`after` are order-sensitive lists of an unordered set (`replace_model_categories`, `admin_service.py`), so re-sending the same set in a different order writes an audit row where `set(before) == set(after)` but `before != after` — the trail reports a change that did not happen.
  evidence: AC-32 specifies only the shape and is met; `tests/test_sot_admin_model_categories.py::test_replace_categories_is_idempotent` proves idempotency through response equality and row count, so it passes by construction and cannot catch this. Sorting both sides is a one-line service change plus a matching test edit; it belongs with whatever story next touches the replace-set audit shape, and should be decided together with the identical posture in `replace_model_tags`.
- source_spec: `_bmad-output/implementation-artifacts/49-5-admin-category-governance.md`
  summary: `BrowseCategoryCreate` and `ModelCategoriesReplace` lack `extra="forbid"` while `BrowseCategoryPatch` has it (`admin_schemas.py`), so a typo'd key is silently dropped instead of rejected.
  evidence: Confirmed by execution — `POST` carrying `inclusion_criteria` (typo) returns 201 with the real field silently null; `PUT {"category_ids": [], "catgory_ids": [<real uuid>]}` returns 200 and clears every assignment, the sharper case because the PUT is a destructive whole-set replace. Deferred on exact ratified precedent: `TagGroupCreate` and `TagsReplace` are identical, so tightening this is a cross-cutting decision that also changes the tags surface. Whoever picks it up should change all four schemas in one pass.
- source_spec: `_bmad-output/implementation-artifacts/49-5-admin-category-governance.md`
  summary: `slug` has no format or length validation on the admin category write path (`admin_schemas.py`), so a category can be created that its own canonical by-slug read contract can never reach.
  evidence: Confirmed by execution — `POST {"slug": "a/b"}` returns 201, after which `GET /api/categories/a/b` and `GET /api/categories/a%2Fb` both 404 while the category is still listed by `GET /api/categories`. Whitespace-only, `"Upper Case Slug"` and a 5000-character slug are all accepted too; `_slugify` exists at `admin_service.py` and is never called on this path. Deferred because `TagGroupCreate.slug` carries the identical posture and constraining the format adds a rejection AC-1/AC-2 do not specify — a contract addition, not a review fix. Rated medium by the review, the highest-severity item in this block.
- source_spec: `_bmad-output/implementation-artifacts/49-5-admin-category-governance.md`
  summary: `position` is an unbounded `int` on both the create and patch schemas (`admin_schemas.py`), so `2**63` reaches the driver and escapes as a 500.
  evidence: Confirmed by execution — `OverflowError: Python int too large to convert to SQLite INTEGER` raised at the `session.flush()` in `create_browse_category` / `update_browse_category`, which the `except IntegrityError` blocks there do not catch (and still do not after the repair pass's finding-#4 work, which re-derives the cause of an IntegrityError and does not widen the caught type). Deferred: `TagGroupCreate.position: int = 0` is identical. NOTE — the Edge Case Hunter's PostgreSQL 32-bit `NumericValueOutOfRange` variant does NOT apply; `app/core/config.py` pins a SQLite `database_url` and this repo has no Postgres deployment, so only the 2^63 SQLite path is real.
- source_spec: `_bmad-output/implementation-artifacts/49-5-admin-category-governance.md`
  summary: `inclusion_criterion` is writable and echoed on writes but exposed by no read endpoint (`sot/schemas.py`, `BrowseCategoryAdminRead`), so reading the current value requires issuing a mutating `PATCH {}` that also writes an audit row.
  evidence: Confirmed. The field appears only on `BrowseCategoryAdminRead`, which is the `response_model` of POST and PATCH; there is no admin GET, and the public read deliberately omits it (Story 49.3 §6 F-12, ratified). `app/core/db/seed.py` populates it for all eight seeded categories, so this is real data with no read path. Deferred as outside this story's ratified scope — Decision AY authorises exactly four write routes and story §10 lists no admin read. Belongs to Story 52.2, the direct consumer.
- source_spec: `_bmad-output/implementation-artifacts/49-5-admin-category-governance.md`
  summary: Efficiency cluster on the new category-governance surface — `browse_category_model_count` materialises every assignment row and calls `len()` instead of `func.count()`, unlike its read-side sibling `_browse_category_model_counts` (`service.py`) which emits `SELECT … count(*)`; `replace_model_categories` validates with one `session.get` per id and `category_ids` carries no `max_length` (hence no `maxItems` in the OpenAPI schema); `delete_browse_category` detaches with a per-row ORM delete loop over fully-loaded entities.
  evidence: Measured by the review — a PUT with 8 categories issued 13 cursor executions. Deferred: no operational consequence at this repo's scale (eight categories, a homelab-sized model set), and NFR26-PERF-1's constant-query mandate governs the `q` search path, not this surface. The repair pass deliberately did NOT fold the two `model_count` definitions into one shared helper (the read-side helper is a whole-table GROUP BY and the admin one needs a single category); it pinned the no-disagreement property with a direct parity test instead — `tests/test_sot_admin_categories.py::test_browse_category_model_count_agrees_with_read_side_for_every_category`. Anyone rewriting either query must keep that test green.
- source_spec: `_bmad-output/implementation-artifacts/49-5-admin-category-governance.md`
  summary: Pre-existing test-order fragility, NOT caused by Story 49.5 — `pytest tests/test_csrf_middleware.py tests/test_sot_categories.py` yields `12 failed, 16 passed`, every failure `401 {"detail":"invalid_access"}`, from a 1-byte HMAC key leaking across files while the victim mints tokens with the 20-byte test secret.
  evidence: Both files and `tests/conftest.py` are byte-unmodified by this story's diff and nothing in it touches auth; the default full-suite collection order is green (re-derived independently by the review at `1905 passed, 3 skipped`, and again by the repair pass at `1915 passed, 3 skipped`). Recorded so the next reader is not surprised, and so it is understood that the story §15 T6.2 witness command's file ordering is load-bearing for its own green result. This is a DIFFERENT pair of modules from the auth/admin-users order coupling recorded under "story 49.4 code review + re-review" above — same class, distinct reproduction; whoever fixes the per-module DB/secret scoping should handle both together.

## Deferred from: code review of 50-1-fe-types-and-hooks (2026-07-28)

Written by the native `bmad-code-review` (CR) pass whose verdict on Story 50.1 is `APPROVE`. Two `defer` findings, both edges of `useCategoryBySlug` that are unreachable until **Story 51.2** mounts the first caller. Zero `patch` and zero `decision-needed` findings were raised, so nothing else from that review is owed here.

- source_spec: `_bmad-output/implementation-artifacts/50-1-fe-types-and-hooks.md`
  summary: `useCategoryBySlug` interpolates the slug into the request path without `encodeURIComponent` (`apps/web/src/modules/catalog/hooks/useCategoryBySlug.ts`), so a category slug containing `#` or `?` silently addresses a **different** category instead of returning 404.
  evidence: Measured against the real app router with `TestClient` — `/api/categories/kitchen%23x`, `%3Fx` and `%20x` all return 401 (route matched, auth gate reached), so percent-encoding *would* resolve correctly; unencoded, `fetch("/api/categories/kitchen#x")` drops the fragment and `…/kitchen?x` splits the query, so the browser requests `/api/categories/kitchen` and gets a real, wrong category back. Raw `/` matches no route either way (`/api/categories/a/b` and `…/kitchen%2Fsub` both 404), so encoding cannot rescue that case — that half is the **backend** half already ledgered under "code review of 49-5-admin-category-governance" (admin `slug` has no format validation: `BrowseCategoryCreate.slug = Field(min_length=1)`, stored verbatim by `create_browse_category`, `_slugify` never called on this path). Deferred rather than patched because reachability today is **zero** (the hook has no caller until 51.2) and AC-5 pins the literal expression `` api<BrowseCategoryRead>(`/categories/${slug}`) `` inside a frozen scope. Whoever picks this up should decide it together with the 49.5 slug-validation entry — constraining the slug charset server-side closes both halves at once; otherwise the FE fix is a one-line `encodeURIComponent(slug)`.
- source_spec: `_bmad-output/implementation-artifacts/50-1-fe-types-and-hooks.md`
  summary: `useCategoryBySlug("")` resolves against the LIST endpoint and returns an array while `.data` is typed as a single `BrowseCategoryRead` (`apps/web/src/modules/catalog/hooks/useCategoryBySlug.ts`) — a silent type lie rather than an error.
  evidence: Measured — `GET /api/categories/` returns **307** to `/api/categories` (`app.router.redirect_slashes` is `True`), and `fetch` follows redirects by default, so an authenticated caller receives `BrowseCategoryRead[]` typed as `BrowseCategoryRead`. Story 50.1's D-4 already records the caller contract (non-empty slug, no `enabled` guard, mirroring `useModel(id)`) and Story 51.2 supplies the slug from a `/categories/$slug` route param that is non-empty by construction, so this is a latent contract edge, not a live defect. Close it in 51.2 with whatever the route actually needs — an `enabled: slug.length > 0` guard, or a router-level guarantee — rather than by widening 50.1's frozen hook shape retroactively.

## Deferred from: code review of 52-1-filters-drawer-consolidation (2026-07-29)

Written by the native `bmad-code-review` (CR) pass whose verdict on Story 52.1 is `APPROVE`. Two `defer` findings. One `patch` finding was raised and applied in the same pass (a correction to the story's own `browse-rail` bbox evidence — no code and no baseline changed), so it is not owed here. Zero `decision-needed`.

- source_spec: `_bmad-output/implementation-artifacts/52-1-filters-drawer-consolidation.md`
  summary: `apps/web/tests/visual/filter-ribbon-selects-open.spec.ts` keeps its Story-5.12a filename while every test inside it, and all 18 of its baselines, are now `filters-panel-*` — so the snapshots live at `__snapshots__/filter-ribbon-selects-open.spec.ts/filters-panel-{status,source,sort}-open-*.png`, a spec whose name contradicts its contents.
  evidence: Confirmed by reading the spec and listing the snapshot directory — the describe block is `FiltersPanel Selects — open-state baselines (Story 52.1)`, the desktop-only `skipOnMobile` gate is gone (it now runs on all four projects, hence 18 baselines rather than 6), and every `toHaveScreenshot` name was renamed. Deferred rather than patched because a file rename relocates 18 PNGs for zero behavioural gain and would enlarge an already 106-baseline diff, and because renaming a spec is not something a review should do inside a frozen story scope. Same class as `remaining-sheets-open.spec.ts`, whose describe title still reads `(E5.12d)` after two of its five tests were retired. Belongs to E54's cross-surface visual-spec hygiene pass; whoever takes it should do both in one commit so the baseline churn is paid once.
- source_spec: `_bmad-output/implementation-artifacts/52-1-filters-drawer-consolidation.md`
  summary: After the D-1 consolidation an active `status` or `source` constraint has no representation outside the Filters panel on any viewport — on desktop it previously showed in the inline `Select` triggers — so `EXPERIENCE.md:347` ("active constraints must be visible regardless of where they were selected") is met for those two only by the `(n)` badge, while tags get chips and `tag_match` gets the toggle.
  evidence: Verified against the diff and the regenerated `catalog-list-desktop-*` baselines: the desktop inline select row is gone and the toolbar carries search + `Filtry` only. NOT a defect against Story 52.1 — `EXPERIENCE.md:228` and `DESIGN.md:239` both place the three Selects inside the surface, D-1 rejects a duplicated desktop control as the exact duplication the story exists to remove, and §10 Q1 already records the desktop one-click-away consequence with a recorded recommendation of "no change". Ledgered because it is the natural landing site for any future "show active status/source as chips" request: the fix belongs in `FilterRibbon`'s active-constraint row alongside the tag chips, NOT in the panel, and it would need a UX call on whether a chip and a badge contribution are honest together (the badge already counts them, so a chip would make the same constraint visible twice).

## Deferred from: code review of 53-2-mature-lightbox-implementation (2026-07-29)

Written by the native `bmad-code-review` (Blind Hunter + Edge Case Hunter + Acceptance Auditor) pass on Story 53.2. Four `defer` findings. Thirteen `patch` findings were raised and applied in the same pass (eight code defects plus five record-accuracy corrections), so they are not owed here. One `decision-needed` (reversal trigger R3 / implementation size) was resolved inside the pass by correcting the measurement rather than the code, and is recorded in the story's § 9. Zero unresolved. `G26-LIB` remains OPEN and no physical Android Chrome evidence exists — every finding below is jsdom / headless-Chromium evidence only.

- source_spec: `_bmad-output/implementation-artifacts/53-2-mature-lightbox-implementation.md`
  summary: A `renderImage` that never mounts an `<img>` at all — the `/share` path where `acquireShareBlob` rejects (404 / 429 / offline) and `AnonymousImage` renders a placeholder `<div>` instead — produces neither a `load` nor an `error` event, so the viewer's capture-phase listener never fires, all three zoom controls stay `disabled` forever and the live region is stuck on "Wczytywanie pełnego obrazu…" with no failure ever announced.
  evidence: Read at `ImageFullscreenViewer.tsx:328` (`frame.querySelector("img")` returns null → `setImageStatus("loading")`, with no timeout) against `apps/web/src/routes/share/$token.tsx` where the blob rejection is swallowed. Deferred rather than patched because the fix is a readiness timeout — a new constant and a new behaviour — and D-6 gives Story 53.2 only the disabled-state announcement plus "close + toolbar stay reachable on error", while `epics.md:4579` puts the error and slow-load matrix in **Story 53.3**. Whoever takes it should decide the timeout against the same contract that governs `shareBlobCache` retries, and must not touch the renderer contract (V-12, NFR10-SHARE-SECURITY-1).
  status: **RESOLVED by Story 53.3 (2026-07-29, branch `feat/E53.3-lightbox-test-contract`).** `IMAGE_READY_TIMEOUT_MS = 15_000` was added to `ImageFullscreenViewer.tsx` with its contract in a comment block: it converts an unreported stall into a reported failure, its FLOOR is `shareBlobCache`'s `MAX_CONCURRENT_FETCHES = 4` semaphore (which queues overflow callers with no timeout or `AbortController` of its own), its CEILING is the window where total silence reads as "broken", and it is explicitly not a latency SLO and carries no retry. The watchdog is armed only while the resolved status is `loading`, and cleared on `load`, on `error`, on an `activeIdx` change and on unmount; a `load` arriving AFTER it fired still restores `ready`. `shareBlobCache.ts`, `$token.tsx`, `types.ts` and the `renderImage` contract were NOT touched (verified by `git diff --stat` at closeout), so NFR10-SHARE-SECURITY-1 and the TB-047 semaphore are intact. Closed by five vitest cases in `ImageFullscreenViewer.test.tsx` § "Story 53.3 readiness timeout + inline error": `turns a never-mounted <img> into a reported failure once the timeout elapses` (a `renderImage` returning no `<img>` at all + fake timers — the defect's actual prop boundary, V-21), `does not fire the timeout once the image has loaded`, `lets a LATE load recover from a fired timeout`, `does not let a timer armed for one image fire onto a later one`, and `clears the readiness timeout on unmount`.
  **AMENDED 2026-07-31 by controller ruling DN-2 (see the 53.3 code-review section below).** Two clauses above are no longer accurate and are corrected here rather than silently rewritten: the watchdog is armed **only on the never-mounted-`<img>` branch**, not "while the resolved status is `loading`", because arming it on the ordinary `<img>` path reported a slow-but-working photo as a terminal failure at 15 s; and the case list has grown to seven, with three of the original five re-pointed at the narrowed branch so they stayed non-vacuous.
  **RE-AMENDED 2026-07-31 by controller ruling DN-4 — the `status:` above is NARROWED, and this is the load-bearing correction on this entry.** The earlier claim *"the defect this entry closes is still closed"* was **too broad**. What is closed is the **initial open**: the viewer mounts, `frame.querySelector("img")` returns `null` because `AnonymousImage` has only its placeholder `<div>`, the probe takes the `initial === null` branch (`ImageFullscreenViewer.tsx:430`) and arms the watchdog. **What is NOT closed is any subsequent in-viewer navigation on `/share`.** `renderImage` is invoked as a plain function (`types.ts:23` types it `(props) => ReactElement`) and `/share` passes `renderImage={AnonymousImage}` **by reference** (`routes/share/$token.tsx:377`), so `AnonymousImage`'s `useState`/`useEffect` (`$token.tsx:129-148`) are hooks of the **viewer's own fiber**, registered *after* the readiness effect. On a next/prev press the readiness effect therefore re-runs FIRST, while the outgoing photo's `<img>` is still mounted and still `complete` with `naturalWidth > 0`: the probe takes the third branch (`:449`) → `setImageStatus("ready")` + `measureMaxScale(staleImg)`, and **`armReadyTimeout()` is never reached**. Only afterwards does `AnonymousImage` null its `objectUrl` and swap the placeholder back in. Measured by the second native code review with a throwaway vitest probe in the production renderer shape: `watchdogs armed=0 · <img> in DOM=false · placeholder=true · live="" · zoomIn disabled=false`, and at **+30 s** still `error element=false · live="" · zoomIn disabled=false`. **Consequence for photos 2..N on `/share`: a blob that 404s/429s leaves the viewer reporting `ready` with all three zoom controls ENABLED over an empty pulsing skeleton, `maxScale` inherited from the previous photo, and nothing announced — ever.** That is worse to a user than the `loading`-forever state this entry was opened for. **Pre-existing at `933013e`** — the third probe branch is byte-identical there — on a branch Story 53.3 never touched, and **not** caused by the DN-2 narrowing, which moved a different branch. **Status of this entry is therefore RESOLVED (NARROWED): closed at probe time / first open; the `/share` navigation case is restated as OPEN below** under the second-pass heading and routed to a follow-up story. Story 53.3 claims no more than that (AC-8 bullet 3, coverage row 15 and § 13.2 were all restated).
- source_spec: `_bmad-output/implementation-artifacts/53-2-mature-lightbox-implementation.md`
  summary: When `imageStatus === "error"` the polite live region renders the empty string and there is no inline error element anywhere in the transform layer, so a failed image gives the user a blank frame and total silence — `EXPERIENCE.md` § State Patterns wants an inline error inside the frame (mockup state E).
  evidence: `ImageFullscreenViewer.tsx:846` — the announcement falls through to `""` on error. This is spec-sanctioned, not a defect: D-6 and D-10 defer the visual error *treatment* to Story 53.3 and AC-5 fixes this story's i18n key set at exactly four, so an error string would have needed a fifth key. Ledgered so that nobody reads the shipped "close + toolbar stay reachable on error" guarantee as "the spine's error state is satisfied" — it is not, and **53.3** owns closing it (one new key in both `en.json` and `pl.json`, plus the inline element).
  status: **RESOLVED by Story 53.3 (2026-07-29).** `data-testid="image-viewer-error"` now renders inside the frame and OUTSIDE the transform layer (so it neither scales nor pans), `pointer-events-none` end to end so it can never swallow a tap meant for the chrome beneath it, using existing `gallery-control` tokens with no new `--color-*`. The polite live region announces the failure from state instead of falling through to `""`. Exactly one key was added to BOTH locale files at the same insertion point — `catalog.image_viewer.error` = `The photo could not be loaded.` / `Nie udało się wczytać zdjęcia.`, the Polish transcribed verbatim from mockup `key-viewer-chrome.html:127` state E rather than translated; key sets verified identical at 1047 → 1048 by set-compare. Closed by `ImageFullscreenViewer.test.tsx::renders the inline error inside the frame, outside the transform layer, and traps nobody (D-6)` and by `tests/visual/image-viewer-zoom.spec.ts::a failed image renders the inline error and never traps the user` (real Chromium, all four projects, literal Polish matcher), plus four new baselines `image-viewer-error-<project>.png`. **One residual, ledgered separately below:** the failed `<img>` stays mounted, so Chromium's own broken-image glyph paints faintly behind the error text — visible in all four new baselines and absent from the mockup.
- source_spec: `_bmad-output/implementation-artifacts/53-2-mature-lightbox-implementation.md`
  summary: Every double-tap-to-zoom visibly flashes the chrome layer — the first tap of the pair toggles `chromeVisible` and starts the 150 ms opacity fade, and the second tap toggles it straight back. The net state is correct; the transition is observable on every double-tap.
  evidence: `ImageFullscreenViewer.tsx:582-604`, where the double-tap branch calls `setChromeVisible((v) => !v)` to undo the flip the first tap already committed at `:603`. Deferred because the only real fix is to defer *every* short tap by `DOUBLE_TAP_WINDOW_MS` before toggling chrome — and AC-3 requires TB-043's four-cell gesture matrix at scale 1.0 to stay behaviourally identical, which a 300 ms latency on the shipped tap-to-toggle path would violate. Any future attempt needs an explicit AC-3 amendment via `bmad-correct-course`, not an in-story patch.
  status: **STILL OPEN after Story 53.3 (2026-07-29) — deliberately NOT fixed.** 53.3 D-1 re-confirms the analysis and § 5 lists "Never 'fix' the double-tap chrome flash" explicitly, because the constraint that blocks it is unchanged: 53.2 AC-3 guarantees TB-043's four-cell matrix at scale 1.0 stays behaviourally identical, and the only real fix puts a 300 ms latency on the shipped tap-to-toggle path. 53.3 did, however, pin the surrounding boundaries (see the next entry), so a future amendment lands against constrained behaviour rather than unconstrained. **Route: an explicit 53.2 AC-3 amendment via `bmad-correct-course`, not an in-story patch by any later story.**
- source_spec: `_bmad-output/implementation-artifacts/53-2-mature-lightbox-implementation.md`
  summary: The `DOUBLE_TAP_WINDOW_MS` (300 ms) and `DOUBLE_TAP_SLOP_PX` (44) boundaries have no test that constrains them — `doubleTapTarget` is unit-tested in isolation, but a regression that inverted the window comparison or dropped the slop check would ship green.
  evidence: `zoom.test.ts` covers the pure arithmetic only; `ImageFullscreenViewer.test.tsx` exercises the wiring at `ImageFullscreenViewer.tsx:587-601` indirectly. Partially closed by this review round, which added a resize-refit test and a toolbar-tap/double-tap interaction test, but the window and slop boundaries themselves are still unconstrained. Belongs with **Story 53.3**'s gesture matrix, where the `jsdom` `e.timeStamp` problem (it cannot be set from `fireEvent`) is best solved once for the whole matrix rather than per test.
  status: **RESOLVED by Story 53.3 (2026-07-29).** The `e.timeStamp` problem is solved ONCE, in a single `touchEndAt(el, at, x, y)` helper reused by the whole boundary matrix, exactly as this entry asked. Implementation note for whoever reads D-9 next: D-9 sketched `new Touch({...})` + `new TouchEvent(...)`, which is correct in Chromium and is what the Playwright specs use, but **jsdom ships `TouchEvent` and NOT the `Touch` constructor** (probed this run), so the vitest helper builds the event with DOM-testing-library's `createEvent.touchEnd` — the same plain-object touch list the rest of that file already dispatches — and overrides `timeStamp` as an own property shadowing `Event.prototype`'s getter. Four boundary cases in `ImageFullscreenViewer.test.tsx` § "Story 53.3 double-tap boundaries" now pin both comparisons from both sides: two taps ON the 300 ms window and one millisecond past it, two taps ON the 44 px slop and one pixel past it. A regression that inverted, widened or dropped either comparison fails at least one of them. The constants are RESTATED in the test rather than imported, so a test cannot silently follow a change to the value it exists to pin.

## Deferred from: dev of 53-3-lightbox-test-contract (2026-07-29)

Written by native `bmad-dev-story` on branch `feat/E53.3-lightbox-test-contract`. Two `defer` findings, both **discovered by the tests this story was written to add** — which is the value the story exists to produce. Neither is a regression: both are pre-existing at `933013e` and untouched by this story's diff. Everything else Story 53.3 owned was closed in-story (see the three `status:` lines under the 53.2 code-review heading above). `G26-LIB` remains OPEN and no physical Android Chrome evidence exists; every finding below is headless-Chromium evidence only.

- source_spec: `_bmad-output/implementation-artifacts/53-3-lightbox-test-contract.md`
  summary: **The modal focus trap does not hold in real Chromium.** With the viewer open, `Shift+Tab` from the initially-focused close button lands directly on the page BEHIND the dialog, and `Tab` past the last thumb reaches the header links and the theme toggle. `#root` carries neither `inert` nor `aria-hidden` while the dialog is open, so the background is traversable by a screen reader as well. This is AC-6's focus-trap clause and the one clause Story 53.3 could not satisfy.
  evidence: Measured this run on all four Playwright projects, in BOTH directions — so it is not a document-end wrap artifact of headless `Tab`. Backwards from `image-viewer-close`: `SPAN` (Base UI focus guard) → the file row's delete button → the download link → the 3D-preview toggle → the render checkbox, all `inside=false`. Forwards past `image-viewer-thumb`: focus guard → `BODY` → five header `A` elements → `Przełącz motyw`. A DOM probe returned `DIV#root inert=false aria-hidden=null` with the dialog open. What DOES hold is pointer modality and the scroll lock: `document.elementFromPoint(5, 5)` returns `[data-slot="dialog-overlay"]`, and `image-viewer-zoom.spec.ts`'s wheel probe shows the lock engaging and restoring across three open/close cycles — so `modal` is on and it is specifically the focus / AT-hiding half that is absent. **Not the viewer's defect and not this story's to repair:** the viewer passes no prop that could disable modality, the cause is in the shared `ui/dialog.tsx` + `@base-ui/react` 1.4.1 modal wiring, and the blast radius is EVERY dialog in the app — `ui/dialog.tsx` is Story 53.3 § 5 "Never" (`architecture.md:3375`). **Kept executable rather than deleted:** `image-viewer-zoom.spec.ts::focus never leaves the open viewer in either Tab direction` asserts the CORRECT contract under `test.fail()`, so the day the modal wiring is fixed Playwright reports "expected to fail but passed" and forces this entry to be closed. Whoever takes it should treat it as an app-wide NFR26-A11Y-1 / WCAG 2.1 SC 2.4.3 item across all `DialogContent` consumers (`Viewer3DModal` included — `architecture.md:3378`), not as a viewer patch.
  status: **RESOLVED by Story 54.2 (2026-07-31) — the defect DOES NOT REPRODUCE, and `apps/web/src/ui/dialog.tsx` was NOT modified.** The bounded `Ask First` grant on that file (54.2 § 11 Q-4) was deliberately NOT exercised, because re-measurement found nothing there to repair. BOTH halves of the original reading were measurement artefacts, and both are now pinned properly in `apps/web/tests/visual/a11y-overlay-traversal.spec.ts` across four overlays (lightbox, `DeleteModelDialog`, Filters panel, Browse sheet) x four projects x both Tab directions. (1) **Focus-escape half.** `@base-ui/react`'s `FloatingFocusManager` renders two `FocusGuard` `<span tabindex=0>` SIBLINGS of the popup, deliberately outside `[role="dialog"]`. Tabbing off either end lands on a guard whose `onFocus` bounces focus back — and the bounce is QUEUED, not synchronous. The 53.3 probe read `document.activeElement` in the same tick as `keyboard.press`, so it sampled the guard on precisely the tick where the trap was doing its job. Settling across two animation frames, and counting the guards as part of the dialog's focus scope (which is what they are), makes every press land inside. (2) **AT-hiding half.** `#root` genuinely carries neither `inert` nor `aria-hidden`, but the background IS hidden: base-ui never sets `inert` at 1.4.1, and `markOthers.js` keeps any ancestor of an `[aria-live]` region out of the hidden set on purpose (a live region must stay announceable), hiding that ancestor's other children individually instead — here Sonner's toaster `<section aria-live>` mounted at `App.tsx:34` is what makes `#root` un-markable. The 53.3 probe asserted the wrong node. The new spec asserts the criterion the SC actually states — every focusable element under `#root` has an `aria-hidden`/`inert` ancestor-or-self — and measured ZERO exposed background controls. `image-viewer-zoom.spec.ts::focus never leaves the open viewer in either Tab direction` has had its `test.fail()` REMOVED in the same change and now passes on all four projects. Evidence: headless-Chromium only; `mobile-*` is Pixel 5 EMULATION, never Android; no screen reader was run, so this is an accessibility-TREE claim.
- source_spec: `_bmad-output/implementation-artifacts/53-3-lightbox-test-contract.md`
  summary: On the `/catalog` mount the failed `<img>` stays mounted underneath the new inline error element, so Chromium's own broken-image glyph paints faintly behind the error text. Mockup `key-viewer-chrome.html` state E shows the error text alone.
  evidence: Visible in all four new `image-viewer-error-<project>.png` baselines, read this run before sign-off. Deferred rather than patched because D-6 authorises exactly two things — the inline element and one i18n key — and says nothing about suppressing the image; hiding the transform layer on `error` is a design decision the UX spine implies but D-6 did not grant, and "no silent scope creep" applies. Cosmetic only: the glyph is small, the error copy is fully legible in both themes, and nothing is unreachable. Cheapest future fix is one conditional class on the transform layer inside `ImageFullscreenViewer.tsx` (the file is already in scope for any viewer story) plus a regeneration of the four baselines; it needs a UX call first, because on `/share` the renderer mounts a placeholder `<div>` rather than an `<img>` and the two mounts would then differ in what they hide.
  status: **EXTENDED by the 53.3 code review (2026-07-29).** The `/share` half of the same residual is worse than the `/catalog` half and was not recorded: `AnonymousImage` keeps `<div className="… animate-pulse bg-muted">` mounted after `acquireShareBlob` rejects (`apps/web/src/routes/share/$token.tsx:129-152`), so once the watchdog fires the frame shows a *pulsing "still loading" skeleton* with "Nie udało się wczytać zdjęcia." stacked on top of it — two contradictory states at once, and the pulse never stops (also a `prefers-reduced-motion` smell). Not reproducible in the suite: V-21 routes `/share` coverage to jsdom, where the animation is invisible. Whoever takes the UX call above must decide BOTH mounts together, which is exactly why the call could not be made inside 53.3.

## Deferred from: code review of 53-3-lightbox-test-contract (2026-07-29)

Written by native `bmad-code-review` (Blind Hunter + Edge Case Hunter + Acceptance Auditor, all three layers returned; `review_mode=full` against the story file) on the dirty working tree at branch `feat/E53.3-lightbox-test-contract`, baseline `933013e`. Triage: 5 patch (ALL APPLIED in-pass), 4 defer (below), 3 **decision-needed** (escalated to the controller — they are recorded here as OPEN so nothing is lost if the ruling takes another session), 15 dismissed. The three decision-needed items are the reason the story did not receive an APPROVE.

- source_spec: `_bmad-output/implementation-artifacts/53-3-lightbox-test-contract.md`
  summary: **DECISION-NEEDED — the readiness watchdog arms on the ordinary `<img>` path too, so a slow-but-working load is announced as a failure and the recovery is announced not at all.** `IMAGE_READY_TIMEOUT_MS`'s own contract comment justifies the timer entirely by the `/share` path where no `<img>` ever mounts ("a timeout is the only mechanism that can [reach that path]"), but the code also arms it in the `else if (!initial.complete)` branch, where `load`/`error` will fire on their own.
  evidence: `apps/web/src/modules/catalog/components/imageViewer/ImageFullscreenViewer.tsx:94-113` (the contract) vs `:415-417` (the second arm site). Failure path, measured by reading the two effects: a phone on a throttled connection opens a large photo from `/catalog/<id>`; at t=15 s the inline error paints over the still-downloading image and the polite region announces `Nie udało się wczytać zdjęcia.`; at t≈22 s the image lands, `onLoad` restores `ready`, and the announcement effect (`:465-486`) falls through to `setAnnouncement("")` because `zoomTouchedRef.current` is false — **and emptying an `aria-live` region announces nothing**. A screen-reader user is told the photo failed and is never told otherwise. Neither half is constrained: the vitest `lets a LATE load recover from a fired timeout` case asserts the visual element disappears but never re-reads `zoomLevel()`. **Why the reviewer did not patch it:** both candidate repairs deviate from a frozen constraint. Narrowing the arm to the `initial === null` branch (which is precisely and only the V-12 defect D-5 exists to close, and would also delete the `slow load` spec's undeclared 15 s budget below) contradicts D-5's literal "arm a timer whenever the resolved status is `loading`". Announcing the recovery needs a SECOND i18n key, which contradicts AC-10's mechanically-asserted 1047 → 1048. Either route is a `bmad-correct-course` amendment, not a reviewer's call.
  status: **RESOLVED by the controller-ruled repair (DN-2, 2026-07-31, same branch, uncommitted).** The controller ruled the semantics be fixed in-story with the smallest contract-consistent change, so the arm site was **narrowed to the `initial === null` branch only** — the never-mounted-`<img>` path D-5 exists to close. On the ordinary `<img>` path the viewer now stays `loading` for as long as the download takes and lets `load` / `error` answer for themselves, which is both true and recoverable; no false terminal failure is announced and the silent-recovery half therefore cannot be reached from `/catalog` at all. **This deviates from D-5's literal mechanism wording** ("arm a timer whenever the resolved status is `loading`") and follows D-5's stated *contract* instead ("convert an UNREPORTED stall into a reported failure") — recorded here rather than hidden, because on a mounted `<img>` the stall is not unreported. **No second i18n key was needed, so AC-10's 1048/1048 parity is untouched.** Regression proof: new vitest case `never reports a slow-but-valid mounted <img> as failed (DN-2)` pins BOTH the absence of the timer (no `setTimeout` at `IMAGE_READY_TIMEOUT_MS`) and its observable consequence (no `image-viewer-error`, live region still `Loading full image…`) across 3× the old deadline. Three pre-existing cases were re-pointed at the narrowed branch so they stayed non-vacuous: `lets a LATE load recover from a fired timeout` and `clears the readiness timeout when the image reports an error` now arm via a placeholder renderer and then swap in a real `<img>` (the actual `/share` late-blob shape), and `does not let a timer armed for one image fire onto a later one` uses a per-source renderer so image 1 arms the watchdog and image 2 does not. The `slow load` Playwright spec's undeclared 15 s budget is genuinely gone; its comment at `image-viewer-zoom.spec.ts:539-544` was rewritten to say so instead of continuing to claim a coupling that no longer exists. **RESIDUAL, still open:** on `/share` a blob that resolves later than 15 s still announces the failure and then recovers silently (`setAnnouncement("")` announces nothing). Announcing that recovery needs a second i18n key and is out of the ruled envelope; it is narrower than the original finding because it is now unreachable from `/catalog`.
- source_spec: `_bmad-output/implementation-artifacts/53-3-lightbox-test-contract.md`
  summary: **DECISION-NEEDED — the new error copy measures 2.92:1 in the light theme (WCAG AA for 14 px body text is 4.5:1), and the treatment it uses deviates from the UX spine's own error style.** The spine's mockup specifies destructive-coloured text with no backdrop; the shipped element is a white-on-translucent-black pill.
  evidence: `ImageFullscreenViewer.tsx:962` (re-measured 2026-07-31 by the third native code review; was cited as `:929`) uses `bg-gallery-control/40` + `text-gallery-control-foreground` + `text-sm`. `--color-gallery-control: hsl(0 0% 0%)` (`apps/web/src/styles/theme.css:56`) at 40 % over the light dialog (`bg-background/95`, `:834`) resolves to `rgb(151,151,151)`; sampled directly out of this story's own new baseline `__snapshots__/image-viewer-zoom.spec.ts/image-viewer-error-desktop-light.png` → **2.92:1** against white glyphs. Dark theme is fine (chip ≈ `rgb(5,7,16)`, ~20:1). The spine says otherwise: `ux-designs/.../mockups/key-viewer-chrome.html:49` — `.err{color:var(--destructive); font-size:12px; text-align:center}`, i.e. no pill at all. **Why the reviewer did not patch it:** three tangled constraints. (a) `DESIGN.md:264` "the spines win on conflict" and story § 5 makes any deviation from the spines an **Ask First** — but here the story's OWN D-6 pre-selected the `gallery-control` pair, so the dev followed the frozen contract and the conflict is between D-6 and the mockup, not between the dev and the story. (b) The spine's own treatment does not fix the problem either: `--color-destructive: hsl(0 84% 60%)` on the light dialog computes to ≈4.06:1, still under AA. (c) The `/40` + white pairing is the shipped house idiom at five other call sites (`ModelGallery.tsx:153,164,173,179`, `CardCarousel.tsx:169,178`, `$token.tsx:323`, and the viewer's own counter at `ImageFullscreenViewer.tsx:1036`), so a one-off fix here creates an inconsistency that 54.2 then has to reconcile. The AA-passing minimal repair inside D-6's stated envelope (existing tokens, no new `--color-*`, no colour literal) is raising the backdrop opacity — `bg-gallery-control/60` → 5.7:1, `/70` → 7.4:1 — plus regenerating the four (still-uncommitted) baselines. **Route: controller ruling, or fold into Story 54.2, which `epics.md:4593` already owns for cross-surface colour contrast and which D-10 already names as the owner of the viewer's axe page-set entry.**
  status: **RESOLVED by the controller-ruled repair (DN-3, 2026-07-31, same branch, uncommitted).** The controller ruled the smallest class/token change inside the existing `gallery-control` envelope, so the error chip's backdrop went `bg-gallery-control/40` → **`bg-gallery-control/60`** at `ImageFullscreenViewer.tsx`. Nothing else changed: no new `--color-*`, no colour literal, no change to the spine's copy, and the five other `/40` call sites the reviewer listed were left alone (a broader reconciliation is still 54.2's). **Measured, not reasoned:** an in-browser probe composited every ancestor background through a 1×1 canvas (so Chromium parsed the `oklab(0 0 0 / .6)` and `bg-background/95` layers, not a regex) and computed the WCAG ratio against the chip's own resolved `color`. Result — light theme (`desktop-light` AND `mobile-light`): composite `rgb(102,102,102)`, **5.74:1**, over AA's 4.5:1; dark theme: **20.39:1**. The probe spec was deleted after measurement. **Finding raised BY this repair, ledgered below:** the four `image-viewer-error-*` baselines do **not** gate this — the change passed all four unmodified before regeneration, because Playwright's default `toHaveScreenshot` `threshold: 0.2` absorbs a 49-level greyscale shift. The four baselines were regenerated (`--update-snapshots=all`, scoped by `-g` to the single test) so the committed pixels are the shipped ones, and the class itself is pinned in vitest instead.
- source_spec: `_bmad-output/implementation-artifacts/53-3-lightbox-test-contract.md`
  summary: **The `image-viewer-error` baselines cannot detect a backdrop-opacity change, and by extension neither can any other baseline in this repo at default settings.** Discovered while repairing DN-3: `bg-gallery-control/40` → `/60` moved the chip composite from `rgb(151,151,151)` to `rgb(102,102,102)` — a 2.92:1 → 5.74:1 contrast change, plainly visible to a human — and all four `image-viewer-error-<project>.png` baselines still **passed** against it.
  evidence: Measured this run: `npx playwright test … -g "inline error state matches baseline"` reported `4 passed` with the new class live and the old PNGs in place, confirmed by an in-page probe showing `backgroundColor: oklab(0 0 0 / 0.6)` on the rendered chip. Cause: Playwright's `toHaveScreenshot` defaults to `threshold: 0.2`, a per-pixel YIQ distance; pixelmatch's normalised delta for a 49-level grey shift is ≈0.034, well inside it. Consequence: the repo's visual suite is a *layout* regression gate, not a *colour* one, and any story that reasons "the baseline covers it" for a colour or opacity change is reasoning wrongly. Not repaired here — tightening `threshold` on this one test would be a one-off outside the ruled DN-3 envelope, and doing it repo-wide is a suite-level decision with real flake risk across four projects. DN-3's own regression pin is a class assertion in `ImageFullscreenViewer.test.tsx::renders the inline error inside the frame…` instead. **Belongs with Story 54.2's cross-surface contrast remit** (`epics.md:4593`), which needs a colour gate that actually fires.
  status: **RESOLVED by Story 54.2 AC-7 (2026-07-31).** The finding itself stands unchanged and is now PROVEN rather than argued: with `bg-gallery-control/60` reverted to `/40`, all four `image-viewer-error-*.png` baselines PASSED while the new gate FAILED at 2.85:1 on both light projects. That demonstration is Decision D-5's requirement and is recorded in the story's § 14. The gate is a COMPUTED-CONTRAST PROBE, `apps/web/tests/visual/a11y-contrast-gate.spec.ts`, not a tightened `threshold`: a repo-wide `playwright.config.ts` change stays `Ask First` (54.2 § 5) because of the four-project flake risk this review measured, and it would answer a different question anyway (any pixel drift, not a contrast failure). The probe composites the real alpha stack through a 1x1 canvas, so Tailwind v4's `oklab(...)` output is parsed by Chromium rather than by a regex — a regex-based first draft reported white-on-white at 1:1 and was caught by its own non-vacuity assertion. A second test injects a known-bad composite and requires the probe to score it below AA, so the detector cannot silently stop detecting. Separately, `accessibility-axe.spec.ts`'s page set grew from 4 surfaces to 10, adding the Initiative 26 routes AND two open-overlay states, and the stale "5 pages" comment drift was corrected. **RESIDUAL, ledgered below:** axe reports `incomplete` (not `violation`) for text over a translucent layer, so it does not close V-6 on its own; and overlay chrome painted over a PHOTO has no DOM-determinable backdrop, so it is routed rather than measured.
- source_spec: `_bmad-output/implementation-artifacts/53-3-lightbox-test-contract.md`
  summary: **DECISION-NEEDED — AC-6's focus-trap clause was closed unmet on the dev agent's own authority, and the story grants that authority only for § 11.** The engineering disposition is correct; the procedure is the open question.
  evidence: The defect itself is real and independently re-measured by this review — `apps/web/tests/visual/image-viewer-zoom.spec.ts::focus never leaves the open viewer in either Tab direction` still reports as an expected failure on all four projects, and `node_modules/@base-ui/react/dialog/popup/DialogPopup.js` already passes `modal: modal !== false` to `FloatingFocusManager`, with `apps/web/src/ui/dialog.tsx:50-58` the only app-owned layer between the viewer and that primitive and `ImageFullscreenViewer.tsx:809` passing no modality prop. So there is genuinely no viewer-local repair, and `ui/dialog.tsx` is genuinely story § 5 "Never" (`architecture.md:3375`) — the dev was correctly blocked. What the story does NOT contain is an authority to close an AC unmet: § 0 and AC-11(b) grant that escape for the physical-Android section **only**, and § 5 routes any deviation from `EXPERIENCE.md:234` (which states trap / Escape / return-focus as ONE contract) to `bmad-correct-course` and the controller. The reviewer's own recommendation is to ACCEPT the disposition — the alternative is either a blast-radius edit the story forbids or a story that cannot close at all — but the acceptance has to be the controller's, recorded, not the dev agent's by default. See the fuller technical entry under "Deferred from: dev of 53-3-lightbox-test-contract" above; this entry exists only to keep the *procedural* item visible.
  status: **ACCEPTED by the controller (DN-1, 2026-07-31) — procedurally closed, technically still OPEN.** The controller ruled that AC-6's focus-trap clause may be closed **unmet**, as an explicit deferral, and that the underlying defect is routed to **Story 54.2** as cross-surface dialog remediation. `apps/web/src/ui/dialog.tsx` was NOT modified inside Story 53.3 (verified zero-diff). **Story 53.3 therefore does not claim focus-trap compliance**, and the record says so in plain words. The technical entry under "Deferred from: dev of 53-3-lightbox-test-contract" above stays OPEN and is the one 54.2 must close; `image-viewer-zoom.spec.ts::focus never leaves the open viewer in either Tab direction` stays executable under `test.fail()`, so the day the shared dialog wiring is fixed Playwright reports "expected to fail but passed" and forces it closed.
- source_spec: `_bmad-output/implementation-artifacts/53-3-lightbox-test-contract.md`
  summary: A reported image failure is a dead end — there is no retry affordance, and re-picking the already-active thumb is a React no-op, so a transient 429 leaves that photo broken for the rest of the viewer session.
  evidence: `ImageFullscreenViewer.tsx:1144-1147` (re-measured 2026-07-31 by the third native code review; was cited as `:1111-1114`) — `onClick={() => setActiveIdx(idx)}` on the active thumb sets the state to the value it already holds, so the readiness effect (deps `[activeIdx, frameEl]`, `:456`) does not re-run, the `<img src>` is unchanged and the browser refetches nothing. The only escape is closing and reopening the dialog. D-5 explicitly forbids adding a retry here ("retry policy belongs to `shareBlobCache`") — and `apps/web/src/routes/share/shareBlobCache.ts` has no retry either, only the `MAX_CONCURRENT_FETCHES = 4` semaphore at `:45`. So the retry contract is currently owned by nobody. Not a regression (the pre-53.3 behaviour was an indefinite silent `loading`, which is worse), and out of 53.3's authorised two-file envelope. Belongs with whoever rules on the watchdog item above.
  status: OPEN.
- source_spec: `_bmad-output/implementation-artifacts/53-3-lightbox-test-contract.md`
  summary: The double-tap SLOP boundary is pinned on the X axis only, so a regression replacing the Euclidean distance with a horizontal-only one would ship green.
  evidence: `ImageFullscreenViewer.tsx:797` (re-measured 2026-07-31 by the third native code review; was cited as `:771`, and the second review's proposed correction to `:788` was itself pre-T-6 and never applied) compares `Math.hypot(t0.clientX - last.x, t0.clientY - last.y) <= DOUBLE_TAP_SLOP_PX`. Both new slop cases (`ImageFullscreenViewer.test.tsx`, § "Story 53.3 double-tap boundaries") move the second tap purely horizontally with `y` held at 100, so `Math.hypot(dx, 0) === Math.abs(dx)` and a regression to `Math.abs(t0.clientX - last.x)` passes all four boundary cases. The product code is CORRECT — this is a falsifiability gap in the new test matrix, not a defect — and AC-5's literal ask ("a just-inside and a just-outside case" per constant) is satisfied on the metric as a whole. One additional diagonal case (e.g. `dx = dy = 32`, hypot ≈ 45.3 > 44) closes it; deferred rather than patched to keep this review's gate evidence to a single run.
  status: OPEN.
- source_spec: `_bmad-output/implementation-artifacts/53-3-lightbox-test-contract.md`
  summary: A `sources` swap at an unchanged `activeIdx` never re-runs the readiness effect, so `imageStatus`, `scale`, `pan` and `maxScaleRef` all carry over to a different photo — and no watchdog is armed for it.
  evidence: `ImageFullscreenViewer.tsx:482` (re-measured 2026-07-31 by the third native code review; was cited as `:456`) — the effect's deps are `[activeIdx, frameEl]`. If the parent deletes photo 0 while the viewer sits on `activeIdx = 1` of 3, the shrinking-`sources` clamp at `:227-232` does not fire (1 < 2), `activeIdx` is unchanged, and the effect body never runs: the zoom controls stay enabled over a photo whose ceiling was measured from a different image, and on `/share` the new src re-enters the placeholder-`<div>` state with **no watchdog at all** — the exact stall D-5 exists to convert into a reported failure. **Pre-existing at `933013e`** (the deps line is not in this story's diff) and therefore explicitly not a 53.3 regression; the new watchdog merely inherits the hole. Reachable only from a parent that mutates `sources` while the viewer is open, which `ModelGallery.tsx` does not currently do — hence low severity, but it is the one place where D-5's guarantee has a gap.
  status: OPEN.

## Deferred from: code review of 53-3-lightbox-test-contract, second pass (2026-07-31)

Written by native `bmad-code-review` (Blind Hunter + Edge Case Hunter + Acceptance Auditor; all three layers returned) on the **repaired** working tree at `feat/E53.3-lightbox-test-contract`, baseline `933013e`, after the controller's DN-1..DN-3 ruling. Triage: **1 decision-needed (DN-4) · 14 patch · 2 defer (below) · 11 dismissed**. Every finding is jsdom / headless-Chromium evidence only; `G26-LIB` remains OPEN and no physical Android Chrome evidence exists.

> **UPDATED 2026-07-31 after the DN-4 repair pass and the third native code review.** This preamble originally read *"1 decision-needed (DN-4, see the story § 14.2 — **not ledgered here because it is unresolved and blocks closeout**) · 14 patch (none applied, pending the DN-4 ruling)"*. Both clauses are now stale: the controller ruled DN-4 (**ACCEPTED** as a narrowed / pre-existing deferral, story § 15.1) and all 14 patch items were applied by the DN-4 repair pass. **The DN-4 residual is now ledgered as its own `status: OPEN` entry below** — the third native code review found it had never been written, even though story § 15.1 and § 15.6 both claimed it was, and the 53.2 never-mounted-`<img>` entry above pointed *"restated as OPEN below under the second-pass heading"* at an entry that did not exist. The mounted-and-hung `/catalog` residual (§ 15.3 R-7) was missing for the same reason and is added below as well.

- source_spec: `_bmad-output/implementation-artifacts/53-3-lightbox-test-contract.md`
  summary: An `error → error` transition across in-viewer navigation announces nothing at all, so a screen-reader user gets no signal that they navigated or that the next photo also failed.
  evidence: `ImageFullscreenViewer.tsx:449` (re-measured 2026-07-31 by the third native code review; was cited as `:440`) — the probe branch `initial.complete && initial.naturalWidth === 0` sets `imageStatus` to `"error"`, the value React is already holding, so the announcement effect's deps do not change; and even on a re-run `setAnnouncement` receives an identical string, so React bails and the polite region never mutates. An `aria-live` region only announces MUTATIONS. Reached by: photo 1 fails, the user presses next, and photo 2's `<img>` is already complete-and-failed at probe time (a re-visit of a previously failed source). Deferred rather than patched because the repair is a re-announce mechanism (a nonce, or clearing then re-setting across a tick) — a UX decision about how often a failure should be re-spoken, not a review patch. Note for whoever takes it: this probe branch is also the ONE branch of the readiness probe with no case in Story 53.3's new vitest block, so the repair should land with the missing coverage.
  status: OPEN.
- source_spec: `_bmad-output/implementation-artifacts/53-3-lightbox-test-contract.md`
  summary: The image-failure copy is present twice in the accessibility tree — once as the visible chip and once in the sr-only polite region — so a screen-reader user browsing the open dialog meets the same sentence twice.
  evidence: The visible chip at `ImageFullscreenViewer.tsx:952-964` renders `t("catalog.image_viewer.error")` (the call itself at `:963`) and is deliberately NOT `aria-hidden` (Story 53.3's D-6 test asserts `ariaHiddenAncestor(error)` is null, because "a failed image never traps the user" is about reachability); the polite region at `:1065-1077` carries the identical string. (Both citations re-measured 2026-07-31 by the third native code review; they read `:953-955` / `:1056-1068` when written, before the DN-4 pass's T-6 comment insert shifted the file.) The loading state has no visible twin, so this is specific to the state D-6 introduced. Deferred rather than patched because suppressing either copy is a cross-surface a11y treatment call — the spine's mockup (`key-viewer-chrome.html:124-133`) shows the visible text and says nothing about the live region — and Story 54.2 owns the cross-surface WCAG 2.2 audit over every `DialogContent` consumer. Whoever takes it should decide it together with the already-ledgered error-over-image residual, since both are about what the error state renders on top of.
  status: **RESOLVED by Story 54.2 T14 (2026-07-31).** One sentence, one source. The visible chip (`ImageFullscreenViewer.tsx`, `data-testid="image-viewer-error"`) is now `aria-hidden="true"` and becomes the sighted-user channel only; the always-mounted polite live region keeps the copy exposed, because it is what ANNOUNCES the failure and hiding it would silence the error entirely. Nothing is lost — the information is still in the tree, once. Story 53.3's D-6 contract is otherwise untouched: the chip is still `pointer-events-none`, still traps nobody, and every control beneath it stays reachable, all re-asserted in the same test. The 53.3 assertion that pinned the OLD contract (`expect(ariaHiddenAncestor(error)).toBeNull()`) was deliberately inverted to `.toBe(error)` — `ariaHiddenAncestor` is ancestor-OR-SELF, so that is the exact, stronger form — with the reason written at the assertion.
- source_spec: `_bmad-output/implementation-artifacts/53-3-lightbox-test-contract.md`
  summary: **DN-4 RESIDUAL — on `/share`, the readiness watchdog never arms after in-viewer navigation, so photos 2..N report `ready` with the zoom controls ENABLED over an empty placeholder and are silent forever.** This is the `/share` half of the never-mounted-`<img>` defect the 53.2 ledger entry above assigned to Story 53.3. Story 53.3 closes it **at probe time only** (first open / initial placeholder); the navigation path is **not** closed.
  evidence: `renderImage` is invoked as a plain function (`apps/web/src/modules/catalog/components/imageViewer/types.ts:23` types it `(props) => ReactElement`) and `/share` passes `renderImage={AnonymousImage}` **by reference** (`apps/web/src/routes/share/$token.tsx:377`), so `AnonymousImage`'s `useState`/`useEffect` (`$token.tsx:129-148`) are hooks of the **viewer's own fiber**, registered *after* the readiness effect. On a next/prev press the readiness effect re-runs FIRST, while the outgoing photo's `<img>` is still mounted and still `complete` with `naturalWidth > 0`, so the probe takes the third branch (`ImageFullscreenViewer.tsx:449`) → `setImageStatus("ready")` + `measureMaxScale(staleImg)` and **`armReadyTimeout()` is never reached**; only afterwards does `AnonymousImage` null its `objectUrl` and swap the placeholder back in. Measured by the second native code review with a throwaway vitest probe in the production renderer shape: `watchdogs armed=0 · <img> in DOM=false · placeholder=true · live="" · zoomIn disabled=false`, and at **+30 s** still `error element=false · live="" · zoomIn disabled=false`. **Pre-existing at `933013e`** — that probe branch is byte-identical there — on a branch Story 53.3 never touched, and **not** caused by the DN-2 narrowing, which moved a different branch. **Falsifiability constraint for whoever takes this:** no test in the current suite can exhibit the ordering. `swapRenderer()` swaps the `renderImage` **prop identity** between two hook-free functions; the real renderer is a *stable* prop whose *internal state* drives the swap and whose effect is ordered after the viewer's. **The regression pin must be written in the hooks-in-the-viewer's-fiber shape or it will pin a stand-in.** No such pin exists — writing one against the current probe would assert the defect rather than the contract. The repair itself needs either a re-probe mechanism (a MutationObserver or equivalent = new product mechanism, Story 53.3 § 5 **Ask First** on D-5's contract) or a renderer-contract change (§ 5 **Never**), so it could not land inside Story 53.3's envelope.
  status: **OPEN — accepted by controller ruling DN-4 (2026-07-31) as a narrowed / pre-existing deferral, NOT as an in-story repair.** Story 53.3's AC-8 bullet 3, coverage row 15, § 13.2 and the 53.2 entry above were all restated to claim closure at probe time only; the story claims no more. Owner: a follow-up story — **likely Story 54.2**, alongside the other `/share` viewer residuals already routed there, **or the next canonical remediation story per BMAD sprint status**; the controller assigns it. Entry written 2026-07-31 by the third native code review, which found that the DN-4 repair pass had claimed this ledger entry in story § 15.1 and § 15.6 without ever writing it.
- source_spec: `_bmad-output/implementation-artifacts/53-3-lightbox-test-contract.md`
  summary: **A mounted `<img>` whose request is black-holed or hangs indefinitely is an UNREPORTED stall that nothing now converts into a reported failure** — the accepted cost of the DN-2 narrowing, on `/catalog` as well as `/share`.
  evidence: After DN-2 the readiness watchdog arms only on the `initial === null` branch (`ImageFullscreenViewer.tsx:430-434`); the `else if (!initial.complete)` branch (`:435-443`) arms nothing. A request that is black-holed or hangs indefinitely on a *mounted* `<img>` fires neither `load` nor `error` until the network stack gives up, which is precisely D-5's *"unreported stall"* — so the viewer sits on `loading` with the three zoom controls disabled and the live region on `Wczytywanie pełnego obrazu…` for as long as the browser keeps the socket. **The narrowing remains the right call** — the alternative condemned every slow-but-valid photo as a terminal failure at 15 s and announced the recovery not at all (an emptied `aria-live` region announces nothing) — but the residual is real and was originally asserted out of existence by a categorical claim (*"on a mounted `<img>` the stall is not unreported"*) in story § 13.2 and in the code comments. Both were corrected on 2026-07-31 (story § 15.3 R-7, code comments § 15.2 T-6, comment-only). Distinguishing a hung request from a slow one needs a signal the component does not have; the honest repair is probably an `AbortController` / timeout in the fetch layer, which is `shareBlobCache`'s remit for `/share` and the browser's for `/catalog` — **not** a second watchdog in the viewer.
  status: **OPEN.** Entry written 2026-07-31 by the third native code review, which found that the DN-4 repair pass had recorded this ledger entry as applied (story § 15.3 R-7, § 15.6) without ever writing it.

## Deferred from: story 54.1 dev (2026-07-31)

- source_spec: `_bmad-output/implementation-artifacts/54-1-i18n-parity-audit.md`
  summary: **English register drift between two sibling admin error titles — the divergent key sits outside Story 54.1's key space and could not be fixed here.** `modules.admin.tagGroups.error_title` says *"Couldn't load the tag groups"* while every in-scope sibling says *"Could not …"*. ⚠️ **CORRECTED 2026-07-31 by native `bmad-code-review`: the direction of the drift as first written here was backwards.** Across the seven `*.error_title` keys in `en.json` the contracted form is the **5:2 majority** — `share.view` and the in-scope `modules.admin.categories` carry *"Could not …"*, while `modules.admin.profiles`, `profileLibrary`, `profileOffers`, `queues` and `tagGroups` all carry *"Couldn't …"* (repo-wide across all values: 20 × *"Could not"* / 14 × *"Couldn't"*). So `tagGroups` is the conforming member of its own family and `modules.admin.categories` is the outlier. § 13 ruling (d)'s `keep, justified` verdict is unaffected either way — nothing in the § 3 scope needed changing — but the fix direction below is now stated honestly.
  evidence: `apps/web/src/locales/en.json:842` = `"Couldn't load the tag groups"` vs `:931` `modules.admin.categories.error_title` = `"Could not load browse categories"`. Read this run. The **in-scope** family is already internally consistent on the contracted form — `modules.admin.categories.error_title` (`:931`), `queue.error` (*"Could not load the curation queue"*), `editor.load_error` (*"Could not load this model's categories"*), `toast.reorder_failed` (*"Could not change the order"*) — so § 13 ruling (d) is `keep, justified` on 54.1's side and nothing in `modules.admin.categories.*` was touched. The single divergent string is `modules.admin.tagGroups.*`, which is **Initiative 25** content, explicitly outside the § 3 key space, and Story 54.1 § 5 forbids editing keys outside that scope ("Do not sweep, rename or delete keys outside the § 3 scope"). Contraction-vs-full-form is an English copy-register choice, not a Polish translation defect: the `pl` side of both keys is correct and unaffected (`"Nie udało się wczytać…"` in both). Cosmetic only, admin-surface only, zero a11y or behavioural impact.
  status: **OPEN.** Owner: a controller-assigned copy pass over the Initiative 25 `modules.admin.tagGroups.*` surface — **not** Story 54.2, whose remit is a11y/contrast/visual (`epics.md:4595`), and **not** 54.1. **Fix direction is now a real decision, not a one-word edit** — per the correction above, aligning on the majority means changing the *in-scope* `modules.admin.categories.*` family to the contracted form, not `tagGroups` to the full form. Whoever takes it should rule the register once for the whole `*.error_title` family rather than edit one key. ⚠️ **Before editing `en.json:842`, note that the literal IS asserted** — not in `apps/web/src/modules/admin/tag-groups-i18n.test.ts` (that file has only key-set/parity/allowlist blocks and no literal string assertion; the original pointer here was wrong), but in `apps/web/src/modules/admin/TagGroupsPage.test.tsx:259` and `:274`, both matching `"Couldn't load the tag groups"` verbatim. Pointer corrected 2026-07-31 by native `bmad-code-review`.

- source_spec: `_bmad-output/implementation-artifacts/54-1-i18n-parity-audit.md`
  summary: **The "pl value must not be a copy of the en value" rule is now asserted in three separate test files with three separate, independently-maintained allowlists.** A single legitimate value change had to be reflected in two of them in this story, which is the drift cost showing up in practice rather than in theory.
  evidence: The rule now exists at `apps/web/tests/i18n.test.ts` (Story 54.1, Initiative-26-scoped, 157 keys, `IDENTICAL_BY_DESIGN` with 3 reasoned entries), `apps/web/src/modules/admin/categories-i18n.test.ts:47-56` (Story 52.2, `modules.admin.categories.*`, `COINCIDENTAL_MATCHES` with 1 entry) and `apps/web/src/modules/catalog/browse-i18n.test.ts:37-45` (Story 51.1, `catalog.browse.*`, no allowlist until this story added one). `apps/web/src/modules/admin/tag-groups-i18n.test.ts` carries the same shape for the Initiative 25 surface. Demonstrated cost, this run: aligning `catalog.browse.categoryWithCount`'s Polish plural family onto the shared `"{{count}} <noun>"` counter shape (§ 13 ruling c) landed the `_one` form on the same `"model"` loanword coincidence the admin counter already documents, which required an allowlist entry in **two** files for **one** locale edit. Not a defect — every one of these checks passes and each is locally legible where a story author will look for it, which is exactly why 52.2 and 51.1 wrote them — but the redundancy is now measurable. Deferred rather than consolidated here because a full consolidation means editing three story-level test files that sit outside § 7's predicted set, when D-1 scopes this story to *extending* `tests/i18n.test.ts`, and because the per-story checks also assert story-specific copy contracts a generic check cannot replace. ⚠️ **CORRECTED 2026-07-31 by native `bmad-code-review`:** as first written this clause said 54.1 "has no mandate over" those files, which this same diff contradicts — it does edit one (`apps/web/src/modules/catalog/browse-i18n.test.ts:43-45`) as the mechanical consequence D-4 permits. The honest reason is scale, not mandate: one forced allowlist entry is a consequence, a three-file merge is a refactor. ⚠️ **Also note the asymmetry this leaves behind:** `IDENTICAL_BY_DESIGN` in `tests/i18n.test.ts:116-124` has a staleness guard that fails when an allowlisted pair stops being identical; `COINCIDENTAL_MATCHES` in `browse-i18n.test.ts:43-45` (like the pre-existing `categories-i18n.test.ts:50` it copies) has none, so its exemption is unconditional and permanent. Any consolidation should carry the guard, not the bare set.
  status: **OPEN.** Owner: unassigned — a test-consolidation candidate for `bmad-correct-course` or a future TEA pass, not a story-blocking item. Suggested shape if taken: keep the per-story files for their copy-contract assertions, move only the generic en≠pl rule into `tests/i18n.test.ts` with one allowlist, and delete the three narrow duplicates in the same commit so no window exists where the rule is unasserted for a prefix.

## Deferred from: code review of 54-1-i18n-parity-audit (2026-07-31)

Written by native `bmad-code-review` (Blind Hunter + Edge Case Hunter + Acceptance Auditor) on the dirty working tree at `feat/E54.1-i18n-parity-audit`, baseline `c3db4aa`. All three entries are findings Story 54.1's own AC-11 owed a routing for and did not write. **NOT** an Ezop signature and **NOT** human review.

- source_spec: `_bmad-output/implementation-artifacts/54-1-i18n-parity-audit.md`
  summary: **`catalog.image_viewer.trigger_label` is the accessible name of TWO sibling buttons rendered at the same time, so Story 54.1 renamed a duplicate accessible name rather than removing one.** `NFR26-A11Y-1`'s *"distinguishable by accessible name, not by appearance alone"* still does not hold on the gallery, before or after the fix.
  evidence: `apps/web/src/modules/catalog/components/ModelGallery.tsx:130` (the full-frame `<button data-testid="gallery-fullscreen-trigger">` wrapping the main `<img>`) and `:151` (the corner `<button data-testid="gallery-fullscreen-icon">` carrying the `Maximize2` glyph) both render `aria-label={t("catalog.image_viewer.trigger_label")}`; `apps/web/src/routes/share/$token.tsx:308` and `:321` are the same pair on the public share page. Both are always in the accessibility tree — the corner button is hidden with `sm:opacity-0 sm:group-hover:opacity-100`, which is opacity, not `display`/`visibility`/`aria-hidden`. So a screen-reader user on `/catalog/$modelId` heard *"Powiększ"* twice before this story and hears *"Otwórz na pełnym ekranie"* twice after it. **Raised independently by two of the three review layers.** The new AC-7 regression guard at `apps/web/tests/i18n.test.ts:153-167` structurally cannot see this: it asserts that no two *keys* in `catalog.image_viewer.*` share a *value*, which says nothing about one key used on two controls. **Pre-existing** — both call sites were shipped by Story 22.3 (`812c7bd`, 2026-05-24) and the pair is byte-identical at `c3db4aa`; Story 54.1 did not create it and did not make it worse. Reviewer-verified by reading both call sites, not inferred from the diff.
  status: **RESOLVED by Story 54.2 T13 (2026-07-31).** Fix shape: one named control, one decorative twin, applied to BOTH pairs. The full-frame trigger keeps the name and the tab stop; the corner icon becomes `aria-hidden="true"` + `tabIndex={-1}` at `ModelGallery.tsx:151` and `routes/share/$token.tsx:321`. Nothing is lost — the corner button's `onClick` is identical to the full-frame button's and the full-frame button covers the whole image, so the function stays keyboard-operable (SC 2.1.1) and pointer-operable from either target. `focus-visible:opacity-100` was removed with the tab stop: it could never fire again and would have read as a live affordance. Pinned on both mounts — `ModelGallery.test.tsx` asserts the name resolves exactly ONCE in jsdom, and `share-anonymous-with-signin.spec.ts` does the same in real Chromium for the anonymous twin, which no vitest suite mounts. That spec is assertion-only and adds zero baselines.

- source_spec: `_bmad-output/implementation-artifacts/54-1-i18n-parity-audit.md`
  summary: **The Polish word `"Powiększ"` still names two different controls on the model-detail route — the collision Story 54.1 closed for `trigger_label` survives against the 3D viewer's expand button.**
  evidence: `apps/web/src/locales/pl.json:399` `catalog.image_viewer.zoom_in` = `"Powiększ"` (en `"Zoom in"`, `en.json:399`) and `apps/web/src/locales/pl.json:459` `viewer3d.tooltip.expand` = `"Powiększ"` (en `"Expand"`, `en.json:459`). Despite the `tooltip` in its key name, `viewer3d.tooltip.expand` renders as the **visible text of a real `<Button>`** at `apps/web/src/modules/catalog/components/viewer3d/Viewer3DInline.tsx:243-245`, mounted from `apps/web/src/modules/catalog/components/tabs/FilesTab.tsx` on the same `/catalog/$modelId` route that hosts the gallery whose lightbox toolbar carries `zoom_in`. One Polish word, two distinct English concepts, one journey — the exact cross-surface terminology defect class `NFR26-I18N-1` names. **Raised independently by two of the three review layers.** Story 54.1's audit could not see it: § 3 defines scope by key *prefix*, and `viewer3d.*` is not an Initiative 26 prefix, so neither the § 12 table nor the new `catalog.image_viewer.*`-scoped uniqueness check at `apps/web/tests/i18n.test.ts:153-167` reaches it. `grep -n viewer3d` over the story file returns zero matches. Related and recorded for the same fixer: `viewer3d.tooltip.close` = `"Zamknij"` is likewise byte-identical to `catalog.image_viewer.close`.
  status: **OPEN.** Correctly **not** fixable inside Story 54.1 — `viewer3d.*` is outside the § 3 key space and § 5 forbids editing keys outside it. What was owed and missing is the **routing**: § 13 ruling (d) set the precedent by routing the out-of-scope half of its own divergence to this ledger, and this one got neither a verdict nor an entry. Owner: unassigned — a controller-assigned copy pass over the `viewer3d.*` surface, or `bmad-correct-course` if the register question turns out to be broader. Cheapest fix is a value change on `viewer3d.tooltip.expand` (e.g. a "pełny ekran" phrasing that tracks its English `"Expand"`); check `apps/web/tests/visual/` for a literal matcher on `"Powiększ"` first — `image-viewer-zoom.spec.ts:78` and `image-viewer-containment.spec.ts:363` both match it, but both are scoped inside `image-viewer-toolbar` and so resolve `zoom_in`, not this key. *(Containment line corrected from `:362` to `:363` on 2026-07-31 by the second native code review — `:362` is the `toolbar` helper; the comment rewrite shifted the matcher down one line.)*

- source_spec: `_bmad-output/implementation-artifacts/54-1-i18n-parity-audit.md`
  summary: **After Story 54.1 no test anywhere can catch `catalog.browse.categoryWithCount_one` reverting to a literal English string, and no fixture renders it.** The accepted cost of § 13 ruling (c)'s allowlist, recorded so it is a known gap rather than a silent one.
  evidence: Ruling (c) aligned the Polish singular onto `"{{name}}, {{count}} model"`, which is byte-identical to the English `_one` form, so the key had to be exempted in **both** places that asserted en≠pl for it: `apps/web/tests/i18n.test.ts:96` (`IDENTICAL_BY_DESIGN`) and `apps/web/src/modules/catalog/browse-i18n.test.ts:44` (`COINCIDENTAL_MATCHES`). Those were the only two assertions covering the key — before this story it read `"{{name}}, model: {{count}}"` and was genuinely different from English. What remains is `browse-i18n.test.ts:65-75` (both placeholders present) and `i18n.test.ts:116-124` (the pair must *stay* identical), neither of which would notice `"{{name}}, {{count}} item"` being pasted in. Rendering coverage is likewise absent: every browse fixture uses `model_count` 12, 7 and 0 (`apps/web/tests/visual/api-stubs.ts:186,197,208`), all of which resolve the Polish `_many` form, and every accessible-name matcher (`browse-rail.spec.ts:39,40,54,74`, `browse-sheet.spec.ts:56,58,75`, `category-browse.spec.ts:41`) asserts `…, N modeli`. No fixture ever produces `count: 1`, so the singular branch of the browse rail — the one a one-model category shows — renders in no test. Reviewer-verified; this is also why nothing broke, since the old singular was equally unasserted.
  status: **OPEN.** Not a Story 54.1 defect: the allowlisting is what AC-3 explicitly permits and the reason given (the `model` loanword) is the same one `modules.admin.categories.model_count_one` already ships. Closing the gap needs a `count: 1` browse fixture, which would touch `api-stubs.ts` and risk baseline churn — both forbidden by § 5 / AC-9 in this story. Owner: unassigned — a TEA/coverage follow-up, naturally bundled with the test-consolidation entry above. Suggested shape: one extra stub category with `model_count: 1` plus an accessible-name assertion on `…, 1 model`, added in a pass that owns its own visual-baseline review.

## Deferred from: second code review of 54-1-i18n-parity-audit (2026-07-31)

Written by the second native `bmad-code-review` (Blind Hunter + Edge Case Hunter + Acceptance Auditor) on the dirty working tree at `feat/E54.1-i18n-parity-audit`, baseline `c3db4aa`, after the first review's five patch findings were applied. **NOT** an Ezop signature and **NOT** human review.

- source_spec: `_bmad-output/implementation-artifacts/54-1-i18n-parity-audit.md`
  summary: **The gallery and the lightbox call the same object two different nouns, in BOTH languages, one click apart inside the same component — an unaudited cross-surface terminology divergence sitting directly on the § 1 journey.** `catalog.gallery.*` says *obraz* / *image*; `catalog.image_viewer.*` says *zdjęcie* / *photo*.
  evidence: `apps/web/src/locales/pl.json:386-388` — `catalog.gallery.previousImage` = `"Poprzedni obraz"`, `nextImage` = `"Następny obraz"`, `thumbN` = `"Obraz {{n}} z {{total}}"` (en `:386-388`: *"Previous image"*, *"Next image"*, *"Image {{n}} of {{total}}"*). Against `pl.json:392-398` — `catalog.image_viewer.prev` = `"Poprzednie zdjęcie"`, `next` = `"Następne zdjęcie"`, `thumb_label` = `"Zdjęcie {{index}}"`, `dialog_title` = `"Galeria zdjęć"` (en: *"Previous photo"*, *"Next photo"*, *"Photo {{index}}"*, *"Photo gallery"*). **One component renders both sets**: `ModelGallery.tsx:161`/`:170`/`:191` carry the gallery arrows and thumb labels, and `:130`/`:151` carry the fullscreen trigger that opens the lightbox — so a user moving from the inline gallery into the lightbox crosses the noun boundary in a single click. This is the divergence class `NFR26-I18N-1` names verbatim (*"consistent terminology … across … viewer controls"*) and it is **strictly stronger** than the `viewer3d.tooltip.expand` item above, which the first review did route: this one is in both locales, in the same component, and on the audited journey. Reviewer-verified by reading both locale files and the call sites, not inferred from the diff.
  status: **OPEN.** Correctly **not** fixable inside Story 54.1 — `catalog.gallery.*` is not one of the § 3 prefixes, and § 5 forbids editing keys outside that scope. What was owed and missing is the **routing** (AC-11), on the precedent § 13 ruling (d) and the first review's `viewer3d` entry both set. **It also exposes a scope-definition weakness worth ruling once:** § 3 defines the key space by *prefix*, but V-11 evicted `catalog.filters.openFilters`/`title` on a *date* argument while Completion Note 5 kept nine `catalog.image_viewer.*` keys that fail the same date test — so `catalog.gallery.*` (Story 22.3, same component, same surface as the kept viewer keys) is out on prefix alone. Owner: a controller-assigned copy pass over the gallery/viewer noun, plausibly bundled with the `viewer3d.*` entry above since both are one-word register decisions on the same route. Whoever takes it should rule the noun once for the whole photo surface rather than edit one key, and must check `apps/web/tests/visual/` for literal matchers before changing a value.

## Deferred from: dev + code review of 54-2-a11y-and-visual-audit (2026-07-31)

Written by native `bmad-dev-story` and the follow-on native `bmad-code-review` / Aider-arbitration pass on the dirty working tree at `feat/E54.2-a11y-visual-audit`, baseline `931bcaa`. **NOT** an Ezop signature and **NOT** human review.

Every entry below was MEASURED — target sizes by `getBoundingClientRect()` in Chromium (`tests/visual/a11y-target-size.spec.ts`), contrast by axe and by the composited probe in `tests/visual/a11y-contrast-gate.spec.ts`, the census by `tests/screenshot-visibility-census.mjs`. Per `epics.md:4385` each non-pass names the story that SHIPPED the surface: a finding is a defect in that story, not new end-of-initiative scope. **`G26-LIB` remains 🔓 OPEN**, no physical Android Chrome evidence exists or is claimed anywhere in this story, the `mobile-*` projects are Pixel 5 **emulation**, and every AT claim is an accessibility-TREE claim, never screen-reader output.

- source_spec: `_bmad-output/implementation-artifacts/54-2-a11y-and-visual-audit.md`
  summary: **Four `tabs/FilesTab.tsx` controls are under WCAG 2.2 SC 2.5.8's 24x24 CSS px floor**, measured on the model-detail route: the render-selection checkbox at **13x13** (no associated `<label>` to enlarge the target), the "Toggle 3D preview" row action at **48x22**, the "Pobierz" download link at **30x22**, and the "Usuń <file>" delete action at **30x22**.
  evidence: `tests/visual/a11y-target-size.spec.ts`, `SC_2_5_8_KNOWN` — each carries a `routed:` string with its measured box, and the gate FAILS on any control not in that list, so these are tracked failures, not waivers. Shipped by `06ece7b` (2026-05-05, Initiative-13 era, two initiatives before this audit). Routed rather than remediated on the same ground Decision D-7 rules for `viewer3d`: FilesTab sits on the model-detail ROUTE but not on the § 1 journey (browse -> detail -> photo lightbox); it is the admin file-management sibling tab. Removing an entry from `SC_2_5_8_KNOWN` is how the owning story proves it closed one.
  status: OPEN. Owner: controller-assigned follow-up (Initiative-13 FilesTab surface).

- source_spec: `_bmad-output/implementation-artifacts/54-2-a11y-and-visual-audit.md`
  summary: **The browse rail's zero-count category label fails WCAG AA in BOTH themes.** `BrowseCategoryList.tsx:140` dims an empty category with `text-muted-foreground/60`; axe measures **2.77:1** light (#939caa on #ffffff) and **3.31:1** dark (#5f6b7f on #0f1729) against a 4.5:1 floor.
  evidence: Found by the AC-7 axe page-set extension the moment it first ran — this surface had never been scanned (§ 2 V-6: the pre-existing `PAGES` set was four pre-Initiative-26 pages). Tracked in `accessibility-axe.spec.ts` as a `KnownContrast` entry with `expectedOn: /^desktop-/` (the rail is `lg:flex`, so it does not render on the Pixel 5 projects). NOT an `AxeBuilder.exclude()` — architecture Decision I reserves the exclude-list for FALSE positives and requires operator approval per entry; this is a true positive, so the scan still sees it and the gate still fails on anything unrouted. Routed rather than fixed because the dimming is a deliberate `DESIGN.md:271` affordance and choosing how "empty" is signalled legibly is a design call, not an opacity slip. Shipping story: Epic 51 browse rail.
  status: OPEN. Owner: controller-assigned follow-up, with a UX call on the empty-category treatment.

- source_spec: `_bmad-output/implementation-artifacts/54-2-a11y-and-visual-audit.md`
  summary: **The shared `ui/badge` `success` outline variant fails WCAG AA in the light theme at 2.11:1** (#21c45d on #e9f9ef), on every Initiative 26 surface that renders it.
  evidence: Same axe extension. Routed, not fixed: this is an app-wide design-system token, pre-Initiative-26, and repairing it is a token change whose blast radius is far outside this story's envelope — story § 0.3 names a sprawling diff as the signal that the audit turned into a redesign. Tracked as a `KnownContrast` entry on four surfaces.
  status: OPEN. Owner: controller-assigned follow-up (design-system token pass).

- source_spec: `_bmad-output/implementation-artifacts/54-2-a11y-and-visual-audit.md`
  summary: **`ScopeChip`'s text action fails WCAG AA by 0.01 — 4.49:1 against a 4.5:1 floor** (#2463eb on #e9effd), light theme.
  evidence: `ScopeChip.tsx:32`, `text-primary` on the chip's own `bg-primary/10`. Worth recording precisely because no threshold-based or pixel-based gate would ever have surfaced a 0.01 miss; only a computed-ratio gate does. Initiative 26's own surface (Epic 51 category scope). Routed because the repair is a primary-token choice.
  status: OPEN. Owner: controller-assigned follow-up (Epic 51 category scope).

- source_spec: `_bmad-output/implementation-artifacts/54-2-a11y-and-visual-audit.md`
  summary: **Overlay chrome painted over a PHOTO has no DOM-determinable backdrop, so the AC-7 contrast gate cannot rule on it** — the `ModelGallery` prev/next/fullscreen controls (`ModelGallery.tsx:153,164,173,179`), the `/share` carousel chrome (`$token.tsx:323`), `CardCarousel.tsx:169,178`, and the viewer's own counter and prev/next while an image is showing.
  evidence: These are the five other `bg-gallery-control/40` + white call sites the DN-3 repair deliberately left alone; the 53.3 review said "a broader reconciliation is still 54.2's". T11 measured them and the honest answer is that they are NOT measurable by the mechanism AC-7 built: a photograph's per-pixel luminance makes the composite unbounded in principle, and neither a DOM walk nor axe can resolve it (axe returns `incomplete`, which fails nothing). The viewer ERROR chip is measurable and IS gated, because the error state means no image painted. The honest repair for the over-photo cases is a guaranteed scrim behind the control rather than a ratio assertion, which is a UX decision.
  status: OPEN. Owner: controller-assigned follow-up, needs a UX call on the scrim.

- source_spec: `_bmad-output/implementation-artifacts/54-2-a11y-and-visual-audit.md`
  summary: **69 of 132 screenshot occurrences are not preceded by an explicit `toBeVisible()`** — 51 lean on a weaker visibility wait, 18 have no proof of any shape.
  evidence: `node apps/web/tests/screenshot-visibility-census.mjs` (committed with the story per AC-6, TypeScript-AST based, resolves file-local helpers to their calling tests). Unit = (screenshot call site) x (test that reaches it). **This number SUPERSEDES the three divergent file-level figures in § 2 V-5 (10 / 20 / 32)**, all of which were per-FILE greps and therefore wrong in both directions. Not remediated here: 69 call-site edits across ~40 specs is a mechanical sweep with its own review surface, and none of it is an a11y or contrast defect — it is test-authoring hygiene (`epics.md:4595`, epic:45/epic:46 TEST-AUTHORING). The script makes the number reproducible, which is the half § 2 V-5 said was missing.
  status: OPEN. Owner: **a controller-assigned follow-up** (re-pointed 2026-07-31 by Story 54.3, taking the alternative this entry already offered). Story 54.3 is docs-only — it ships no test, spec or baseline change — so a mechanical sweep across ~40 specs has no home in it. Re-run the script for the current number; do not carry 69 forward as prose.

- source_spec: `_bmad-output/implementation-artifacts/54-2-a11y-and-visual-audit.md`
  summary: **This ledger's own 52.1 entry states the wrong baseline count** — it says `filter-ribbon-selects-open.spec.ts` has "all 18 of its baselines" and "18 baselines rather than 6". Measured: **12**.
  evidence: `ls apps/web/tests/visual/__snapshots__/filter-ribbon-selects-open.spec.ts/` returns 12 PNGs — `filters-panel-{status,source,sort}-open-{desktop,mobile}-{light,dark}.png`, i.e. 3 `test()` blocks x 4 projects. Independently confirmed three times (54.2's create pass VS-1, its dev pass, and this review pass). Recorded here rather than edited into the 52.1 entry in place, so the correction is visible to anyone who already read the wrong figure. **The rename itself stays OUT of 54.2** by controller ruling Q-2 — it is pure hygiene and would violate AC-8's zero-PNG-churn requirement on the very spec AC-8 edits.
  status: OPEN (the rename). The figure is CORRECTED to 12 by this entry. Owner: **E54 visual-spec hygiene pass** (re-pointed 2026-07-31 by Story 54.3 to the only alternative owner this entry names — it offers no controller-follow-up option, so none was invented). Story 54.3 is docs-only and must ship zero PNG churn; the rename touches `__snapshots__/` and therefore the baseline-review gate.

- source_spec: `_bmad-output/implementation-artifacts/54-2-a11y-and-visual-audit.md`
  summary: **Story 54.2 § 2 V-3's "8 redundant admin `auth/me` re-stubs" is wrong — only 4 are redundant.** AC-8's proof-first order (Decision D-3) overturned the classification on 4 of the 8, one of them empirically after the deletion had already been made.
  evidence: Re-measured per spec, then proved by running each spec after removal and checking for PNG churn. **Removed (4, zero churn, proved by re-run):** `admin-dropdowns-tooltip-open.spec.ts`, `destructive-dialogs-edit-sheets-open.spec.ts`, `remaining-sheets-open.spec.ts`, `admin-invites.spec.ts`. **Kept, load-bearing (4):** `agents-info-dialog.spec.ts` — the spec's own SELECTORS bind to the payload (`waitForSelector("text=Ezop")`, `getByRole("button", { name: "Ezop" })`), so against the fixture default every test fails to find its trigger; `sessions.spec.ts` — both baselines are FULL-PAGE, so the header `UserMenu` trigger paints `display_name` ("Test User" vs "Admin"); `admin-users.spec.ts` — `UsersPage.tsx:420` computes `isSelf` from `user.id === auth.user?.id` and this file's fixture rows use the same `id: "u1"`, so the default's UUID flips `isSelf` false and ENABLES the row actions; `filter-ribbon-selects-open.spec.ts` — **caught only by the re-run**: `captureOpenSelect` screenshots `page.locator("body")`, not the Select popup, so the header label is in frame and all three baselines churned by 320 px on BOTH mobile projects while desktop absorbed it. The removal was reverted. Each of the 4 kept, plus the 5 MEMBER and 2 401 overrides, now carries an inline `Story 54.2 AC-8 — KEPT, LOAD-BEARING` comment saying why.
  status: **RESOLVED by Story 54.2 (2026-07-31)** for the consolidation itself. Recorded because § 2 V-3's table is cited elsewhere and its 8/5/2 split should not be carried forward as fact — the measured split is 4 removed / 11 kept.

## Deferred from: code review of 53-4-android-chromium-lightbox-fit-repair (2026-08-01)

Written by the native `bmad-code-review` (Blind Hunter + Edge Case Hunter + Acceptance Auditor, run inline) on the dirty working tree at `fix/E53.4-android-chromium-lightbox-fit-repair`, baseline `36edc9c`, and by the follow-on repair pass that applied its two blocking findings. **NOT** an Ezop signature and **NOT** human review. The review's two blocking findings and its `offsetLeft`/`offsetTop` coverage finding were RESOLVED in the repair pass (see the story's § 12 "Review-fix pass"); the two entries below are the ones deliberately routed instead.

- source_spec: `_bmad-output/implementation-artifacts/53-4-android-chromium-lightbox-fit-repair.md`
  summary: **RULED TRADE-OFF, recorded rather than silently accepted — page-pinch no longer magnifies the lightbox.** After 53.4 the dialog is `0.98 x visualViewport.width` CSS px, so at page scale `s` it occupies `0.98 x L` device px *whatever `s` is*: pinching the page while the viewer is open now re-fits the viewer instead of enlarging it.
  evidence: `ImageFullscreenViewer.tsx` `syncVisibleRegion` — `--viewer-w` is recomputed from `visualViewport.width` on every `resize`/`scroll`, and `visualViewport.width` shrinks in inverse proportion to the scale. Measured in `image-viewer-containment.spec.ts` (the two page-scale tests): at scale 2 on `mobile-light` the dialog is `192.56` CSS px inside a `196.5` CSS px region — the same on-screen size as at scale 1. This is the DIRECT consequence of AC-4's "always fit the visible region" and is the behaviour the repair exists to produce; it is not a defect. It matters because `user-scalable=no` is banned (`EXPERIENCE.md:291`, `architecture.md:3374`) precisely so pinch stays available, and it brushes `NFR26-A11Y-1`.
  status: **ACCEPTED as the intended consequence, OPEN as a question for the § 9 physical retest.** Rationale for accepting: magnification of the PHOTO is what `FR26-VIEW-1` promises and it is delivered by the in-viewer zoom (>= 4x, three visible single-pointer controls, `AC-5` pins), which is reachable one-handed and — unlike a page pinch — cannot push the close button off-screen. The surface still honours the pinch BAN on the underlying page; what changed is that a page pinch no longer double-magnifies the viewer chrome. Owner: whoever runs the § 9 retest should note whether a real user reaches for a page pinch inside the viewer; if they do, the follow-up is a UX call (route to `bmad-correct-course`), not a geometry change.

- source_spec: `_bmad-output/implementation-artifacts/53-4-android-chromium-lightbox-fit-repair.md`
  summary: **The visual-viewport compensation is validated on Chromium ONLY.** The repair assumes `position: fixed` is laid out in the LAYOUT viewport, so it ADDS `visualViewport.offsetLeft`/`.offsetTop` to place the dialog. An engine that anchored fixed elements to the VISUAL viewport instead would be compensated twice.
  evidence: The whole visual matrix is four Chromium projects (`tests/visual/playwright.config.ts` — `Desktop Chrome` x2, `Pixel 5` x2); nothing in the repo runs WebKit or Firefox. The assumption is measured true on Chromium (`image-viewer-containment.spec.ts`, "the viewer follows the visible region when the user pans a zoomed page": with the offset terms removed the dialog sits at `1.95` while the visible region starts at `120`, i.e. the engine did NOT move it — fixed is anchored to the layout viewport). It is also what the CSSOM View spec says, but that is a citation, not a measurement, and this story exists because a citation-grade premise hid a defect for a month.
  status: OPEN as a **risk without coverage**, not a known defect — no evidence of a divergent engine was found, and adding a WebKit/Gecko project is a test-infrastructure decision well outside a bugfix story's scope. Owner: whoever next revisits the browser matrix. If a non-Chromium project is ever added, the panned-page test above is the one that answers this in a single run.

## Deferred from: code review RE-RUN #2 of 53-4-android-chromium-lightbox-fit-repair (2026-08-01)

Written by the native `bmad-code-review` re-run #2 (log `.hermes/run-logs/t_962a3d46-bmad-code-review-rerun-2-20260801_041133.log`, verdict `REQUEST_CHANGES`) and by review-fix pass 3, which resolved its three blocking findings in code and tests. **NOT** an Ezop signature and **NOT** human review. BLOCKING-5 (the missing `visualViewport` refit), BLOCKING-6 (the toolbar/root containment bound) and BLOCKING-7 (the overclaimed hit test) were **FIXED**, not routed — see the story's § 12 "Review-fix pass 3". The single entry below is what BLOCKING-6's measurement surfaced and could not fix inside this story's scope.

- id: DW-53.4-C
  source_spec: `_bmad-output/implementation-artifacts/53-4-android-chromium-lightbox-fit-repair.md`
  summary: **The viewer stops fitting the visible region above ~3× page scale on a phone, and the close button is covered above ~3.06×. Story 53.4 raised the bound and does NOT claim it removed — AC-2 is satisfied over a MEASURED ~1×–3× range, not unlimited.** `apps/web/index.html:5` sets no `maximum-scale`, so Chromium grants a user up to 5× on the layout viewport; between ~3.06× and 5× the viewer is degraded in two ways this story deliberately did not repair.
  evidence: Measured this run on `mobile-light` (393px layout viewport, portrait) **after** the pass-3 repair, toolbar top edge vs the visible region — scale 3: frame 150.20, toolbar 2 rows × 40 = 96, slack **+44.25**, contained; 3.5: frame 117.33, slack +10.52, contained; **4: frame 92.66, slack −14.81, OUT**; 4.5: 3 rows = 144, slack −82.50, OUT; 5: slack −98.25, OUT. Separately and *lower*, the wrapped toolbar grows upwards from its bottom anchor and covers the close button's centre from **~3.06×** — measured onset 3.05× (close topmost, +0.44px) → 3.1× (the toolbar is topmost) → 3.2× (the zoom-out button is topmost). Both have the same cause: the `h-20` thumb strip is a fixed 80px of a dialog that is only 172px tall at 4×, so the vertical budget — not the width the repair fixed — becomes the binding constraint. Related but distinct, and unfixed for the same reason: `--viewer-img-max-h` is `calc(${height}px - 5rem)`, which goes negative below `visualViewport.height` 84.2px (reachable on a 393px-tall LANDSCAPE viewport at ~4.7×) and clamps `max-height` to 0. Pre-53.4 the shape existed but `dvh` is the layout viewport and invariant under a page pinch, so **reachability via pinch is new**. The two desktop projects are not affected in the tested range: Chromium refuses the emulation above 4× there (`Emulation.setPageScaleFactor(4.5)`/`(5)` both leave `visualViewport.scale` at 4) and the viewer is contained at 3/3.5/4 (+90.00/+56.56/+31.50).
  status: **OPEN — owed, and explicitly NOT claimed by Story 53.4.** Repairing it means changing *what the viewer renders* at tiny heights — dropping, shrinking or re-anchoring the thumb strip and the toolbar — which is a viewer redesign, and the review that raised it (BLOCKING-6) ruled that out of scope for a repair story in the same sentence that raised it. The regression test is pinned at **page scale 3** on all four projects and its +4.20px of close-button slack is deliberately the last safe step, so it is the tripwire that fires if anything grows the toolbar or the strip. Owner: whoever next revisits the fullscreen viewer's small-viewport layout; if `maximum-scale` is ever considered as the cheap alternative, note that capping user zoom is banned by `EXPERIENCE.md:291` / `architecture.md:3374` and is **not** an available answer here.
