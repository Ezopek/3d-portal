---
baseline_commit: 0df663e12d2718dde441dd89418cae0a301386fc
---

# Story 49.5: Admin category governance

Status: done

<!-- Provenance. CREATE: native bmad-create-story (action=create, menu CS), 2026-07-28, Claude Sonnet 5 agent session, routed via the repository's native bmad-help -> _bmad/_config/bmad-help.csv row `bmad-create-story` (CS / create; preceded-by bmad-sprint-planning, followed-by bmad-create-story:validate, required=true). This session's `.claude/skills/` directory (the actual native skill bodies, gitignored per `.gitignore:143`) was ABSENT from this isolated `git worktree add` checkout — `.claude/skills/` is untracked/regenerated-by-installer content and `git worktree add` only materializes tracked files. Root-caused against `/home/ezop/repos/3d-portal` (same repo, `_bmad/_config/manifest.yaml` byte-identical at version 6.10.0) and repaired by copying that worktree's `.claude/skills/` (90 skill directories, purely local/gitignored, zero git diff) into this worktree before invoking any skill — disclosed as §17.6. Customization resolved via `python3 _bmad/scripts/resolve_customization.py --skill <skill-root> --key workflow`: no team override (`_bmad/custom/bmad-create-story.toml` absent) and no user override; `activation_steps_prepend`, `activation_steps_append` and `on_complete` all empty; `persistent_facts` = `file:{project-root}/**/project-context.md`, resolved to `_bmad-output/project-context.md` (321 lines, loaded). `discover-inputs.md` executed (epics.md loaded selectively for the Initiative 26 / Epic 49 section; architecture.md loaded selectively for Decisions AX/AY/AZ; no PRD/UX file pattern matched beyond what epics.md already embeds). One `Explore` research subagent was dispatched for exhaustive current-code grounding (tag_group_admin_router.py, audit.py, admin_service.py, admin_schemas.py, test precedents) — disclosed as §17.4, a partial (not full) use of the base workflow's subagent recommendation given this session's serial-tool-call operating mode. `checklist.md` executed against the drafted artifact before finalising (§6 below doubles as the exhaustive-analysis record the checklist requires). Baseline HEAD `0df663e12d2718dde441dd89418cae0a301386fc` on branch `docs/init26-e49-5-create` in worktree `/home/ezop/worktrees/3d-portal-e49-5-create`, working tree clean at start and clean at finish except this artifact and `sprint-status.yaml`. **VALIDATE was NOT run in the Create session** — that session's operating instructions explicitly scoped it to Create only ("Do not invoke validation"), a deliberate, disclosed narrowing of the base `bmad-create-story` cycle (49.1/49.2/49.3/49.4 all bundled Create+Validate in one session; that one did not) — see §17.1. G26-DEVGO is **NOT** granted by that pass. NO human review of any kind. No Ezop signature, no Ezop review, and no Laura review is recorded, implied or claimable from this document. No code, no test, no doc, no live DB touched. No commit, push, merge, deploy, migration, seed, or live action.

VALIDATE: native bmad-create-story (action=validate, menu VS), 2026-07-28, fresh independent Claude Sonnet 5 agent session, same isolated worktree and baseline `0df663e12d2718dde441dd89418cae0a301386fc` on branch `docs/init26-e49-5-create`. Routed via `AGENTS.md` / `CLAUDE.md` / `_bmad-output/project-context.md` / the Laura Agent Rulebook, then `_bmad/_config/manifest.yaml` (BMad 6.10.0) + `bmad-help.csv:27` (VS row: action=validate, phase 4-implementation, preceded-by `bmad-create-story:create`, followed-by `bmad-dev-story`, required=false) + `skill-manifest.csv:40`, confirming `bmad-create-story:validate` as the canonical action after Create. `.claude/skills/bmad-create-story/` was already present (Create pass's §17.6 repair) and used as-is; the Validate action's substance is `checklist.md`, executed under its own "When Running in Fresh Context" protocol — the story file and every source document re-derived independently rather than trusted. **Verdict: PASS** — full evidence, re-derived citations and the two explicit priority arbitrations are recorded in §16. Status advanced `ready-for-validation` → `ready-for-dev`; `epic-49` unchanged at `in-progress`. **G26-DEVGO remains NOT granted** — validation is not implementation authorization. NO human review of any kind in this pass either: no Ezop signature, no Ezop review, no Laura review recorded or implied. No product, test, config, or migration file touched; no live DB, no network, no commit, push, merge, deploy, migration, or seed. Only this artifact and `sprint-status.yaml` were written. -->

## 1. Story

As an **admin**,
I want **to create, rename/reorder/reparent and delete browse categories, and to atomically set (replace) the full set of categories assigned to a model**,
so that **the browse-category taxonomy stood up by Stories 49.1–49.3 can actually be curated — the eight starter categories from 49.2 are just a seed, and this is the only write surface that lets an admin correct, extend, or retire that taxonomy and assign models to it, with every change auditable.**

**Epic:** E49 — Browse-category data + additive API foundation (backend).
**Story key:** `49-5-admin-category-governance` *(renumbered from 49.6 by the 2026-07-26 controller review; there is no Story 49.6).*
**Requirements:** FR26-ADMIN-1 (admin CRUD/reorder/replace-set; 409-unless-detach delete; audit per write), FR26-CAT-3 (1–3 per model is an advisory warning owned by 52.2, not this story — see §10), FR26-CAT-4 (nullable `parent_id`; service-layer depth-2 ceiling; flat MVP UI).
**Architecture:** Decision AY "Admin governance" sub-section (`architecture.md:3334-3341`) is the canonical source for the write contract; Decision AX (`architecture.md:3285-3301`) supplies the entity shape and the depth-2/self-cycle service-layer mandate being enforced here.
**Depends on:** Story 49.1 (`done`, entities + migration, live at this baseline), Story 49.3 (`done`, read API — `GET /api/categories[/{slug}]`, `ModelDetail.categories`, unmodified by this story). **Hard prerequisite for** Story 52.2 (admin category management screen — the UI over this API).

**This story writes to `browse_category` and `model_browse_category`.** It is the first story in Initiative 26 to do so; every prior E49 story (49.1–49.4) was additive-schema or read-only.

## 2. Gate and authorization posture (truthful)

- **G26-DEVGO GRANTED 2026-07-28 by Laura/controller** under the operator's standing Initiative 26 delegation, after controller audit of the fresh native Validate `PASS`, the exact two-path workflow diff, clean `git diff --check`, and parsed sprint state. This is a Laura/controller authorization only: **never an Ezop signature, Ezop review, or claim that a human reviewed the artifact.** It authorizes native `bmad-dev-story` on an isolated Story 49.5 branch; the implementer still may not independently review, commit, push, merge, deploy, or touch live state.
- **No human has reviewed this document.** Not Ezop, not Laura, not any other agent acting as a reviewer — this holds for both the Create pass and the Validate pass. Every claim, citation, and design decision below is Claude/native-BMAD-session-only; the Validate pass re-derived them independently from source, which is agent-independence, not human sign-off.
- **No live-database action of any kind, in either pass.** All research in §6 and all re-derivation in §16 read source files and prior committed artifacts only; no test was executed, no throwaway SQLite database was built, no `pytest` command ran.
- **Live posture (inherited, not re-measured here):** at the 49.4 closeout the live database carried eight seeded `browse_category` rows (Story 49.2) and **zero** `model_browse_category` rows. Consequently, on first deploy of this story, every `POST/PATCH/DELETE /api/admin/categories` call against a real category will succeed with no assignment-conflict path exercised until an admin actually assigns a model — the 409-in-use and `detach=true` paths are real and tested here (§4, §5) but will not be naturally exercised on live data until 52.2 or a manual assignment exists.
- **G26-ROUTE-PATH, G26-CAT-SET, G26-UXGATE, G26-SCP-RATIFY — closed**; none of them gate this story. G26-MIGRATE does not apply (§3.1 — no DDL).

## 3. Binding constraints (a violation is a story defect, not a preference)

1. **No destructive DDL, no schema migration, no seed behaviour.** `browse_category` and `model_browse_category` are exactly the tables Story 49.1 shipped (`_entities.py:130-183`, `migrations/versions/0020_browse_categories.py`) — this story adds **zero** columns, indexes, or tables. `apps/api/scripts/seed_browse_categories.py` (Story 49.2) is not touched or re-run.
2. **No frontend/admin screen.** That is Story 52.2. This story ships API + tests + docs only. `apps/web/**` stays byte-unchanged.
3. **No category-assignment automation and no inference from tags.** Every write in this story is an explicit admin-initiated HTTP call. Nothing here reads `Tag`/`TagGroup`/`ModelTag` to auto-populate `model_browse_category`.
4. **`current_admin` only, on every new route.** No route in this story accepts an `agent` principal — a deliberate tightening relative to the tags precedent this story's replace-set endpoint is modelled on (§6 F-9); there is no `agent-write` tag anywhere in this story's surface.
5. **`Model` and existing `BrowseCategory*`/`ModelDetail`/read-router surfaces stay byte-unchanged.** `GET /api/categories[/{slug}]`, `GET /api/models?category=`, and `ModelDetail.categories` (all Story 49.3) are not touched. `apps/api/tests/test_sot_categories.py` and `test_sot_models_category_scope.py` pass byte-unmodified as non-regression witnesses.
6. **New schema identifiers are `BrowseCategory*`, never bare `Category*`.** `test_no_category_schemas_in_components` (`test_openapi_agent_surface.py:334-341`) hard-fails on any component schema `startswith("Category")`. No property named `category`/`category_id` may be added to `ModelSummary`/`ModelDetail`/`ModelCreate`/`ModelPatch`/`ShareModelView` (`test_no_category_properties_on_model_schemas`, `:344-354`).
7. **Depth-2 ceiling and self-cycle rejection are service-layer rules, not DDL.** `BrowseCategory.parent_id` stays a plain nullable self-FK with `ondelete="RESTRICT"` — no CHECK constraint, no trigger. All enforcement is in `admin_service.py` (§4 AC-12…AC-16, §6 F-4).
8. **Delete = `409 Conflict` while assignments exist, unless explicit `detach=true`** (payload of `?detach=true` query param). Detach + delete is **one transaction**; the audit row carries both the detached model ids and a count (epic:42 unbounded-audit-payload precedent applied by analogy, §6 F-6). A category with **children** is a **second, independent** conflict source (`parent_id` is also `RESTRICT`) that `detach=true` does **not** resolve — §6 F-4 records why this is a story-level decision the planning artifacts left open.
9. **`PUT /api/admin/models/{id}/categories` is whole-set replace, idempotent, explicit last-writer-wins.** No merge, no diff, no optimistic-concurrency precondition (no `revision` column, no `If-Match`) is built in this story — that is a named future trigger (§8.6), not a defect. The endpoint audits the resulting set; it must never claim conflict detection it does not have.
10. **`entity_type="browse_category"`** is added to `KNOWN_ENTITY_TYPES` (`app/core/audit.py:51-69`) with `browse_category.create` / `browse_category.update` / `browse_category.delete` actions; the model-scoped replace-set reuses the existing `entity_type="model"` / `action="model.update"` convention `replace_model_tags` already established (§6 F-9) — no new M:N-specific `entity_type` is introduced.
11. **CSRF and route-enforcement gates apply unmodified.** Every mutating route sits behind the existing prefix-based `csrf_guard` (`app/core/auth/csrf.py`) and resolves to `current_admin` in `test_route_enforcement_gate.py`'s auth-dependency scan — no edit to `_PUBLIC_ROUTES`, no new auth-dependency name introduced (§6 F-7, F-8).
12. **Strict RED → GREEN.** Every behavioural assertion in §4 is proven by a test observed failing before the production code satisfying it exists, with the failure output quoted verbatim by the dev pass in §15. A test that passes by construction must be labelled a structural/composition guard, never dressed up as a RED (§5 ordering rule).

## 4. Acceptance Criteria

### `POST /api/admin/categories` — create

**AC-1 — Success shape.** A valid `{slug, name_en}` (minimum) payload returns **201** + `BrowseCategoryAdminRead` — the 9-key `BrowseCategoryRead` set (`{id, slug, name_en, name_pl, description_en, description_pl, position, parent_id, model_count}`) **plus `inclusion_criterion`** (§6 F-3 — the public read contract deliberately omits it; the admin contract is where it is first exposed over HTTP). `model_count` is always `0` on create (no assignment can exist yet for a brand-new id).

**AC-2 — Defaults.** Omitting `name_pl`, `description_en`, `description_pl`, `inclusion_criterion`, `parent_id` leaves them `null`. Omitting `position` defaults to `0` (mirrors `TagGroupCreate.position: int = 0`).

**AC-3 — Slug uniqueness.** A `slug` colliding with an existing `browse_category.slug` (the `uq_browse_category_slug` unique index, `_entities.py:141`) returns **409**, not 422 and not 500.

**AC-4 — Auth matrix.** Anonymous → **401**. `member`/`agent` role → **403** (`admin_required`). `admin` → **201**. No route in this story ever accepts `agent`.

**AC-5 — CSRF.** A `POST` without `X-Portal-Client: web` returns **403 `csrf_required`** — proven by the existing, unmodified `test_csrf_middleware.py` staying green (prefix-based middleware, no per-route code); not a new test this story authors (§6 F-8).

**AC-6 — `parent_id` on create: unknown parent → 404.** A `parent_id` that does not resolve to any existing `browse_category.id` returns **404** (mirrors the "unknown model/tag id → 404" convention `PUT /models/{id}/tags` already establishes for referenced-resource validation, §6 F-9), not 422 and not a raw `IntegrityError`.

**AC-7 — `parent_id` depth ceiling on create.** A `parent_id` that itself has a non-null `parent_id` (i.e. is already a child) returns **422** `parent_not_root`. A brand-new category can only ever be created as a root (`parent_id: null`) or as a direct child of an existing **root** category — never as a grandchild.

### `PATCH /api/admin/categories/{category_id}` — rename / reorder / reparent

**AC-8 — Partial update, `exclude_unset`.** Only fields present in the request body change. `model_config = ConfigDict(extra="forbid")` — an unknown key returns 422. Returns **200** + `BrowseCategoryAdminRead`.

**AC-9 — Omitted vs. explicit null on NOT NULL fields.** `slug`, `name_en`, `position` are `NOT NULL` on the entity. Omitting any of them from the PATCH body leaves the current value untouched (Pydantic v2 skips `@field_validator` on an unset field). An **explicit** `{"slug": null}` / `{"name_en": null}` / `{"position": null}` in the body returns **422** — the same `_reject_explicit_null` pattern `TagGroupPatch` already uses (`admin_schemas.py:270-280`), applied to a new `BrowseCategoryPatch`. `name_pl`, `description_en`, `description_pl`, `inclusion_criterion`: an explicit `null` **clears** the field (200), matching `TagGroupPatch.name_pl`'s posture.

**AC-10 — Empty PATCH is a no-op that still audits.** `PATCH {}` on an existing category returns **200** unchanged, and still writes **exactly one** `browse_category.update` audit row with `before == after` — the unconditional-audit convention `update_tag_group` already establishes (`admin_service.py:1204-1205,1232-1240`). This is deliberate, not a bug: an admin action that changes nothing is still a recorded event.

**AC-11 — Slug uniqueness on rename.** Patching `slug` to a value already used by a **different** category returns **409**. Patching a category's `slug` to its **own current** `slug` (a no-op rename) succeeds with 200 (not a false-positive 409 against itself).

**AC-12 — Unknown category → 404.** `PATCH /api/admin/categories/{unknown-uuid}` returns **404**, not a silent no-op and not 422.

**AC-13 — `parent_id` self-cycle rejection.** `PATCH {"parent_id": "<own id>"}` returns **422** `self_cycle` and mutates nothing.

**AC-14 — `parent_id` depth ceiling on reparent (target side).** Setting `parent_id` to a category that is itself already a child (has a non-null `parent_id`) returns **422** `parent_not_root` — identical rule to AC-7, applied on update.

**AC-15 — `parent_id` depth ceiling on reparent (source side) — the case AC-7/AC-14 alone do not cover.** Setting `parent_id` (to any non-null root category) on a category that **currently has children** returns **422** `reparent_exceeds_depth` — because it would push those children to depth 3. Verified independently of AC-14: a category with zero children can always be reparented onto any valid root; a category with ≥1 child can never be reparented onto anything (only its `parent_id` can move **toward** `null`, never **away from** `null`, while it has children).

**AC-16 — Clearing `parent_id` (making a child a root again) always succeeds.** `PATCH {"parent_id": null}` on a child category returns 200 regardless of the category's own children (setting a category's parent to `null` can never increase any depth).

### `DELETE /api/admin/categories/{category_id}` — delete, with optional detach

**AC-17 — Clean delete.** A category with **no** model assignments and **no** children: `DELETE` (no query param, or `?detach=false`) returns **204**. One `browse_category.delete` audit row is written, `before` = the full bounded snapshot (§6 F-6), no `after`.

**AC-18 — 409 while model-assignments exist, `detach` omitted or `false`.** A category with ≥1 `model_browse_category` row and **no** children: `DELETE` returns **409** `category_in_use`. **Nothing is deleted, nothing is audited** — the request has no side effect (proven by an unchanged row count and an unchanged audit-row count for that `entity_id`, §5 T5).

**AC-19 — `detach=true` performs detach + delete atomically.** Same precondition as AC-18, with `?detach=true`: returns **204**. In **one transaction**: every `model_browse_category` row for that category is deleted, then the category row itself is deleted, then the category commits. One `browse_category.delete` audit row carries `before` = the full snapshot **plus** `detached_model_ids: [uuid, ...]` (every detached model id, as strings) **and** `detached_model_count: int` — both fields together, per §3.8 and the epic:42 precedent (§6 F-6). A subsequent `GET /api/models?category=<deleted-slug>` (via a fresh, still-existing category with the same slug — the slug itself does not survive) is not asserted here; the assignment rows themselves are proven gone by a direct row-count query in §5 T5.

**AC-20 — `detach=true` on a category with NO assignments is a harmless no-op flag.** `DELETE ...?detach=true` on a clean category (AC-17's precondition) still returns 204, still writes exactly one audit row, and the row carries **no** `detached_model_ids`/`detached_model_count` keys (they are only present when at least one row was actually detached) — `detach=true` never fabricates a non-empty detach record.

**AC-21 — Children are a second, independent 409 source that `detach=true` does NOT resolve.** A category with ≥1 child category (regardless of model assignments, regardless of `detach`): `DELETE` — with or without `?detach=true` — returns **409** `category_has_children`. Deleting a category's children (or reparenting them to `null`/elsewhere) first is a **separate prior request**; this story does not add a cascading-child-delete or auto-reparent-on-delete behaviour. This is a story-created-pass decision, not inherited from any planning artifact — see §6 F-4 for the full reasoning and why it is the correct minimal-diff resolution of the RESTRICT-on-`parent_id` hazard the planning artifacts did not address.

**AC-22 — Both conflict sources present.** A category with both ≥1 child **and** ≥1 model assignment: `DELETE ...?detach=true` still returns **409** `category_has_children` (children are checked first, unconditionally) and performs **zero** detach side effects — the model-assignment rows are untouched, proven by an unchanged row count.

**AC-23 — Unknown category → 404.** `DELETE /api/admin/categories/{unknown-uuid}` returns **404** regardless of the `detach` query param.

**AC-24 — Auth + CSRF matrix.** Same posture as AC-4/AC-5, applied to DELETE: anonymous 401, member/agent 403, admin 204/409/404 per the above; missing CSRF header → 403 `csrf_required` (existing middleware, no new test required).

### `PUT /api/admin/models/{model_id}/categories` — replace-set assignment

**AC-25 — Success, whole-set replace.** A valid `{"category_ids": [...]}` (any length, including empty `[]`) against an existing, non-soft-deleted model returns **200** + `list[BrowseCategorySummary]` (the embeddable 5-key shape `ModelDetail.categories` already uses, §6 F-3) — the resulting set, ordered `(position, slug)` matching the existing `list_browse_categories`/`ModelDetail.categories` ordering convention.

**AC-26 — Idempotent.** Calling the same `{"category_ids": [A, B]}` twice in a row against the same model produces the identical result both times (200, same resulting set) and writes **two independent** `model.update` audit rows (once per call) — idempotent means "same end state," not "audited once." Verified by asserting the row set is unchanged between calls, not by asserting only one audit row exists.

**AC-27 — Empty set clears all assignments.** `{"category_ids": []}` on a model with existing assignments returns 200 + `[]`, and every prior `model_browse_category` row for that model is gone. A model with **zero** categories remains fully valid and public (FR26-CAT-2 — no downstream visibility change; this story does not touch any read/visibility path).

**AC-28 — Duplicate ids in the payload → 400.** `{"category_ids": [A, A]}` (the same id twice) returns **400**, not a 500 from an `IntegrityError` on the composite PK `(model_id, category_id)`, and not a silent de-duplication. This is a **deliberate improvement** over the `PUT /models/{id}/tags` precedent, which the research for this story found has no such guard and would hit an uncaught `IntegrityError` on a literal-duplicate payload (§6 F-9) — Story 49.5 closes that gap for categories rather than copying it forward, and does **not** retroactively fix the tags endpoint (out of scope, not silently touched).

**AC-29 — Unknown category id in the payload → 404.** Any `category_ids` entry that does not resolve to an existing `browse_category.id` returns **404**, and the request has **no** side effect (no partial replace) — validation happens before any `ModelBrowseCategory` row is deleted or inserted.

**AC-30 — Unknown or soft-deleted model → 404.** `PUT /api/admin/models/{unknown-or-deleted-uuid}/categories` returns **404**, mirroring `_get_model_active`'s existing "absent or soft-deleted" LookupError semantics that `replace_model_tags` already relies on (§6 F-9) — no new soft-delete logic is introduced.

**AC-31 — Detach transaction rollback on mid-replace failure.** If validation (duplicate check or existence check) fails, **zero** `ModelBrowseCategory` rows are deleted — the existing set for that model is provably unchanged (row-count + row-identity comparison before/after the failed call). This proves the "validate everything, then mutate" ordering, not a partial replace.

**AC-32 — Audit fidelity.** Exactly one `model.update` audit row per successful `PUT` call, `entity_type="model"`, `entity_id=<model_id>`, `before={"category_ids": [...]}` (the pre-call set, as strings), `after={"category_ids": [...]}` (the post-call set, as strings) — the identical shape `replace_model_tags` already uses for `tag_ids` (§6 F-9), applied to categories. No merge/conflict-detection claim appears anywhere in the response, the audit row, or the route `description` (§3.9).

**AC-33 — Auth + CSRF matrix.** Anonymous 401, member/agent 403 (this is the deliberate tightening vs. the tags precedent — §6 F-9 records this as an open rationale gap the Validate pass should double-check, not a silent given), admin 200; missing CSRF header → 403 (existing middleware).

### Audit contract (cross-cutting)

**AC-34 — `entity_type="browse_category"` registered.** `app/core/audit.py::KNOWN_ENTITY_TYPES` gains `"browse_category"` with a doc-comment entry in the same style as the existing 15 entries (`:16-49`), naming the three actions and noting the delete-with-detach payload shape. `record_event`'s `ValueError` guard is unaffected for existing entity types (proven by every pre-existing audit-touching test staying green, §7 asserted-unchanged list).

**AC-35 — Bounded, truthful snapshot — no PII, no unbounded growth beyond the one documented exception.** `_browse_category_snapshot` carries exactly `{slug, name_en, name_pl, description_en, description_pl, inclusion_criterion, position, parent_id}` (UUIDs/strings/ints/null only — D-AUDIT-2 posture, §6 F-6). The **one** intentionally-unbounded field, `detached_model_ids` on delete, is the epic:42-precedented exception — always paired with `detached_model_count` so a reader never has to count array elements to know the scale, and is re-evaluated (not capped) if model-assignment cardinality per category grows large in practice, exactly mirroring the still-open epic:42 action item (§6 F-6). This story does **not** invent a truncation/cap mechanism — that would be scope creep beyond what the ratified precedent asks for.

### Non-regression

**AC-36 — Neighbouring read paths are untouched.** `GET /api/categories`, `GET /api/categories/{slug}`, `GET /api/models?category=<slug>`, `GET /api/tag-groups`, `GET /api/tags`, and the existing `POST/PATCH/DELETE /api/admin/tag-groups` + `POST /api/admin/tags` governance routes all pass their existing test suites **byte-unmodified**. `test_route_enforcement_gate.py`'s three tests pass unmodified with the four new routes registered (§6 F-7 — `current_admin` resolves to the already-recognized `_current_admin_dep`, no allowlist edit needed).

**AC-37 — OpenAPI honesty for the new, off-target-tag-set routes.** All four new routes carry `tags=["sot-admin-governance"]` (never `agent-write`), and each carries a non-empty `summary` and `description` — proven by two new tests mirroring `test_governance_routes_absent_from_agent_write_set` / `test_governance_routes_have_summary_and_description` (`test_openapi_agent_surface.py:257-297`), since `sot-admin-governance` sits outside `TARGET_ROUTER_TAGS` and is not auto-covered by the generic `test_every_admin_sot_operation_has_summary/description` (§6 F-5).

## 5. Tasks / Subtasks — strict RED → GREEN

> **Ordering rule.** Within each slice the test is authored and observed failing **before** the production symbol it exercises exists. Failure output is quoted verbatim into §15. A test that passes by construction (e.g. AC-20's "no detach keys on a clean delete" once T3 already exists) is either restructured to have a genuine RED, or explicitly labelled a structural/composition guard in both the test docstring and §15 — never dressed up as a RED.

- [x] **T0 — Baseline capture (no edits).**
  - [x] T0.1 Confirm clean tree at `0df663e`; run the regression set that must stay green and record pass counts verbatim: `.venv/bin/pytest -q tests/test_sot_categories.py tests/test_sot_models_category_scope.py tests/test_sot_admin_tag_groups.py tests/test_sot_admin_tags.py tests/test_route_enforcement_gate.py tests/test_openapi_agent_surface.py tests/test_csrf_middleware.py`.
  - [x] T0.2 Confirm (grep, not assumption) that `create_category`/`update_category`/`delete_category` genuinely do not exist in `admin_service.py` — the 42.4 story's own Dev Notes cite a now-stale template at those old line numbers (§6 F-1); do not follow that citation.

- [x] **T1 — Schemas: `BrowseCategoryCreate` / `BrowseCategoryPatch` / `BrowseCategoryAdminRead` / `ModelCategoriesReplace` (no route yet).**
  - [x] T1.1 **RED** — a schema-level unit test (or the first router test, whichever lands first — see T2.1) proving `BrowseCategoryPatch(extra="forbid")` rejects an unknown key and `BrowseCategoryPatch(slug=None)` raises at construction; both fail with `ImportError`/`AttributeError` before the class exists. Quote the failure.
  - [x] T1.2 Add `BrowseCategoryCreate`, `BrowseCategoryPatch` to `admin_schemas.py` (request DTOs, alongside `TagGroupCreate`/`TagGroupPatch`), and `ModelCategoriesReplace` (mirroring `TagsReplace`, `admin_schemas.py:129-143`).
  - [x] T1.3 Add `BrowseCategoryAdminRead(BrowseCategoryRead)` to `sot/schemas.py` (response DTO, alongside `BrowseCategorySummary`/`BrowseCategoryRead`), adding exactly `inclusion_criterion: str | None`.
  - [x] T1.4 **GREEN** — quote.

- [x] **T2 — `POST /api/admin/categories` (AC-1 … AC-7).**
  - [x] T2.1 **RED** — author `tests/test_sot_admin_categories.py`, new file. Success shape + defaults; slug conflict 409; auth matrix (401/403/403/201); unknown `parent_id` 404; non-root `parent_id` 422 `parent_not_root`. Observe route-absent 404s and quote.
  - [x] T2.2 Add `_browse_category_snapshot`, `create_browse_category` to `admin_service.py` (new section, mirroring `create_tag_group`'s `try: session.flush() except IntegrityError: rollback; raise ValueError("slug_conflict")` shape, plus the new parent-id existence + root-check validation ahead of the insert).
  - [x] T2.3 Add `KNOWN_ENTITY_TYPES` entry `"browse_category"` in `app/core/audit.py` (AC-34), with the doc-comment block.
  - [x] T2.4 Create `apps/api/app/modules/sot/browse_category_admin_router.py` (new file, mirrors `tag_group_admin_router.py`'s docstring/router-declaration shape verbatim: `router = APIRouter(prefix="/api/admin", tags=["sot-admin-governance"])`), with `POST /categories`.
  - [x] T2.5 Register the new router in `app/router.py`, immediately after `sot_tag_group_admin_router` (same registration block, same relative order as the existing governance router).
  - [x] T2.6 **GREEN** — quote.

- [x] **T3 — `PATCH /api/admin/categories/{category_id}` (AC-8 … AC-16).**
  - [x] T3.1 **RED** — extend `test_sot_admin_categories.py`: partial update; omitted-vs-explicit-null on `slug`/`name_en`/`position`; explicit-null clears `name_pl`; empty-patch no-op + audit; slug conflict (incl. no-op self-rename 200); unknown category 404; self-cycle 422; `parent_not_root` on reparent-target 422; `reparent_exceeds_depth` on reparent-source-with-children 422; clearing `parent_id` on a childful category succeeds. Quote each failure as it's added, or the first composite failure if authored as one RED batch — label accordingly in §15.
  - [x] T3.2 Add `update_browse_category` to `admin_service.py`: `patch.model_dump(exclude_unset=True)`; before/after snapshots; the reparent validation block (self-cycle check first, then "is the *target* parent itself non-root", then "does *this* category currently have children and is parent_id being set non-null") — all three checks independent and all three must be exercised (§4 AC-13/14/15 are three distinct tests, not one).
  - [x] T3.3 Add `PATCH /categories/{category_id}` to `browse_category_admin_router.py`, mapping `LookupError` → 404, `ValueError("slug_conflict")` → 409, `ValueError("self_cycle")`/`ValueError("parent_not_root")`/`ValueError("reparent_exceeds_depth")` → 422.
  - [x] T3.4 **GREEN** — quote.

- [x] **T4 — `DELETE /api/admin/categories/{category_id}` (AC-17 … AC-24).**
  - [x] T4.1 **RED** — extend `test_sot_admin_categories.py`: clean delete 204 + one audit row; 409 `category_in_use` with no side effect (assert row survives + assert no new audit row, mirroring the pattern §4 AC-18 requires); `detach=true` 204 + detach-and-delete-in-one-tx + audit row with both `detached_model_ids` and `detached_model_count`; `detach=true` on a clean category → 204 + audit row **without** those two keys (AC-20 — author this as an explicit assertion, not an assumption); `category_has_children` 409 with and without `detach=true` (AC-21); both-conflicts-present still 409 `category_has_children` with zero detach side effects (AC-22); unknown category 404. Quote failures.
  - [x] T4.2 Add `delete_browse_category(session, *, category_id, detach, actor_user_id)` to `admin_service.py`: look up category (404 if missing) → **query children first** (`select(BrowseCategory.id).where(BrowseCategory.parent_id == category_id)`) → if any, `raise ValueError("category_has_children")` **unconditionally, before even looking at `detach`** → else query `ModelBrowseCategory` rows for the category → if any and not `detach`, `raise ValueError("category_in_use")` → else (clean, or has-assignments-with-detach): if detaching, delete the `ModelBrowseCategory` rows and capture their `model_id`s for the audit payload; delete the category row; **one** `_audit_entity(action="browse_category.delete", entity_type="browse_category", ...)` call carrying the base snapshot plus (only if any were detached) `detached_model_ids`/`detached_model_count`; single `session.commit()`.
  - [x] T4.3 Add `DELETE /categories/{category_id}` to the router, `detach: bool = False` query param, mapping `LookupError` → 404, `ValueError("category_has_children")`/`ValueError("category_in_use")` → 409.
  - [x] T4.4 **GREEN** — quote.

- [x] **T5 — `PUT /api/admin/models/{model_id}/categories` (AC-25 … AC-33).**
  - [x] T5.1 **RED** — author `tests/test_sot_admin_model_categories.py`, new file, mirroring `test_sot_admin_tags.py`'s replace-tags test shapes: success + defaults ordering; idempotent double-call; empty-set clear; duplicate-id 400 with no side effect; unknown-category-id 404 with no side effect; unknown/soft-deleted model 404; rollback-proof (unchanged row set) on a failed validation call; audit row shape (`before`/`after` keysets); auth matrix incl. agent → 403 (the deliberate tightening, AC-33). Quote failures.
  - [x] T5.2 Add `replace_model_categories` to `admin_service.py`, mirroring `replace_model_tags` (`admin_service.py:807-859`) exactly in structure but adding the **duplicate-id pre-check** `replace_model_tags` itself lacks (AC-28 — validate `len(payload.category_ids) == len(set(payload.category_ids))` before touching the session, `raise ValueError("duplicate_category_ids")` if not) and validating every id against `BrowseCategory` (not `Tag`) before any delete/insert.
  - [x] T5.3 Add `PUT /api/admin/models/{model_id}/categories` to `browse_category_admin_router.py` (not `sot/admin_router.py` — this is an admin-only, non-`agent-write` route; keeping it beside the category CRUD keeps the whole category-write surface in one cohesive, easily-audited module). Map `LookupError` → 404, `ValueError("duplicate_category_ids")` → 400, `ValueError(f"category not found: {cid}")` → 404.
  - [x] T5.4 **GREEN** — quote.

- [x] **T6 — OpenAPI honesty + route-enforcement non-regression (AC-36, AC-37).**
  - [x] T6.1 Extend `test_openapi_agent_surface.py`'s `_GOVERNANCE_ROUTES` set with the four new `(METHOD, path)` tuples, extending — not duplicating — `test_governance_routes_absent_from_agent_write_set` and `test_governance_routes_have_summary_and_description` coverage (§6 F-5). **RED** first (the new tuples fail against the pre-T2…T5 OpenAPI doc, since the routes don't exist yet) — fold this into T2.1/T3.1/T4.1/T5.1's RED batches rather than a separate isolated RED, and say so plainly in §15.
  - [x] T6.2 Run `tests/test_route_enforcement_gate.py` and `tests/test_no_category_schemas_in_components`/`test_no_category_properties_on_model_schemas` (`test_openapi_agent_surface.py`) **unmodified** — quote passes.
  - [x] T6.3 Run `tests/test_csrf_middleware.py` **unmodified** — quote pass (proves AC-5/AC-24/AC-33's CSRF posture without any story-specific edit).

- [x] **T7 — Scope + quality (§7).**
  - [x] T7.1 `.venv/bin/ruff format --check .` and `.venv/bin/ruff check .` in `apps/api`.
  - [x] T7.2 Full API suite: `.venv/bin/pytest -q` in `apps/api`.
  - [x] T7.3 `git diff --name-only` + `git status --porcelain` proving the §7 file set exactly, and `git diff --name-only` returning empty for every AC-36 byte-unchanged path.

- [x] **T8 — Controller-owned closeout (leave UNCHECKED by the implementer).**
  - [x] T8.1 Frozen final `infra/scripts/check-all.sh` 16/16 on the final commit. Controller evidence: `.hermes/run-logs/check-all-e49-5-rerun-20260728_164803.log` → `passed: 16`, `all green.`, `CHECK_ALL_RC=0` after provisioning the worktree-local ignored toolchains (`workers/render/.venv`, `apps/web/node_modules`) and restoring `core.hooksPath=.githooks`.
  - [x] T8.2 Determinism triple (NFR26-DETERMINISM-1). Controller evidence: `.hermes/run-logs/e49-5-determinism-triple-20260728_170039.log` → focused `tests/test_sot_admin_categories.py tests/test_sot_admin_model_categories.py` passed **72/72** three consecutive times, each `72 passed, 124 warnings`.
  - [x] T8.3 Native `bmad-code-review`, then independent `laura-aider-review-diff` per the Laura Agent Rulebook (Aider routine default; Codex fallback/high-stakes only; Gemini not a default reviewer). Evidence: native `.hermes/run-logs/e49-5-native-approve-review-20260728.md` verdict `APPROVE`; Aider `.hermes/run-logs/e49-5-aider-review-20260728_164015.log` verdict `APPROVE`.
  - [x] T8.4 One commit, ff-only merge, push, deploy. Controller evidence: implementation commit `52519d851444828371b987eb2ab7cb6f2c778078` was fast-forward merged to `main`, pushed to `origin/main`, deployed by `infra/scripts/deploy.sh` with `DEPLOY_RC=0`, `infra/.last-deploy-sha=52519d851444828371b987eb2ab7cb6f2c778078`, .190 containers running, release symbolication/runbook fingerprint OK, and live smoke `GET /api/health -> 200` plus `/api/openapi.json` containing all three new admin category paths.

### Review Findings

Written by the native `bmad-code-review` (CR) pass, 2026-07-28 — **verdict `REQUEST_CHANGES`**. Full evidence, all three layer outcomes, the complete 13-path scope audit, the commands actually executed with verbatim output, and the arbitration of prediction deltas A/B are in the durable report `/home/ezop/repos/3d-portal/.hermes/run-logs/e49-5-native-review-20260728.md`. Severities are the review orchestrator's own (per the skill's step-03 rule that subagent severities are disregarded). NO human review of any kind.

**Repair status (native `bmad-dev-story` repair pass, 2026-07-28).** All **6 patch** findings are repaired and the **1 decision-needed** finding is arbitrated by Laura/controller — each box below is ticked only where that is literally true, with the repair named inline. The **8 defer** findings stay deferred, are unchanged in substance, and were additionally appended to the canonical ledger `_bmad-output/implementation-artifacts/deferred-work.md` under the heading `## Deferred from: code review of 49-5-admin-category-governance (2026-07-28)`, closing the CR pass's own disclosed deviation #2. Evidence for every repair is in §15 → "Post-review repair pass". NO human review of any kind in this pass either.

**Decision needed (1) — ARBITRATED**

- [x] [Review][Decision] **ARBITRATED 2026-07-28 by Laura/controller: PRESERVATION-FIRST, no behaviour change.** Under the standing Initiative 26 delegation — a controller product-default decision, **never an Ezop signature and never a human review**. ALL `model_browse_category` rows, including rows whose model is soft-deleted, continue to make an ordinary DELETE return `409 category_in_use`. Rationale: the alternative silently destroys category relationships that `restore_model` may need, and filtering the delete gate to live models would run `session.delete(cat)` straight into the RESTRICT FK. Public/admin `model_count` remains the Decision-AY count of distinct NON-DELETED models and may therefore truthfully read `0` while a protected soft-deleted assignment exists — an accepted, now-documented consequence, not a defect. `detach=true` stays the explicit destructive opt-in. **Explicitly NOT done:** no implicit detach, no `deleted_at` filter on the delete gate, no broadening of the API response shape. Pinned end to end by `tests/test_sot_admin_categories.py::test_soft_deleted_model_assignment_is_preserved_until_explicit_detach` (active assignment → `model_count` 1 → soft-delete → `model_count` 0 with the row still present → ordinary DELETE 409 with the assignment preserved → `?detach=true` 204 removing it, audit row naming the detached model). The service docstring now states the posture. Original finding text retained below for the record: Soft-deleted-model assignments make DELETE return an invisible, unclearable `409 category_in_use` (medium) — `browse_category_model_count` filters `Model.deleted_at IS NULL` (`admin_service.py:1322-1329`) but `delete_browse_category`'s assignment pre-query does not (`:1519-1523`), and `soft_delete_model` (`:309-341`) leaves the join rows. Single-user, no race: assign M to C, soft-delete M → `GET /api/categories` and `PATCH /api/admin/categories/{C}` both report `model_count: 0`, then DELETE returns 409 with no read surface exposing the cause, and `PUT /api/admin/models/{M}/categories` 404s (`_get_model_active`, `:1581`) so the blocking rows cannot be cleared non-destructively. `?detach=true` is the only escape and silently destroys the assignments `restore_model` (`:273`) would need. Confirmed by execution by two independent layers. No AC is violated — AC-18's literal text is satisfied. **Needs a product decision because the obvious fix is a regression:** filtering the delete gate by `deleted_at` would run `session.delete(cat)` straight into the RESTRICT FK and return an unhandled 500. Options: implicitly detach dead assignments; surface a separate dead-assignment count; or just name the cause in the 409 body.

**Patch (6) — ALL REPAIRED**

- [x] **REPAIRED** — two new guards, and the story wording corrected in §15's Completion Notes. The shared-helper option was evaluated and **declined**: the read-side definition is a whole-table `GROUP BY` and the admin one needs exactly one category, so delegating would trade a scoped query for a full-table aggregate and would import a private symbol across the read/write boundary — not "genuinely smaller or clearer". Decision AY's no-disagreement property is pinned directly instead, by `test_browse_category_model_count_agrees_with_read_side_for_every_category` (compares BOTH definitions over EVERY category in the DB, on a fixture mixing counts of 3, 1, 0 and a soft-deleted-only category) plus `test_browse_category_model_count_non_zero_excludes_soft_deleted_models` (asserts the non-zero path on all three surfaces at once: service helper, admin write response, public `GET /api/categories`). Decision AY semantics unchanged. Non-vacuity proven by mutation probe (§15). Original finding: `browse_category_model_count` is a second, independent definition of a field Decision AY requires to never disagree, with zero non-zero-path coverage (medium) [apps/api/app/modules/sot/admin_service.py:1314-1331] — Decision AY: "`model_count` = distinct non-deleted models assigned, computed by the same shared-helper approach … so the two can never disagree", vs the read-side `sot/service.py:176-195`. All six `model_count` references in the two new test files assert `0` (`tests/test_sot_admin_categories.py:255`), so the join, the `deleted_at` filter and the agreement with the read side are untested — and that filter is the exact semantics the Decision finding above turns on. Probed directly and currently correct; the gap is drift risk plus an untested production branch under a strict RED→GREEN story and AGENTS.md's TDD rule.
- [x] **REPAIRED** — `delete_browse_category`'s `session.delete(cat)` + `session.commit()` are now wrapped in `except IntegrityError`, mirroring the ratified `delete_tag` rollback/error mapping. Because SQLite does not name which FK fired, the cause is **re-derived after the rollback**, children first — so children-first precedence and `category_in_use` semantics both survive the race, and the rolled-back transaction leaves no audit residue (asserted). The assignment pre-query was extracted to `_browse_category_assignments` so both halves of the race are reproducible in-process. The docstring's "never by catching an IntegrityError" claim is corrected in place. Genuine RED first (§15): the child race raised `sqlalchemy.exc.IntegrityError: (sqlite3.IntegrityError) FOREIGN KEY constraint failed [SQL: DELETE FROM browse_category WHERE browse_category.id = ?]` straight out of the router. Original finding: `delete_browse_category` has no `IntegrityError` safety net, so the documented 409 becomes an unhandled 500 (medium) [apps/api/app/modules/sot/admin_service.py:1547-1548] — the docstring at `:1503-1505` claims conflicts are "never" detected by catching `IntegrityError`; a child or assignment created between the pre-query and the commit fires the RESTRICT FK and escapes the router (`browse_category_admin_router.py:173-176` maps only `LookupError`/`ValueError`). The ratified precedent handles it: `delete_tag` catches `IntegrityError` → `ValueError("tag_in_use")` (`:1076`). Confirmed by both layers via forced in-process interleave; realistic probability is low (SQLite single-writer, single admin), so this is contract-honesty/robustness rather than an operational hazard.
- [x] **REPAIRED** — both flush handlers (create and update) now call the new `_raise_browse_category_flush_error(session, exc, parent_id)`, which rolls back and then applies the smallest deterministic mapping: **parent disappeared → `LookupError("parent not found")` → 404**, everything else → the shipped `ValueError("slug_conflict")` → 409. Existing duplicate-slug behaviour is preserved and re-pinned by a new non-regression test that sends a colliding slug together with a perfectly valid `parent_id` (still 409). Genuine RED first (§15): both probes returned `AssertionError: {"detail":"slug already exists"} / assert 409 == 404`. Original finding: Blanket `except IntegrityError → slug_conflict` mislabels a `parent_id` FK failure as `409 "slug already exists"` (low) [apps/api/app/modules/sot/admin_service.py:1389-1393, :1461-1465] — safe for `TagGroup` (no second FK), newly wrong for `BrowseCategory`. Parent deleted between `_validate_parent_is_root` and the flush → 409 blaming a provably unique slug; retrying with a new slug never helps. Confirmed by execution.
- [x] **REPAIRED** — renamed `test_retired_taxonomy_admin_crud_route_is_gone` → **`test_admin_category_write_carries_no_recursive_taxonomy_contract`**, which is what the body actually asserts today (the route is deliberately PRESENT and returns 200 with the flat Initiative 26 shape). Body unchanged; the docstring now records the old name and why it was misleading. The read-side sibling `test_retired_taxonomy_read_route_is_gone` is Story 49.3's surface and was deliberately left untouched. Original finding: The re-pointed D-1 guard keeps a name asserting the opposite of what it now tests (low) [apps/api/tests/test_openapi_agent_surface.py:451-527] — `test_retired_taxonomy_admin_crud_route_is_gone` now asserts the route is present and returns 200. The re-point itself is arbitrated LEGITIMATE (see §20), but the name misdescribes the test for every future reader and grep, and the guard is now coupled to Story 49.5's live response contract.
- [x] **REPAIRED** — `test_create_category_201_admin_read_shape` now asserts `set(after.keys()) == {…the 8 keys…}` on the `browse_category.create` audit row, matching the equality already present on the update and delete paths. Non-vacuity proven by mutation probe (§15): dropping `inclusion_criterion` from `_browse_category_snapshot` fails it. Original finding: AC-35's 8-key snapshot equality is not asserted on the create path (low) [apps/api/tests/test_sot_admin_categories.py:259-262] — the exact-keyset equality exists for update (`:438-447`) and delete (`:619-628`); create asserts only `after["slug"] == slug`, so a snapshot drift confined to `browse_category.create` would go undetected.
- [x] **REPAIRED** — (a) AC-25's `(position, slug)` tie-break is now exercised by `tests/test_sot_admin_model_categories.py::test_replace_categories_orders_ties_by_slug`: three categories SHARE one position and are seeded in reverse slug order, with a fourth at a lower position proving `position` still dominates, so a `position`-only sort cannot pass by accident (mutation probe confirms, §15). (b) `test_delete_unknown_category_404` is parametrised over all three spellings `""` / `"?detach=true"` / `"?detach=false"`, matching AC-23's "regardless of the `detach` query param". Original finding: Two AC assertions are narrower than their AC text (low) — AC-25's `(position, slug)` slug tiebreak (`admin_service.py:1624`) is never exercised because `tests/test_sot_admin_model_categories.py:118-138` uses distinct positions 1 and 9; AC-23 says 404 "regardless of the `detach` query param" but `test_delete_unknown_category_404` (`tests/test_sot_admin_categories.py:731-736`) omits `?detach=false`.

**Deferred (8) — unchanged in substance, and now also in the canonical ledger.** Each of the eight was appended verbatim in substance to `_bmad-output/implementation-artifacts/deferred-work.md` under `## Deferred from: code review of 49-5-admin-category-governance (2026-07-28)` by the repair pass, deduplicated against the sixteen pre-existing headings (no 49.5 heading existed; the test-order entry cross-references the distinct 49.4 auth/admin-users coupling rather than restating it). No unrelated ledger entry was rewritten. Three entries additionally record what the repair pass DID and did NOT change nearby, so the ledger does not go stale: the depth-2 reparent race is untouched by the delete-path safety net; `position` overflow is still uncaught because finding #4 re-derives an `IntegrityError`'s cause without widening the caught type; and the efficiency entry names the new parity test that any `model_count` rewrite must keep green.

- [x] [Review][Defer] Depth-2 ceiling is an unlocked read-then-write check; interleaved reparents produce a persistent depth-3 chain (low) [apps/api/app/modules/sot/admin_service.py:1445-1451, :1371-1372] — deferred: inherent to the ratified design, not a deviation from it. Decision AX mandates service-layer-only enforcement; §3.9/§8.6 explicitly scope out any concurrency mechanism, which the story names as a future trigger. Confirmed by both layers on the PATCH and POST paths via forced interleave.
- [x] [Review][Defer] Replace-set audit `before`/`after` are order-sensitive lists of an unordered set (low) [apps/api/app/modules/sot/admin_service.py:1593, :1603, :1613-1614] — deferred: AC-32 specifies only shape and is met; re-sending the same set reordered logs `before != after` for an unchanged end state, and `tests/test_sot_admin_model_categories.py:141-161` passes by construction. Sorting both sides needs a matching test edit, out of a review's scope.
- [x] [Review][Defer] `BrowseCategoryCreate` and `ModelCategoriesReplace` lack `extra="forbid"` (low) [apps/api/app/modules/sot/admin_schemas.py:288-300, :349-370] — deferred, exact ratified precedent (`TagGroupCreate:234`, `TagsReplace:129-143`); tightening it is cross-cutting into tags. Confirmed: POST with a typo'd key → 201 with the field silently null; `PUT {"category_ids": [], "catgory_ids": [<uuid>]}` → 200 clearing every assignment, the sharper case since the PUT is destructive.
- [x] [Review][Defer] `slug` has no format or length validation (medium) [apps/api/app/modules/sot/admin_schemas.py:302] — deferred: `TagGroupCreate.slug` is identical, and constraining the format adds a rejection AC-1/AC-2 do not specify. Confirmed: `POST {"slug":"a/b"}` → 201, then `GET /api/categories/a/b` and `/a%2Fb` both 404 — listed by `GET /api/categories` but permanently unreachable by its own canonical by-slug contract. Whitespace-only, mixed-case and 5000-char slugs also accepted; `_slugify` (`admin_service.py:73`) is never called.
- [x] [Review][Defer] `position` is an unbounded `int`; `2**63` reaches the driver and 500s (low) [apps/api/app/modules/sot/admin_schemas.py:308, :341] — `OverflowError: Python int too large to convert to SQLite INTEGER` at `admin_service.py:1390`/`:1462`, uncaught by the `except IntegrityError` blocks. Deferred: precedent `TagGroupCreate.position: int = 0` is identical. NOTE — the Edge Case Hunter's Postgres 32-bit overflow variant does NOT apply; `app/core/config.py:31` is SQLite-only.
- [x] [Review][Defer] `inclusion_criterion` is writable and echoed on writes but exposed by no read endpoint (low) [apps/api/app/modules/sot/schemas.py:122-133] — deferred: outside this story's ratified scope. Decision AY authorises exactly four write routes, §10 lists no admin read, and the public omission is ratified (49.3 §6 F-12). Reading the value today requires a mutating `PATCH {}` that also writes an audit row, and `seed.py` populates it for all eight seeded categories. Belongs to Story 52.2, the direct consumer.
- [x] [Review][Defer] Efficiency cluster (low) — `browse_category_model_count` materialises rows and calls `len()` instead of `func.count()` unlike its read-side sibling (`admin_service.py:1322-1330` vs `service.py:188-194`); `replace_model_categories` does one `session.get` per id (`:1586-1588`) with no `max_length` on `category_ids` (`admin_schemas.py:370`); `delete_browse_category` detaches with a per-row ORM delete loop over fully-loaded entities (`:1519-1521`, `:1534-1536`). Measured: a PUT with 8 categories issued 13 cursor executions. Deferred: no operational consequence at this repo's scale, and NFR26-PERF-1's constant-query mandate governs the `q` search path, not this surface.
- [x] [Review][Defer] Pre-existing test-order fragility, NOT caused by this story (low) — `pytest tests/test_csrf_middleware.py tests/test_sot_categories.py` → `12 failed, 16 passed`, every failure `401 {"detail":"invalid_access"}`; captured stderr shows a 1-byte HMAC key leaking across files. Both files and `tests/conftest.py` are byte-unmodified by this diff and nothing here touches auth. The default full-suite order is green (re-derived `1905 passed, 3 skipped`). Recorded so it is understood that the §15 T6.2 witness command's file ordering is load-bearing for its own green result.

**Dismissed as noise (6):** the claim that `tests/test_sot_admin_categories.py:135-136` overclaims typo→422 (its docstring is explicitly scoped to `BrowseCategoryPatch`); the claim that the D-1 re-point deleted coverage of other recursive CRUD routes (the baseline body probed exactly one path and never carried it); the Postgres 32-bit `position` overflow (SQLite-only deployment); `?detach=yes|1|on` also activating detach (standard FastAPI bool coercion); `_admin_read`'s post-commit count transaction (cosmetic, folded into the efficiency cluster); parent+child co-assignment being unruled and PATCH not documenting LWW (no artifact states a co-assignment rule, and PATCH-level LWW is universal in this repo).

### Re-review Findings (native `bmad-code-review`, RE-REVIEW 2026-07-28)

Written by a fresh, independent native `bmad-code-review` pass run after the repair pass — **verdict `REQUEST_CHANGES`**. Neither the prior review nor the repair session's `subtype=success` was trusted; every closure claim was re-derived from source and the load-bearing ones re-proved by this pass's own mutation probes (file restored byte-identically each time, `sha256(admin_service.py)` = `ef7d124c11a0896a…`). All three adversarial layers ran (`failed_layers` empty). Full evidence, the complete 14-path audit, the prior-finding closure matrix and all deviations are in the durable report `/home/ezop/repos/3d-portal/.hermes/run-logs/e49-5-native-rereview-20260728.md`. Severities are this pass's own (step-03 rule 4). **NO human review of any kind.**

**Prior findings — all closed.** #2, #3, #4, #11, #12 and #13 are each independently CLOSED (mutation-proven). The #1 decision-needed finding's controller preservation-first arbitration is **UPHELD**: Decision AY's ratified delete policy says "409 Conflict while assignments exist" with no live/soft-deleted qualifier, so blocking on a soft-deleted-model assignment is the literal reading, `model_count`'s "distinct non-deleted models" definition is separately truthful, and the alternative would run `session.delete(cat)` into the RESTRICT FK. Both disclosed prediction deltas re-arbitrate as legitimate and minimal. The eight `defer` findings are truthfully ledgered (`deferred-work.md` +29/-0, 16 → 17 headings, exactly 8 entries) and conceal no unmet AC or safety defect. **No AC is violated and no binding constraint is breached.**

**Patch (4) — ALL REPAIRED by the post-re-review `bmad-dev-story` pass, 2026-07-28.** Each box below is ticked only where that is literally true, with the repair named inline; evidence is in §15 → "Post-RE-REVIEW repair pass". The **7 defer** findings stay deferred and unchanged in substance. NO human review of any kind in the repair pass either.

- [x] **REPAIRED** — the safety net now re-derives BOTH RESTRICT FKs after the rollback and re-raises the original `IntegrityError` when neither is actually violated (`admin_service.py`, `delete_browse_category`'s `except IntegrityError` block: children → `category_has_children`, assignments → `category_in_use`, otherwise bare `raise`). The false 409 is gone and the case is back to the honest outcome its sibling routes already give for the same cause — no new status code, no response-shape change, no widening of the caught type. Genuine RED first (§15): `E AssertionError: unrelated integrity failure reported as category_in_use` / `assert 'http-409' != 'http-409'`. Pinned by `tests/test_sot_admin_categories.py::test_delete_category_unrelated_integrity_failure_is_not_a_false_409`, which uses a ghost admin actor (valid JWT, no `user` row), asserts the delete answers *identically to the sibling create route* rather than freezing an unhandled-exception representation, asserts the same for `?detach=true`, asserts zero side effects and zero audit residue, and ends with the control real-admin delete → **204**. Original finding: The delete safety net's `except IntegrityError` is unconditional, so any integrity failure that is NOT one of the two RESTRICT FKs is reported as a false `409 category_in_use` [apps/api/app/modules/sot/admin_service.py:1588-1596] — the wrapped `session.commit()` also flushes the `audit_log` INSERT whose `actor_user_id` FKs `user.id`, and `current_admin` is pure-JWT with no user-existence check. Reproduced with an admin JWT whose `User` row does not exist: `DELETE` on a category with zero children and zero assignments → `409 {"detail":"category_in_use"}` (and identically with `?detach=true`), while the control with a real admin row → 204 and the same actor gives an honest 500 on POST/PATCH/PUT. **This is a regression introduced by the finding-#3 repair** — the case used to be an honest 500 and is now a confident wrong answer the client cannot falsify. Reachability is near-zero (needs a hard-deleted user inside a 30-min token TTL) and no data is lost. Fix is ~2 lines: if the post-rollback re-derivation finds neither children nor assignments, re-raise rather than assert `category_in_use`.
- [x] **REPAIRED** — the delete/insert/audit/commit block in `replace_model_categories` is now wrapped in `except IntegrityError`, routed to the new `_raise_model_categories_commit_error` helper which rolls back and re-derives the cause against the SAME semantics the proactive checks use: vanished/soft-deleted model → `_get_model_active`'s existing `LookupError` → **404**; vanished category → the newly extracted `_validate_browse_category_ids` → `ValueError("category not found: …")` → **404**; anything else is **re-raised unchanged** (no invented 404, mirroring the N-1 posture). Duplicate payloads are unaffected — the 400 pre-check still runs ahead of everything. The per-id existence loop was extracted into `_validate_browse_category_ids` so one definition serves both the proactive and the reactive path, exactly as `_browse_category_assignments` does on the delete side. Genuine RED first (§15), both halves. Pinned by three new tests in `tests/test_sot_admin_model_categories.py`: `…vanished_category_race_is_404_not_an_unhandled_500` and `…vanished_model_race_is_404_not_an_unhandled_500` (each asserting 404 + unchanged assignment set + **no audit residue**, so no partial side effect survives the rollback) plus `test_replace_categories_duplicate_payload_is_still_400_not_404` as the non-regression. Original finding: `replace_model_categories` has no commit-time safety net, so the structurally identical prequery→COMMIT race that finding #3 was repaired for still escapes as an unhandled 500 [apps/api/app/modules/sot/admin_service.py:1643-1649, :1665] — both `session.flush()` calls and the `session.commit()` sit outside every `try`, and the router maps only `LookupError`/`ValueError` (`browse_category_admin_router.py:132-137`) while the description promises a 404. Confirmed by execution by both hunter layers (vanished-category and vanished-model variants); the unwrapped commit is directly demonstrated by the ghost-actor `PUT → 500`. Leaving the sibling of a just-repaired defect unguarded is an incomplete repair, not a design decision.
- [x] **REPAIRED** — `test_patch_category_partial_update_200` now inspects the `browse_category.update` audit body: exactly one row, `before["name_en"] == ` the pre-patch value, `after["name_en"] == "Renamed"`, `before != after`, and the untouched `slug`/`position` still equal on both sides. **Coverage-only repair** over already-correct production behaviour — no genuine RED is possible without breaking correct code, so it is labelled a characterisation guard per §5's ordering rule, never dressed up as a RED. Non-vacuity proven by re-running the re-review's own mutant (moving `after = _browse_category_snapshot(cat)` above the `setattr` loop): previously `70 passed`, now **`1 failed, 75 passed`**, the single failure being this test. `admin_service.py` restored byte-identically afterwards (§15). Original finding: The `browse_category.update` audit's `after` snapshot is pinned by no test [apps/api/app/modules/sot/admin_service.py:1483-1487; tests/test_sot_admin_categories.py:439-463] — the only test that inspects an update audit body is the empty-`{}` no-op, where `before == after` is the expected result. Mutation probe: moving `after = _browse_category_snapshot(cat)` above the `setattr` loop (so every update records "nothing changed") → **`70 passed`**. This is the exact class finding #12 closed for the create path, left open for update. Fix: one assertion in `test_patch_category_partial_update_200`.
- [x] **REPAIRED — both halves, and the product contract is unchanged.** (a) `tests/test_sot_admin_categories.py::test_delete_category_children_checked_before_assignments_are_queried` seeds BOTH conflict sources, calls `DELETE ?detach=true`, and monkeypatches `_browse_category_assignments` with a **call-recording sentinel**: the 409 `category_has_children` must arrive with the assignment query never having run. Deleting the proactive children pre-query now makes the request fall through to the detach branch, so the sentinel fires — **`2 failed, 69 passed`** under that mutant, where before only the race probe failed incidentally (`1 failed, 64 passed`). (b) `tests/test_sot_admin_model_categories.py::test_replace_categories_validates_every_id_before_any_delete_is_attempted` records the real sequence — a wrapper on `_validate_browse_category_ids` plus a SQLAlchemy `before_delete` mapper listener on `ModelBrowseCategory` — and asserts `steps == ["validate", "delete"]`, i.e. that no delete is even *attempted* before the whole payload resolves. Re-running the Acceptance Auditor's exact mutation (existence loop moved after the deletes with a compensating rollback) → **`1 failed, 14 passed`**, the one failure being this test while the outcome-only `unknown_category_404_no_side_effect` stays green, which is precisely the gap the finding named. Both are coverage-only guards over already-correct behaviour and are labelled as such; neither changes the observable contract. Original finding: Two ordering claims the ACs state as proven are pinned by outcome only, not by order [apps/api/app/modules/sot/admin_service.py:1556-1557, :1629-1643] — (a) AC-21/AC-22's "children checked first, unconditionally": deleting the proactive children pre-query leaves `test_delete_category_with_children_409` (all three parametrisations) and `test_delete_category_children_win_over_assignments` GREEN (`1 failed, 64 passed`, the one failure being the race probe failing incidentally), because finding #3's safety net re-derives children-first after rollback and yields an identical observable 409; (b) AC-31's "validate everything, then mutate": moving the existence loop after the deletes with a compensating rollback keeps all 11 replace tests green. The binding contract's observable guarantees hold either way — this is a traceability gap, not a behaviour defect. One shared fix shape.

**Defer (7) — real, not actionable inside this frozen delta.** Recorded here and in the durable report only; this pass is authorised to write the story artifact and `sprint-status.yaml` only, so `deferred-work.md` was NOT edited (disclosed deviation, mirroring the prior CR pass).

- [x] [Review][Defer] A repeated `detach` query parameter is last-one-wins, so `?detach=false&detach=true` silently selects the destructive branch (204 + every assignment destroyed) while `?detach=true&detach=false` gives 409 [browse_category_admin_router.py:166] — standard FastAPI/ASGI scalar semantics, same class as the previously-dismissed `?detach=yes|1|on`, recorded rather than dismissed only because the flag is destructive.
- [x] [Review][Defer] `_browse_category_children` has no `id != category_id` exclusion, so a pre-existing self-parented row is permanently undeletable [admin_service.py:1333-1337] — no path in this story can create the state (`create`/`update` both reject `self_cycle`); it needs data injected outside the API.
- [x] [Review][Defer] The already-ledgered depth-2 TOCTOU can persist a parent CYCLE (A→B, B→A), after which both categories are permanently undeletable — a sharper end-state than the ledger's recorded "persistent depth-3 chain"; append to that entry when it is next touched.
- [x] [Review][Defer] Slug uniqueness is byte-exact, so case-only duplicates coexist (`probe-case-x` and `PROBE-CASE-X` both 201) — folds into the ledger's existing `slug`-validation entry.
- [x] [Review][Defer] `position` accepts JSON booleans, numeric strings and negatives via Pydantic lax coercion (`{"position": true}` → 201 with `position: 1`) — folds into the ledger's existing unbounded-`position` entry.
- [x] [Review][Defer] The "idempotent" replace churns `model_browse_category.created_at` (delete + re-insert of identical rows) — idempotence is claimed for the SET and the set genuinely is; same posture as `replace_model_tags`.
- [x] [Review][Defer] Router error mapping is substring/catch-all based, so any future non-enumerated `ValueError` becomes a confident wrong status (404 on replace, 409 on delete, 422 on create/patch) — mirrors the ratified `tag_group_admin_router.py` posture; tightening it is cross-cutting. Also: the DELETE route description never warns that `model_count` may read 0 while `category_in_use` is returned — every sentence in it is true, but Story 52.2 is the direct consumer that needs the caveat.

**Dismissed as noise (3):** `?detach=` (empty) returning 422 rather than the description's 409 (a 422 on an unparseable bool is correct and universal; the description enumerates conflict sources, not parse errors); AC-37's literal "two new tests" wording (§5 T6.1 mandates *extending* `_GOVERNANCE_ROUTES`, which is what was done and what §15 T6.1 states); the `model.update` audit stream not being self-describing (explicitly ratified by §3.10 and Decision AY, and documented at `audit.py:15-21`).

**Recorded, not charged (inherited):** `_audit_entity` (`admin_service.py:114-135`) bypasses `record_event`, so `KNOWN_ENTITY_TYPES` registration is inert at runtime on this story's write path and is protected by the two pinned drift guards instead — pre-existing since Story 42.4, neither introduced nor worsened here.

**Record-fidelity note.** The tree carries **14 paths (11 tracked modified + 3 untracked)**, not the 13 the prior review found; the repair pass added `deferred-work.md`, and §15's re-derived File List already says so. Only T8 remains unchecked (five boxes, all controller-owned). Two AC texts remain known-wrong on the page and are already disclosed elsewhere: AC-34's "every pre-existing audit-touching test stays green" (falsified, D-2) and AC-37's "two new tests".

### Final-review Findings (native `bmad-code-review`, THIRD PASS 2026-07-28)

Written by a fresh, independent native `bmad-code-review` pass run after the post-RE-REVIEW repair pass — **verdict `REQUEST_CHANGES`**. Neither the repair session's literal `subtype=success` / `rc=0` nor any of its numeric claims were trusted: every N-1…N-4 closure was re-derived from source and re-proved by **this pass's own five mutation probes**, with `admin_service.py` restored byte-identically each time (`sha256 eff4e930c2b90717…`, asserted inside the probe harness). Full evidence, the complete 14-path audit, the closure matrix and all deviations are in the durable report `.hermes/run-logs/e49-5-native-final-review-20260728.md`. Severities are this pass's own (step-03 rule 4). **NO human review of any kind.**

**All four re-review patch findings independently CONFIRMED CLOSED, and the tests are non-vacuous.** Each mutant below was applied by me, the named suite run, and the file restored: N-1 (revert the bare `raise` to `raise ValueError("category_in_use")`) → `1 failed, 55 passed`, killed by `test_delete_category_unrelated_integrity_failure_is_not_a_false_409`; N-2 (strip the whole `try`/`except` from `replace_model_categories`) → `2 failed, 13 passed`, killed by both vanished-category/vanished-model race probes; N-3 (`after` snapshot taken above the `setattr` loop) → `1 failed, 55 passed`, killed by `test_patch_category_partial_update_200`; N-4a (delete the proactive children pre-query) → `2 failed, 54 passed`, killed by the new sentinel **and** the race probe; N-4b (existence loop moved after the deletes with a compensating rollback) → `2 failed, 13 passed`, killed by the new ordering sentinel **and** `test_replace_categories_audit_fidelity`. No test anywhere in the delta was weakened, narrowed or removed.

**All gates re-executed and reproduced exactly**, including the full suite, which this pass **does** claim (unlike the re-review): focused `71 passed` (`--collect-only` 56 + 15); §14 witness set `196 passed`; audit/2FA/OpenAPI/CSRF/admin-models witness `92 passed`; full `apps/api` **`1921 passed, 3 skipped in 416.17s`** on a certified-quiescent tree (sha at baseline, every probe restored, scratch file already deleted, only read-only work concurrent); `ruff format --check` `281 files already formatted`; `ruff check` `All checks passed!`; `git diff --check` exit 0; runtime OpenAPI dump of all four routes `sot-admin-governance`, non-empty summary+description, **zero `agent-write`** anywhere on a category path; `sprint-status.yaml` parses, 336 keys, `epic-49: in-progress`, no `49-6`.

**Binding contract verified clean by direct reading**: `current_admin` on all four routes; CSRF covered by construction (`csrf.py` is a bare `/api/` prefix guard with a single `/api/share/` exemption and no allowlist); audit exactly-once (four `_audit_entity` calls, four commits, the replace-path audit now inside the guarded block so a losing race leaves no residue); children-first delete precedence and replace-set validate-before-mutate now pinned **by order**, not only by outcome; bounded 8-key snapshots; empty replace set valid; public reads and Tag/TagGroup governance untouched; no migration/DDL/seed/frontend/live path anywhere in the 14-path inventory. **No AC is violated and no binding constraint is breached. Nothing found is a security, authorization, data-corruption, data-loss or merge-blocking safety defect.**

**Repair status (native `bmad-dev-story` repair pass, 2026-07-28 — post-FINAL-review).** The **1 patch** finding F-1 is **repaired**, not deferred: the controller elected the ~4-line closure over the defensible recorded deferral, so E49.5 can proceed toward closeout with the contract honest. The **2 defer** findings stay deferred and unchanged in substance; the ledger gap (F-3) is itself one of them and still needs the bounded `deferred-work.md` write authorization no review or repair pass has had — this pass did not have it either and did **not** touch the ledger. The **2 minor-recorded** items and the **3 dismissed** items are untouched. Evidence for the repair is in §15 → "Post-FINAL-review repair pass". NO human review of any kind in this pass either.

**Patch (1) — REPAIRED**

- [x] [Review][Patch] **The N-1 repair closes only the zero-assignment branch: with `detach=true` on a category that HAS assignments, a non-RESTRICT integrity failure still returns the false `409 category_in_use`** (low) [apps/api/app/modules/sot/admin_service.py:1636-1648, specifically the re-derivation at `:1639-1640`] — the safety net re-derives its cause **after** `session.rollback()`, and that rollback also restores the very assignment rows the request just deleted at `:1615-1617`. The re-derivation therefore always sees assignments present and raises `category_in_use`, unable to distinguish "the rows I deleted, restored by my own rollback" from "a racing INSERT", and can never reach the `:1648` bare `raise` that N-1 added. Reproduced by execution with a ghost admin actor (valid JWT, no `user` row): `GHOST detach=true WITH assignments -> http-409`, assignments still 1, category still present, **0** delete-audit rows, sibling `POST` on the same cause → `integrity-error`, control real-admin `detach=true` → **204**. The route description (`browse_category_admin_router.py:153-154`) defines `category_in_use` as "the category has model assignments **and `detach` was omitted or false**" — `detach` was `true`, so the client is told a conflict exists that its own flag was supposed to resolve, retrying with `detach=true` fails identically, and `model_count` gives it nothing to act on. Same unfalsifiable-wrong-answer shape the re-review charged as N-1, in the branch N-1's own test did not cover (that test does exercise `?detach=true`, but only on a category with **zero** assignments). **NOT a failed repair** — the repair pass implemented N-1's prescribed fix literally and correctly and it is mutation-proven; the prescription itself was incomplete. New to this pass. Reachability is the same near-zero as N-1 (needs a hard-deleted admin `user` row inside a 30-min token TTL) and **no data is lost** — the rollback is total and no audit row survives, both verified. Fix ~4 lines: compare the post-rollback assignment set against the pre-delete set already computed as `detached_ids` (`:1612`) and re-raise when it is unchanged, plus one assertion extending the existing N-1 guard to the has-assignments case. **A recorded deferral is defensible** if the controller prefers to hold the frozen delta, provided it is written down rather than silent. — **REPAIRED** exactly in that prescribed shape: `detached_ids` is now initialised to `[]` before the detach branch and therefore in scope in the handler; the post-rollback re-derivation binds `remaining = _browse_category_assignments(...)` and raises `category_in_use` only when `{row.model_id for row in remaining}` is **not** a subset of `set(detached_ids)` — i.e. only when an assignment appeared that this request had not already accounted for. Children are still re-derived first, so precedence is unchanged. With `detach=false` nothing is accounted for, so every prior 409 path is byte-for-byte identical in behaviour. Genuine RED → GREEN: the new `test_delete_category_detach_with_assignments_unrelated_integrity_failure_is_not_a_false_409` fails against the pre-repair code and passes after, and re-proved non-vacuous by this pass's own mutation probe (collapse the guard back to `if remaining:` → `1 failed, 71 passed`, killed by exactly that test; `admin_service.py` restored byte-identically, `sha256 3d393e846d8d0ca0…`). The new test also pins zero audit residue, category + assignment both preserved after rollback, the real 409 still returned for `detach=false` on the same category, and a real-admin `detach=true` control returning **204** with `detached_model_ids` intact in the audit `before`. The docstring's safety-net paragraph now states the restored-rows caveat. One residual corner is accepted and named rather than hidden: a racing INSERT that re-creates an assignment for a model this same request had just detached is indistinguishable from the rollback's own restoration and re-raises instead of 409 — unobservable after rollback without row-level identity the ORM no longer has.

**Defer (2)**

- [x] [Review][Defer] The N-4a ordering sentinel is satisfiable by refactor rather than by order (low) [apps/api/tests/test_sot_admin_categories.py:1142-1154] — it proves precedence by monkeypatching the module-global `_browse_category_assignments` and asserting it was never called, so any future refactor that inlines or renames that query makes `queried == []` vacuously true. Deferred, not charged: strictly stronger than what it replaced, standard technique already used in these files, and no smaller alternative exists without a query-level recorder.
- [x] [Review][Defer] The canonical ledger records 8 of this story's deferred items; the re-review's 7 plus this pass's 2 exist only in this artifact and the run-log reports (low, record completeness, **controller-owned**) — `deferred-work.md` carries only the first CR pass's eight entries because every pass since (CR, re-review, repair, this one) was scoped to write only this artifact and `sprint-status.yaml`. Each disclosed it honestly, so nothing is concealed, but the ledger is incomplete as a standalone artifact. Closing it needs a write authorization no review or repair pass has had; recommended as one bounded ledger-append at T8 time.

**Minor (2, recorded not charged):** AC-34's "every pre-existing audit-touching test stays green" and AC-37's "two new tests" remain known-wrong on the page (both already disclosed as D-2 / T6.1, so the record *about* them is honest, but either AC read alone would still mislead); §8's Dev Notes sketches (§8.4 delete tail, §8.5 replace-set) are now two safety nets behind the shipped code, deliberately and disclosedly left alone by both repair passes.

**Dismissed as noise (3):** `_audit_entity` bypassing `record_event` (true, but pre-existing since 42.4 and neither introduced nor worsened here); the per-id `session.get` loop running twice on the race path (already in the ledgered efficiency cluster, and only on an error path ending in a 404); the re-raised `IntegrityError` surfacing as a 500 (that is the intended, documented outcome of the N-1 repair and matches the sibling routes).

**Record-fidelity check: PASS.** Only T8 remains unchecked (five boxes, all controller-owned). `epic-49` untouched at `in-progress`. No `49-6` key. No human-review claim anywhere, and the Laura/controller arbitration of finding #1 is correctly labelled a controller product-default decision rather than a human review. Every executable claim in §15's post-RE-REVIEW record reproduced exactly.

### Approve-review Findings (native `bmad-code-review`, FOURTH PASS 2026-07-28 — post-F-1 repair)

Written by a fresh, independent native `bmad-code-review` pass run after the post-FINAL-review F-1 repair — **verdict `APPROVE`**. Full evidence, the complete 14-path audit and all deviations are in the durable report `.hermes/run-logs/e49-5-native-approve-review-20260728.md`. Severities are this pass's own (step-03 rule 4). **NO human review of any kind, and this is native BMAD agent approval ONLY** — `check-all.sh` 16/16, the determinism triple, the frozen manifest, the independent `laura-aider-review-diff` (Aider routine default per the Laura Agent Rulebook; Codex fallback/high-stakes only; Gemini is not a default reviewer) and the whole T8 commit/ff-merge/push/deploy chain are all still owed and all controller-owned.

**F-1 is genuinely CLOSED, verified in both directions by this pass's own evidence.** Two mutation probes, each restored byte-identically with the restore sha-asserted inside the probe command (`sha256 3d393e846d8d0ca0…`): **(A)** collapsing the new guard back to the pre-repair `if remaining:` → `1 failed, 71 passed`, killed by **exactly** `test_delete_category_detach_with_assignments_unrelated_integrity_failure_is_not_a_false_409` and nothing else, so the guard is load-bearing and its test non-vacuous; **(B)** deleting the `category_in_use` mapping outright → `1 failed, 56 passed`, killed by `test_delete_category_assignment_race_is_409_not_an_unhandled_500`, so the repair did **not** hollow out the documented racing-insert 409. With `detach=false` the proactive check at `:1607` already raised, so `detached_ids` is provably `[]` and every pre-existing 409 path is behaviourally identical. Children are still re-derived first (`:1642` before `:1644`), so delete precedence is untouched.

**All prior closures re-checked at source and intact.** N-1 (bare `raise` still terminates the handler at `:1659`), N-2 (`_raise_model_categories_commit_error` byte-unchanged; the `try`/`except` still wraps delete + insert + **audit** + commit), N-3, N-4a and N-4b sentinels all present and green. No test anywhere in the delta was weakened, narrowed, renamed or removed.

**All gates re-executed and reproduced exactly:** full `apps/api` suite **`1922 passed, 3 skipped, 2268 warnings in 419.39s`** on a quiescent tree, claimed by this pass (= the FINAL review's `1921` plus exactly this repair's one new test, no pre-existing test changing status); focused `72 passed` (`--collect-only` 72); `ruff format --check` `281 files already formatted`; `ruff check` `All checks passed!`; `git diff --check` exit 0; `sha256 3d393e846d8d0ca0c487870d3be0033ec13f91dd74468b0bedb50cccca998efe`; **14 paths**; `sprint-status.yaml` parses, 336 keys, `epic-49: in-progress`, `49-5: review`, no `49-6`; runtime OpenAPI dump of all four routes `sot-admin-governance` with **zero `agent-write`** against 25 agent-write routes repo-wide, none on any category path.

**Binding contract re-verified clean:** `current_admin` on all four routes; CSRF covered by construction (`csrf.py:12-19`, bare `/api/` prefix guard, one `/api/share/` exemption, no allowlist); audit exactly-once (four `_audit_entity`, four `session.commit()`, no `record_event` added by this story); children-first delete precedence and replace-set validate-before-mutate pinned by order; bounded 8-key snapshots; additive-only — the single deletion in the entire production diff is `-from typing import Any`; **no migration / DDL / seed / frontend / infra / live path anywhere in the 14-path inventory.** No AC violated, no binding constraint breached, and nothing found is a security, authorization, data-corruption, data-loss or merge-blocking defect.

**Critical: none. Important: none. Patch: none.**

**Defer (3)**

- [x] [Review][Defer] The new subset guard's true-positive branch has no test (low; new) [apps/api/app/modules/sot/admin_service.py:1650] — probe A proves the guard is killed by the F-1 test, and `test_delete_category_assignment_race_is_409_not_an_unhandled_500` covers the racing insert, but only with `detach=false`, where `detached_ids` is provably empty and the subset comparison is degenerate. The `detach=true` + unaccounted-racing-row → 409 path is exercised by **no** test. Verified correct by out-of-tree execution instead (a probe living entirely in `/tmp`, creating no repo file): the service returned `ValueError('category_in_use')` → 409 with the category still present, 2 assignments intact and **0** delete-audit rows. Coverage gap, not a defect. Deferred rather than charged: the branch is verified working, reachability is the same near-zero as F-1's, closing it needs a third `_blind_first_call`-shaped probe, and the intent contract is frozen. Worth adding whenever the delete race posture is next touched.
- [x] [Review][Defer] (carried, unchanged) The N-4a ordering sentinel is satisfiable by refactor rather than by order (low) [apps/api/tests/test_sot_admin_categories.py:1142-1154].
- [x] [Review][Defer] (carried, unchanged) The canonical ledger records 8 of this story's deferred items; the re-review's 7 plus F-2, plus now the item above, exist only in this artifact and the run-log reports (low, record completeness, **controller-owned**) — verified directly: the ledger's 49.5 heading still carries exactly the first CR pass's eight `- source_spec` entries and nothing since. This is the **fifth consecutive pass** to disclose the identical gap rather than skip it silently, and the fifth without the write authorization to close it. One bounded ledger-append at T8 time closes it.

**Minor (3, recorded not charged)**

- **The post-FINAL-review F-1 repair run terminated `subtype=error_max_turns`, not `success`.** `.hermes/run-logs/e49-5-final-f1-repair-20260728_155740.jsonl` ends `{"subtype":"error_max_turns","is_error":true,"terminal_reason":"max_turns","errors":["Reached maximum number of turns (50)"],"num_turns":51}` and has **no `RUN_EXIT` line at all** (`grep '^RUN_'` returns only `RUN_START`). The cap fired immediately after its last assistant text, `"Now sprint-status.yaml."`. **The writes nevertheless landed** — parsing the log's own final tool calls shows the artifact edits, the Change Log insert, the sprint-status edit (`tool_result: ok / keys 336 / story review / epic49 in-progress / 49-6 present: False`) and a closing tree verification all completing before the cap, and every resulting state is independently re-confirmed from the tree by this pass. Nothing about the code or the records is in doubt; recorded only so that **no downstream summary cites "repair pass rc=0" for that run.**
- **One residual corner in the F-1 fix, correctly self-disclosed by the repair rather than hidden.** A racing INSERT re-creating an assignment for a model *this same request had just detached* is, after `session.rollback()`, byte-identical to the rollback's own restoration — the composite key is `(model_id, category_id)` and `category_id` is fixed, so the two are literally the same row — and therefore falls to the bare `raise` instead of 409. Undecidable from post-rollback state without row identity the ORM no longer holds. Strictly narrower than F-1, same near-zero reachability, no data loss, and the trade errs toward re-raising the real cause over asserting a conflict it cannot prove — the exact principle F-1 was charged under. Already named in the docstring (`:1583-1586`) and the §19 Change Log. Correctly not charged.
- **(carried, unchanged)** AC-34's and AC-37's own texts remain known-wrong on the page (both already disclosed as D-2 / T6.1); §8's Dev Notes sketches (§8.4 delete tail, §8.5 replace-set) are now **three** safety nets behind the shipped code, deliberately and disclosedly left alone by all three repair passes.

**Dismissed as noise (3, carried):** `_audit_entity` bypassing `record_event` (pre-existing since 42.4); the per-id `session.get` loop running twice on the race path (already ledgered, error path only); the re-raised `IntegrityError` surfacing as a 500 (the intended, documented outcome).

**Record-fidelity check: PASS.** Only T8 remains unchecked (five boxes, all controller-owned). `Status: review` — **never `done`**. `epic-49` untouched at `in-progress`. No `49-6` key. No human-review claim anywhere, and no Aider / Codex / Gemini / `check-all.sh` / determinism / frozen-manifest / commit claim anywhere. Every executable claim in §15's post-FINAL-review record reproduced exactly.

## 6. Verify-at-create findings (traced against real code at HEAD `0df663e` this session)

**F-1 — The 42.4 story's own Dev Notes cite a stale template; do not follow it literally.** Story 42.4's artifact (`_bmad-output/implementation-artifacts/42-4-admin-group-governance.md`, `done`) references `admin_service.py:1150-1306 — Category CRUD is the exact template` and named functions `create_category`/`update_category` at specific line numbers. **These functions and the entire `class Category` feature they governed were retired by Epic 47** (`0019_drop_category`) and do **not exist anywhere** in this worktree — confirmed by an exhaustive grep for `def create_category\|def update_category\|def delete_category\|class Category\b` across `apps/api/app/` returning zero matches. The line range `1150-1306` in the current file is occupied by `TagGroup` CRUD (`create_tag_group`/`update_tag_group`/`delete_tag_group`, verified verbatim at `admin_service.py:1150-1284` this session), which **is** the correct template — exactly what `sprint-status.yaml:383`'s own directive already says ("extend `tag_group_admin_router.py` + the 42.4 audit posture"). This story's tasks (§5 T2-T5) cite `create_tag_group`/`update_tag_group`/`delete_tag_group`/`delete_tag`/`replace_model_tags` throughout, never the retired citation.

**F-2 — Current `BrowseCategory`/`ModelBrowseCategory` entities, verified line-for-line at `_entities.py:130-183`.** Both FKs on `ModelBrowseCategory` are `ondelete="RESTRICT"` (`model_id` → `model.id` and `category_id` → `browse_category.id`), and `BrowseCategory.parent_id` is **also** `ondelete="RESTRICT"` (self-FK). `BrowseCategory` has **no** `deleted_at`/soft-delete column (confirmed by the `list_browse_categories` docstring in `sot/service.py`: "No lifecycle filtering — BrowseCategory has no soft-delete column"). `position: int = Field(default=0)` at the ORM layer, `server_default="0"` at the migration layer (`migrations/versions/0020_browse_categories.py:60-113`).

**F-3 — Neither existing read schema exposes `inclusion_criterion`.** `BrowseCategorySummary` (`sot/schemas.py:87-101`, 5 keys: `id, slug, name_en, name_pl, position, parent_id`) and `BrowseCategoryRead` (`:104-120`, +`description_en, description_pl, model_count`) both deliberately omit it — Story 49.3's own §6 F-12 recorded this as following Decision AY's exact public-read keyset and explicitly deferred a wider DTO to "a later admin/curation DTO for E49.5/52.3." **This story is that later DTO.** `BrowseCategoryAdminRead(BrowseCategoryRead)` (T1.3) is additive — it does not touch `BrowseCategorySummary`/`BrowseCategoryRead`, and `GET /api/categories[/{slug}]` (Story 49.3, unmodified) continues to omit `inclusion_criterion` from its response, which is the correct, deliberate asymmetry: the public read contract and the admin write-response contract are allowed to differ, and Decision AY's keyset governs only the former.

**F-4 — The double-RESTRICT hazard on category delete is real and unaddressed by any planning artifact — this story resolves it.** `sprint-status.yaml:383` and `architecture.md:3337` both say "409 Conflict while assignments exist, unless explicit `detach=true`" — language that reads as though `model_browse_category` rows are the *only* RESTRICT source. They are not: `BrowseCategory.parent_id`'s own `ondelete="RESTRICT"` (F-2) means a category with children raises `IntegrityError` on delete for a completely independent reason, and `detach=true`'s documented scope ("detach + delete in one transaction," carrying **model** ids) has no natural extension to "also reparent or delete children" — doing so would silently redefine what `detach=true` means and would require its own depth/self-cycle re-validation on every affected child. **Resolution adopted by this Create pass (AC-21/AC-22, §5 T4.2):** children are checked **first and unconditionally** — a category with any child is a flat 409 `category_has_children` regardless of `detach`. Deleting or reparenting children is a separate, prior admin action using the existing PATCH/DELETE routes this story already ships (`PATCH {"parent_id": null}` on the child, or `DELETE` the child first). This is the minimal-diff resolution: it adds no new query parameter, no new transaction shape, and no cascading-delete semantics that nothing in Decision AX/AY asked for — Decision AX explicitly says depth-2 is "flat MVP" with "MVP writes no child categories at all" in the seed/product sense, so in practice this path is rare, but the API must not crash with a raw 500 `IntegrityError` when it is hit. **Flagged for the Validate pass**, not silently applied: an alternative design (making `detach=true` also orphan children to `parent_id=null`) is plausible and was considered but rejected here as a larger, unrequested behavioural surface.

**F-5 — `sot-admin-governance` sits outside `TARGET_ROUTER_TAGS`.** `test_openapi_agent_surface.py:61` — `TARGET_ROUTER_TAGS = {"admin", "sot-admin", "sot-read"}` — confirmed does **not** include `sot-admin-governance`. The generic `test_every_admin_sot_operation_has_summary`/`test_every_admin_sot_operation_has_description` (`:165-182`) therefore do not automatically cover this story's four new routes; the existing precedent for closing that gap is `test_governance_routes_have_summary_and_description` (`:290-297`), which iterates an explicit `_GOVERNANCE_ROUTES` set literal (`:257-262`) — §5 T6.1 extends that literal rather than writing a parallel, redundant test.

**F-6 — The epic:42 unbounded-audit-payload precedent, quoted exactly.** `_bmad-output/implementation-artifacts/deferred-work.md` / `sprint-status.yaml:439-442` records: *"Re-evaluate the unbounded `detached_tag_ids` delete-audit payload only if tag-group membership can grow large (42.4 native-review Minor #1; bounded in practice by the small admin-rare tag table, `detached_tag_count` carried alongside)."* `sprint-status.yaml:383`, `architecture.md:3337`, and the SCP all invoke this **by analogy** for 49.5. The shipped precedent shape (`delete_tag_group`, `admin_service.py:1267-1279`) is `{"slug":..., "name_en":..., "detached_tag_ids": [str,...], "detached_tag_count": int}`. This story's `browse_category.delete` audit (AC-19) mirrors it exactly with `detached_model_ids`/`detached_model_count`, and inherits the same accepted, open, non-blocking posture — **not** a mandate to build truncation. `_browse_category_snapshot`'s base fields (AC-35) follow `_tag_group_snapshot`'s "full bounded snapshot, UUIDs/strings/ints only, no PII" discipline (D-AUDIT-2, `admin_service.py:1140-1147`).

**F-7 — Route-enforcement gate needs no edit.** `test_route_enforcement_gate.py`'s `_AUTH_DEP_NAMES = {"_current_user_dep", "_current_admin_dep", "_current_member_or_admin_dep", "_current_admin_or_agent_dep"}` already recognizes `current_admin`'s underlying callable (`_current_admin_dep`, `app/core/auth/dependencies.py:76`), and this story introduces no new auth-dependency function — all four new routes reuse `current_admin` verbatim. `_PUBLIC_ROUTES` (`main.py:50-61`) needs no edit; none of these routes is anonymous.

**F-8 — CSRF middleware is prefix-based and needs no per-route test.** `app/core/auth/csrf.py`'s `csrf_guard` gates every `UNSAFE_METHODS` request under `/api/` except `/api/share/*`, keyed only on path prefix and `X-Portal-Client` header presence — it has no per-route allowlist to extend. `tests/test_csrf_middleware.py` (whole-file, path-prefix-driven) already proves this generically; §5 T6.3 runs it unmodified rather than authoring four redundant per-route CSRF tests.

**F-9 — `PUT /models/{model_id}/tags` is the direct structural precedent for the replace-set endpoint, verified line-for-line at `admin_service.py:807-859` / router at `admin_router.py:571-598`.** Its docstring's contract ("Returns 200 + the resulting tag list... 404 if model or any tag id is missing. 400 on validation failure (e.g. duplicate ids in the payload)") is the source for this story's AC-28/AC-29/AC-30 status-code choices. **Two deliberate departures:** (a) the tags endpoint is `_current_principal` (admin OR agent) and tagged `agent-write`; this story's `PUT /categories` is `current_admin`-only with no `agent-write` tag. (b) the tags endpoint has **no duplicate-id guard** in the actual shipped code (only in its own docstring's stated contract) — a literal-duplicate `tag_ids` payload today hits an uncaught `IntegrityError` on the second `session.add(ModelTag(...))` at `session.flush()`. AC-28 closes this gap for categories; this story does **not** retroactively patch `replace_model_tags` (out of scope, would be silent unrequested scope creep into a different, closed story's surface).

**F-9 addendum — RESOLVED by the Validate pass (2026-07-28), correcting departure (a)'s framing.** The Create pass characterized the `current_admin`-only tier as "a real auth-tier tightening with no ratified rationale beyond the task's own explicit instruction" — re-derived against source, that understates the evidence and is corrected here: **Decision AY** (`architecture.md:3334-3341`) places **all four** new routes — including `PUT /api/admin/models/{id}/categories` — under one explicit heading, *"Admin governance (`current_admin`, audit row on every write; structural precedent `tag_group_admin_router.py` + the 42.4 audit posture)"*. This is a ratified planning-artifact requirement, not a session-invented or merely task-instructed choice. It is also not a novel pattern in this codebase: `tag_group_admin_router.py`'s own docstring (verified verbatim at HEAD) states the identical doctrine for facet governance — *"Facet governance is admin curation, not agent ingestion (FR25-ADMIN-1 / Decision AW)... never advertises an agent capability"* — and `D-ADMINONLY-1` already relocated `POST /tags` from the agent-write router to this admin-only one on that exact basis. Categories vs. tags is the same admin-curation/agent-ingestion split applied to a new taxonomy family, not an unprecedented asymmetry. The genuinely open item is narrower than Q-1 as originally framed: `architecture.md` states the `current_admin` requirement but does not itself spell out *why* the split falls the way it does for the replace-set operation specifically (as opposed to the create/rename/delete routes, which are admin-curation by any reading) — that is a documentation-completeness observation for a future architecture-hygiene pass, not an unratified or ungrounded design decision. §8.1's router docstring and §18 Q-1 are amended below to state this truthfully.

**F-10 — Test-authoring precedent, `test_sot_admin_tag_groups.py` (507 lines, whole file read).** `_admin_cookie(client)` (`:52-57`) seeds a real `User(role=admin)` row via a direct `Session(get_engine())` write (required because `AuditLog.actor_user_id` FKs `user.id` with `ondelete=SET NULL`, so a bare JWT-minted UUID with no backing row would still pass auth but produce an audit row whose FK reference is technically valid-but-orphaned rather than exercising the real actor-linkage path). The auth-matrix pattern (`:407-451`) sequentially swaps `client.cookies` between no-cookie / member-token / agent-token / `_admin_cookie(client)` and asserts 401/403/403/2xx — replicated verbatim for this story's four routes. Audit assertions query `audit_log` directly via a small `_tag_group_audits(action, entity_id)` helper (`:80-89`) using `select(AuditLog).where(AuditLog.action == ..., AuditLog.entity_id == ...)`, then `json.loads(log.before_json/after_json)` and exact-keyset assertions (`set(before.keys()) == {...}`) — this story's `_category_audits`/`_model_audits` helpers follow the identical shape. `test_delete_tag_409_in_use` (`test_sot_admin_tags.py:477-491`) is the closest existing 409-in-use test body shape (seed admin + model + tag + a `ModelTag` row, then assert `client.delete(...).status_code == 409`) — this story's AC-18/AC-19/AC-21/AC-22 tests extend that exact shape with a seeded `ModelBrowseCategory` row and/or a seeded child `BrowseCategory` row.

**F-11 — No admin-facing docs section is falsified by this story (unlike Story 49.3's forced doc-honesty correction).** `docs/operations.md:471-474`'s "Admin write" enumeration ("`/api/admin/*` for models, files, tags, notes, prints, external_links") is **incomplete** relative to the already-shipped `tag-groups` governance router and will remain incomplete after this story ships `categories` too — but it states nothing **false**; it is a documentation gap, not a documentation lie, unlike the `/api/categories` retirement text Story 49.3 was forced to correct because deploying would make it false. Per §3/§10 minimal-diff discipline, this story does **not** edit `docs/operations.md` — closing that enumeration gap (for `tag-groups` too, not just `categories`, since it's already stale) is left as a reported, not silently absorbed, observation for a future doc-hygiene pass.

## 7. Predicted file changes (exact)

**Total: 9 product/doc files** = **6** production + **3** test (2 NEW + 1 additive extension of an existing file). Plus the two workflow records (this artifact + `sprint-status.yaml`).

**Production code — 6 files:**

| File | Change |
|---|---|
| `apps/api/app/modules/sot/browse_category_admin_router.py` | **NEW.** `POST /api/admin/categories`, `PATCH /api/admin/categories/{category_id}`, `DELETE /api/admin/categories/{category_id}`, `PUT /api/admin/models/{model_id}/categories`. All `current_admin`, all `tags=["sot-admin-governance"]`, mirroring `tag_group_admin_router.py`'s docstring/structure. |
| `apps/api/app/modules/sot/admin_schemas.py` | MODIFY. ADD `BrowseCategoryCreate`, `BrowseCategoryPatch` (with the NOT-NULL `_reject_explicit_null` validator on `slug`/`name_en`/`position`), `ModelCategoriesReplace`. |
| `apps/api/app/modules/sot/admin_service.py` | MODIFY. ADD `_browse_category_snapshot`, `create_browse_category`, `update_browse_category`, `delete_browse_category`, `replace_model_categories`. No existing function's body changes. |
| `apps/api/app/modules/sot/schemas.py` | MODIFY. ADD `BrowseCategoryAdminRead(BrowseCategoryRead)` (+`inclusion_criterion`). `BrowseCategorySummary`/`BrowseCategoryRead`/`ModelDetail` unchanged. |
| `apps/api/app/core/audit.py` | MODIFY. ADD `"browse_category"` to `KNOWN_ENTITY_TYPES` + its doc-comment entry. No existing entry changes. |
| `apps/api/app/router.py` | MODIFY. ADD one `api_router.include_router(...)` line for the new router, positioned immediately after the existing `sot_tag_group_admin_router` line. |

**Tests — 2 files, both NEW:**

| File | Change |
|---|---|
| `apps/api/tests/test_sot_admin_categories.py` | **NEW.** `POST`/`PATCH`/`DELETE /api/admin/categories[...]` — AC-1…AC-24, AC-34…AC-37 (category-scoped OpenAPI/audit assertions). |
| `apps/api/tests/test_sot_admin_model_categories.py` | **NEW.** `PUT /api/admin/models/{id}/categories` — AC-25…AC-33. |

**Test file extended in place (counted above as part of the "6 production" OpenAPI-guard coverage, not a 9th file — `test_openapi_agent_surface.py` is a test file; recorded here separately for clarity, bringing the true test-file count to 3):**

| File | Change |
|---|---|
| `apps/api/tests/test_openapi_agent_surface.py` | MODIFY — additive only. Extend `_GOVERNANCE_ROUTES` (`:257-262`) with the four new `(METHOD, path)` tuples. No existing test body changes; every existing assertion in the file, including the `Category*`-naming guards (§3.6), stays byte-unmodified and must keep passing against the new routes/schemas. |

**Workflow records (separate from the product commit):** this story artifact and `_bmad-output/implementation-artifacts/sprint-status.yaml`.

**Asserted byte-unchanged (AC-36):** `apps/api/app/core/db/models/_entities.py`; `apps/api/app/core/db/models/__init__.py`; `apps/api/migrations/**`; `apps/api/app/core/db/seed.py`; `apps/api/scripts/seed_browse_categories.py`; `apps/api/app/modules/sot/router.py`; `apps/api/app/modules/sot/service.py`; `apps/api/app/modules/sot/admin_router.py`; `apps/api/app/main.py`; `apps/api/app/core/auth/csrf.py`; `apps/api/app/core/auth/dependencies.py`; `apps/api/tests/test_sot_categories.py`; `apps/api/tests/test_sot_models_category_scope.py`; `apps/api/tests/test_sot_models_tag_search.py`; `apps/api/tests/test_sot_admin_tag_groups.py`; `apps/api/tests/test_sot_admin_tags.py`; `apps/api/tests/test_sot_auth_boundary.py`; `apps/api/tests/test_route_enforcement_gate.py`; `apps/api/tests/test_csrf_middleware.py`; `apps/api/tests/test_browse_category_entity.py`; `apps/api/tests/test_migration_0020.py`; `apps/api/tests/test_seed_browse_categories.py`; `apps/api/tests/test_orm_migration_parity.py`; `apps/api/tests/conftest.py`; `apps/web/**`; `workers/**`; `docs/operations.md` (F-11); `pyproject.toml`; `uv.lock`; `infra/scripts/check-all.sh`; all `_bmad-output/planning-artifacts/**`.

## 8. Dev Notes

### 8.1 Router file shape — mirror `tag_group_admin_router.py` verbatim

```python
"""Admin-only browse-category governance write endpoints (Story 49.5).

Prefix: /api/admin
Auth: `current_admin` (admin role only — 403 `admin_required` otherwise).

Deliberately SEPARATE from `sot/admin_router.py` (the agent-write ingestion
surface): categories are admin curation, never agent-writable — per ratified
Decision AY (architecture.md:3334-3341), which places every route in this
router under `current_admin`, mirroring the identical admin-curation/
agent-ingestion split `tag_group_admin_router.py` already applies (FR25-ADMIN-1
/ Decision AW, D-ADMINONLY-1) — not a task-local invention (see story §6 F-9).
Lives under the `sot-admin-governance` tag alongside tag-group governance.

Owns:
  POST   /api/admin/categories                        — create
  PATCH  /api/admin/categories/{category_id}           — rename/reorder/reparent
  DELETE /api/admin/categories/{category_id}           — delete (409 unless clean or detach=true)
  PUT    /api/admin/models/{model_id}/categories        — replace-set assignment
"""
```

### 8.2 `create_browse_category` — parent validation ahead of insert

```python
def create_browse_category(session, *, payload, actor_user_id) -> BrowseCategory:
    if payload.parent_id is not None:
        parent = session.get(BrowseCategory, payload.parent_id)
        if parent is None:
            raise LookupError("parent not found")
        if parent.parent_id is not None:
            raise ValueError("parent_not_root")
    now = datetime.datetime.now(datetime.UTC)
    cat = BrowseCategory(
        slug=payload.slug, name_en=payload.name_en, name_pl=payload.name_pl,
        description_en=payload.description_en, description_pl=payload.description_pl,
        inclusion_criterion=payload.inclusion_criterion, position=payload.position,
        parent_id=payload.parent_id, created_at=now, updated_at=now,
    )
    session.add(cat)
    try:
        session.flush()
    except IntegrityError as exc:
        session.rollback()
        raise ValueError("slug_conflict") from exc
    _audit_entity(session, action="browse_category.create", entity_type="browse_category",
                  entity_id=cat.id, actor_user_id=actor_user_id, after=_browse_category_snapshot(cat))
    session.commit()
    session.refresh(cat)
    return cat
```

### 8.3 `update_browse_category` — the three independent reparent checks

```python
def update_browse_category(session, *, category_id, patch, actor_user_id) -> BrowseCategory:
    cat = session.get(BrowseCategory, category_id)
    if cat is None:
        raise LookupError("category not found")
    before = _browse_category_snapshot(cat)
    data = patch.model_dump(exclude_unset=True)

    if "parent_id" in data and data["parent_id"] is not None:
        new_parent_id = data["parent_id"]
        if new_parent_id == category_id:
            raise ValueError("self_cycle")
        parent = session.get(BrowseCategory, new_parent_id)
        if parent is None:
            raise LookupError("parent not found")
        if parent.parent_id is not None:
            raise ValueError("parent_not_root")
        has_children = session.exec(
            select(BrowseCategory.id).where(BrowseCategory.parent_id == category_id)
        ).first()
        if has_children is not None:
            raise ValueError("reparent_exceeds_depth")

    for field, value in data.items():
        setattr(cat, field, value)
    cat.updated_at = datetime.datetime.now(datetime.UTC)
    after = _browse_category_snapshot(cat)
    session.add(cat)
    try:
        session.flush()
    except IntegrityError as exc:
        session.rollback()
        raise ValueError("slug_conflict") from exc
    _audit_entity(session, action="browse_category.update", entity_type="browse_category",
                  entity_id=cat.id, actor_user_id=actor_user_id, before=before, after=after)
    session.commit()
    session.refresh(cat)
    return cat
```

Note the **no-op self-rename** case (AC-11): patching `slug` to its own current value reaches `session.flush()` with the row's own pre-existing unique index entry unchanged — SQLite's unique index does not conflict with a row against itself, so this returns 200 without any special-case code.

### 8.4 `delete_browse_category` — children checked first, unconditionally

```python
def delete_browse_category(session, *, category_id, detach, actor_user_id) -> None:
    cat = session.get(BrowseCategory, category_id)
    if cat is None:
        raise LookupError("category not found")

    child_ids = session.exec(
        select(BrowseCategory.id).where(BrowseCategory.parent_id == category_id)
    ).all()
    if child_ids:
        raise ValueError("category_has_children")

    assignment_rows = session.exec(
        select(ModelBrowseCategory).where(ModelBrowseCategory.category_id == category_id)
    ).all()
    if assignment_rows and not detach:
        raise ValueError("category_in_use")

    before = _browse_category_snapshot(cat)
    if assignment_rows:
        detached_ids = [row.model_id for row in assignment_rows]
        before["detached_model_ids"] = [str(m) for m in detached_ids]
        before["detached_model_count"] = len(detached_ids)
        for row in assignment_rows:
            session.delete(row)
        session.flush()

    _audit_entity(session, action="browse_category.delete", entity_type="browse_category",
                  entity_id=cat.id, actor_user_id=actor_user_id, before=before)
    session.delete(cat)
    session.commit()
```

### 8.5 `replace_model_categories` — duplicate-id guard the tags precedent lacks

```python
def replace_model_categories(session, *, model_id, payload, actor_user_id) -> list[BrowseCategory]:
    _get_model_active(session, model_id)  # LookupError("model not found") — reused verbatim

    if len(payload.category_ids) != len(set(payload.category_ids)):
        raise ValueError("duplicate_category_ids")

    for cid in payload.category_ids:
        if session.get(BrowseCategory, cid) is None:
            raise ValueError(f"category not found: {cid}")

    before_ids = [row.category_id for row in session.exec(
        select(ModelBrowseCategory).where(ModelBrowseCategory.model_id == model_id)
    ).all()]

    existing_rows = session.exec(
        select(ModelBrowseCategory).where(ModelBrowseCategory.model_id == model_id)
    ).all()
    for row in existing_rows:
        session.delete(row)
    session.flush()

    for cid in payload.category_ids:
        session.add(ModelBrowseCategory(model_id=model_id, category_id=cid))
    session.flush()

    after_ids = list(payload.category_ids)
    _audit_entity(session, action="model.update", entity_type="model", entity_id=model_id,
                  actor_user_id=actor_user_id,
                  before={"category_ids": [str(c) for c in before_ids]},
                  after={"category_ids": [str(c) for c in after_ids]})
    session.commit()

    cats = (session.exec(select(BrowseCategory).where(BrowseCategory.id.in_(after_ids))).all()
            if after_ids else [])
    return sorted(cats, key=lambda c: (c.position, c.slug))
```

Router-level `HTTPException` mapping: `LookupError` → 404, `ValueError("duplicate_category_ids")` → 400, any other `ValueError` (the `f"category not found: {cid}"` shape) → 404 — matching `PUT /models/{id}/tags`'s documented ("404 if model or any tag id is missing") contract exactly (§6 F-9).

### 8.6 Anti-patterns — do not do these

- ❌ Trying to catch `IntegrityError` from the DELETE to distinguish "children" vs. "assignments" — SQLite's error message does not reliably name which FK fired; the proactive pre-query approach in §8.4 is required, not stylistic.
- ❌ Letting `detach=true` reparent or delete children — that redefines the flag's documented meaning (model-assignment detach only) with no planning-artifact mandate; AC-21/AC-22 forbid this explicitly.
- ❌ Silently de-duplicating `category_ids` in the replace-set payload instead of rejecting with 400 — a caller sending literal duplicates has a bug worth surfacing, not silently masking (AC-28).
- ❌ Building a `revision`/`ETag`/`If-Match` precondition on the replace-set endpoint "while we're in here" — explicitly out of scope; §3.9/AC-32 name it as a **future** trigger, not this story's work.
- ❌ Adding `category`/`category_id` as a property anywhere on `ModelSummary`/`ModelDetail`/`ModelCreate`/`ModelPatch`/`ShareModelView`, or naming any new schema bare `Category*` — two shipped guard tests fail immediately (§3.6).
- ❌ Fixing `docs/operations.md`'s stale "Admin write" enumeration as a drive-by — reported (F-11), not edited, to keep this story's diff to exactly its own surface.
- ❌ Retroactively adding a duplicate-id guard to `replace_model_tags` "since we're fixing the same bug for categories" — that is a different, closed story's surface; report it (already done, F-9), don't silently patch it here.

## 9. Risks

| # | Risk | Mitigation |
|---|---|---|
| R-1 | Implementer follows the 42.4 story's stale `create_category`/`admin_service.py:1150-1306` citation and either invents dead code or gets confused about the real template. | §6 F-1 states the fact plainly and points at the real, verified template (`create_tag_group`/`update_tag_group`/`delete_tag_group`). |
| R-2 | A category-with-children delete raises a raw, unhandled `IntegrityError` (500) instead of a clean 409, because the planning artifacts only speak of "assignments." | §6 F-4 + AC-21/AC-22 + §8.4's children-first proactive query make this a first-class, tested case. |
| R-3 | `detach=true` scope creeps into reparenting/deleting children "to be helpful," silently redefining the flag. | AC-21 makes children-present a flat, unconditional 409 regardless of `detach`; §8.6 lists it as an explicit anti-pattern. |
| R-4 | A literal-duplicate `category_ids` payload crashes with an uncaught `IntegrityError` → 500, replaying the `PUT /models/{id}/tags` gap this story was supposed to close. | AC-28 + §8.5's explicit pre-check, tested in T5.1 before any session mutation. |
| R-5 | The admin-only auth tier on `PUT /categories` is later found inconsistent with `agent`-capable model ingestion, and a future agent workflow needs to set categories but can't. | §6 F-9 flags this explicitly as an unresolved-rationale tightening for the Validate pass / future story to revisit; not silently asserted as obviously correct. |
| R-6 | The new governance routes are invisible to `test_every_admin_sot_operation_has_summary/description` because `sot-admin-governance` sits outside `TARGET_ROUTER_TAGS`, and ship with an empty `description` unnoticed. | §6 F-5 + AC-37 + T6.1 extend the existing `_GOVERNANCE_ROUTES`-driven test rather than relying on the generic (non-covering) gate. |
| R-7 | Audit snapshot payload grows unbounded on a category with a very large assigned-model set, mirroring the still-open epic:42 concern. | AC-35 states the accepted posture explicitly (paired count, re-evaluate-if-large, not cap-now) so it is not mistaken for an oversight at review time. |

## 10. Non-goals (this story ships none of these)

Any frontend/admin screen (`52.2`); the 1–3-per-model advisory warning UI/logic (`FR26-CAT-3`, owned by `52.2`); curation QA surfaces (`52.3`); category-assignment automation or inference from tags (explicitly forbidden by Decision AX/AY); any new Alembic migration or DDL change; any seed-script change or re-run; any optimistic-concurrency mechanism (`revision`/ETag/`If-Match`) on the replace-set endpoint — a named future trigger, not this story's work; cascading child-delete or auto-reparent-on-delete; a truncation/cap mechanism on the audit payload; correcting `docs/operations.md`'s stale "Admin write" endpoint enumeration (reported, not edited — F-11); any change to `GET /api/categories[/{slug}]`, `GET /api/models?category=`, or `ModelDetail.categories` (Story 49.3, untouched); any change to `PUT /models/{id}/tags` or any other existing agent-write route; any live-database, production, push, merge or deploy action.

## 11. Branch and commit atomicity

One story branch off `main`, one commit containing the **9** product/doc files in §7 (6 production + 3 test, 2 of the 3 test files NEW), plus the two workflow records. `check-all.sh` 16/16 must pass on the branch alone. The story is independently mergeable: it depends only on Story 49.1 and Story 49.3, both already on `main` at this baseline.

## 12. Traceability

| AC | Requirement / Decision | Source |
|---|---|---|
| AC-1…AC-7 | FR26-ADMIN-1 create; Decision AY admin governance | `architecture.md:3334-3341`; `epics.md:4491-4493` |
| AC-8…AC-16 | FR26-ADMIN-1 rename/reorder; FR26-CAT-4 depth-2 + self-cycle | `architecture.md:3299` (Decision AX depth ceiling); `epics.md:4491-4493` |
| AC-17…AC-24 | FR26-ADMIN-1 delete/409/detach; epic:42 audit precedent | `sprint-status.yaml:383`; `architecture.md:3337`; `sprint-status.yaml:439-442` (epic:42 action item) |
| AC-25…AC-33 | FR26-ADMIN-1 replace-set; honest LWW concurrency posture | `architecture.md:3338-3340`; `sprint-status.yaml:383,398` |
| AC-34, AC-35 | Audit contract; D-AUDIT-2 bounded snapshot; epic:42 precedent | `app/core/audit.py:16-49`; `42-4-admin-group-governance.md` AC #6 (D-AUDIT-2) |
| AC-36 | Additive-only invariant; route-enforcement gate | `test_route_enforcement_gate.py`; §3.5 |
| AC-37 | OpenAPI honesty for off-target-tag-set routes | `test_openapi_agent_surface.py:61,257-297` |

## 13. Project structure notes

One new router file (`browse_category_admin_router.py`), sized and shaped like its direct sibling `tag_group_admin_router.py` — the codebase's established pattern of one focused router module per governance taxonomy family (tags/tag-groups vs. categories), rather than folding new write surface into the already-980-line agent-write `admin_router.py`. Request/response schemas and service functions extend the existing shared `admin_schemas.py`/`admin_service.py`/`sot/schemas.py` files rather than fragmenting further — matching how tag-group governance itself was added to those same three files in Story 42.4. No new top-level module, package, or directory. No conflict with the unified project structure was found.

## 14. Testing standards summary

`.venv/bin/pytest` from `apps/api` (the venv-relative form `check-all.sh:86-87` uses). Final commands:

```bash
# targeted (new coverage)
cd apps/api && .venv/bin/pytest -q tests/test_sot_admin_categories.py tests/test_sot_admin_model_categories.py

# non-regression witnesses (must pass byte-unmodified, or additive-only for the one extended file)
cd apps/api && .venv/bin/pytest -q \
  tests/test_sot_categories.py tests/test_sot_models_category_scope.py \
  tests/test_sot_admin_tag_groups.py tests/test_sot_admin_tags.py \
  tests/test_sot_auth_boundary.py tests/test_openapi_agent_surface.py \
  tests/test_route_enforcement_gate.py tests/test_csrf_middleware.py \
  tests/test_orm_migration_parity.py tests/test_browse_category_entity.py

# full suite + lint
cd apps/api && .venv/bin/pytest -q
cd apps/api && .venv/bin/ruff format --check . && .venv/bin/ruff check .

# controller-owned frozen gate (NOT run by the implementer)
infra/scripts/check-all.sh
```

## 15. Dev Agent Record

**Pass:** native `bmad-dev-story` (Dev Story, menu DS), 2026-07-28, Claude/native-BMAD implementation author session. **NO human review of any kind** — no Ezop signature, no Ezop review, no Laura review is recorded, implied or claimable from this section. The implementer did **not** run `bmad-code-review`, did **not** run Aider or any other independent reviewer, and did **not** commit, push, merge, deploy, seed, migrate, or touch live state. The tree is left uncommitted for controller review.

**Routing.** `AGENTS.md`, `CLAUDE.md`, `_bmad-output/project-context.md`, the Laura Agent Rulebook (`/home/ezop/repos/configs/agent-toolchain/rules/LAURA_AGENT_RULEBOOK.md`, 190 lines) and this full story artifact were read before any edit. Mandatory session-start native `bmad-help` executed FIRST against `_bmad/_config/bmad-help.csv` + `_bmad/_config/skill-manifest.csv`: `bmad-help.csv:28` (`bmad-dev-story` / Dev Story / **DS** / phase `4-implementation`, `preceded-by=bmad-create-story:validate`, `required=true`) and `skill-manifest.csv:42` confirm DS as the canonical next skill after the §16 Validate PASS. Effective config resolved with `uv run --python 3.11 _bmad/scripts/resolve_config.py --project-root .` → `communication_language=Polish`, `document_output_language=English`, `implementation_artifacts={project-root}/_bmad-output/implementation-artifacts`; `_bmad/bmm/config.yaml` → `user_skill_level=intermediate`. Skill customization resolved with `python3 _bmad/scripts/resolve_customization.py --skill .claude/skills/bmad-dev-story --key workflow` → `activation_steps_prepend=[]`, `activation_steps_append=[]`, `on_complete=""`, `persistent_facts=["file:{project-root}/**/project-context.md"]` (loaded). No team override (`_bmad/custom/bmad-dev-story.toml` absent; `_bmad/custom/config.toml` is the untouched installer comment stub) and no user override.

**Worktree posture.** Branch `feat/E49.5-admin-category-governance`, HEAD `b1a46a2` (the story's own `docs:` commit, sitting directly on the story's recorded `baseline_commit` `0df663e`). The frontmatter `baseline_commit: 0df663e…` was **preserved, not overwritten**, per the DS skill's explicit preserve-existing rule. Working tree clean at session start. `.claude/skills/` (90 skill directories) was already present in this worktree — no repair needed, unlike the Create pass's §17.6. `apps/api/.venv` was **absent** (gitignored local toolchain, never materialised by `git worktree add`) and was bootstrapped IN THIS WORKTREE with `uv sync --frozen --all-extras` (uv 0.5.9, Python 3.12.3). It was **not** copied from, and no command was run against, the sibling checkout `/home/ezop/repos/3d-portal`. `.venv` remains untracked and produces zero git diff.

### Agent Model Used

Claude Opus 5 (1M context), model id `claude-opus-5[1m]`, driving the native BMAD `bmad-help` → `bmad-dev-story` chain. Single serial session, no subagents dispatched.

### Debug Log References

Every command below was executed in `/home/ezop/worktrees/3d-portal-e49-5-dev/apps/api` unless stated otherwise. Counts are verbatim pytest output, not inferred.

**T0 — baseline (no edits).**

- T0.1 `.venv/bin/pytest -q tests/test_sot_categories.py tests/test_sot_models_category_scope.py tests/test_sot_admin_tag_groups.py tests/test_sot_admin_tags.py tests/test_route_enforcement_gate.py tests/test_openapi_agent_surface.py tests/test_csrf_middleware.py` → **`145 passed, 198 warnings in 19.89s`**.
- T0.2 `grep -rn "def create_category\|def update_category\|def delete_category\|class Category\b" apps/api/app/` → **zero matches (exit 1)**. §6 F-1 confirmed at this baseline: the 42.4 story's `create_category` / `admin_service.py:1150-1306` citation is stale and was not followed. The real template used throughout is `create_tag_group` / `update_tag_group` / `delete_tag_group` / `replace_model_tags`.

**T1 — schemas. GENUINE RED.**

- RED: `.venv/bin/pytest -q tests/test_sot_admin_categories.py` → **`9 failed in 0.28s`**, every one an import failure against a symbol that did not exist, e.g. verbatim:
  `E  ImportError: cannot import name 'BrowseCategoryAdminRead' from 'app.modules.sot.schemas'`
  `E  ImportError: cannot import name 'ModelCategoriesReplace' from 'app.modules.sot.admin_schemas'`
- GREEN after T1.2/T1.3: **`9 passed in 0.16s`**.

**T2 — `POST /api/admin/categories`. GENUINE RED.**

- RED: `.venv/bin/pytest -q tests/test_sot_admin_categories.py tests/test_openapi_agent_surface.py` → **`8 failed, 43 passed, 14 warnings in 2.19s`**. Verbatim:
  `>  assert r.status_code == 201, r.text` / `E  AssertionError: {"detail":"Not Found"}` / `E  assert 404 == 201`
  and, from the folded-in T6.1 `_GOVERNANCE_ROUTES` extension, `E  KeyError: '/api/admin/categories'`.
- **Honest labelling:** `test_create_category_unknown_parent_404` PASSED at this RED checkpoint **by construction** — an absent route also returns 404, so at T2 RED it proved nothing. It only becomes load-bearing after the route exists (it is green in every later run, where the 404 is the service's parent-existence check). Recorded as a pass-by-construction guard, not as a RED.
- GREEN after T2.2–T2.5: `.venv/bin/pytest -q tests/test_sot_admin_categories.py tests/test_openapi_agent_surface.py tests/test_route_enforcement_gate.py` → **`54 passed, 16 warnings in 2.48s`**.

**T3 — `PATCH /api/admin/categories/{category_id}`. GENUINE RED (authored as one batch).**

- RED: **`17 failed, 51 passed, 48 warnings in 4.83s`** — 15 new PATCH tests plus the two `_GOVERNANCE_ROUTES` OpenAPI guards.
- **Honest labelling:** `test_patch_unknown_category_404` and `test_patch_category_unknown_parent_404` PASSED at this RED checkpoint **by construction** (absent route → 404). Both are pass-by-construction guards at T3 RED and only become load-bearing once the route exists.
- GREEN after T3.2/T3.3 required one unpredicted repair (see Completion Note **D-1**); final: `.venv/bin/pytest -q tests/test_sot_admin_categories.py tests/test_openapi_agent_surface.py tests/test_route_enforcement_gate.py tests/test_sot_categories.py` → **`93 passed, 92 warnings in 8.91s`**.

**T4 — `DELETE /api/admin/categories/{category_id}`. GENUINE RED, no pass-by-construction cases.**

- RED: **`14 failed, 66 passed, 74 warnings in 6.91s`**. All 12 new DELETE tests failed genuinely — including both `test_delete_unknown_category_404` parametrisations, which returned **405 Method Not Allowed** (the path existed for PATCH but not DELETE), so nothing passed vacuously here.
- GREEN after T4.2/T4.3: `.venv/bin/pytest -q tests/test_sot_admin_categories.py tests/test_openapi_agent_surface.py tests/test_route_enforcement_gate.py` → **`83 passed, 76 warnings in 7.11s`**.

**T5 — `PUT /api/admin/models/{model_id}/categories`. GENUINE RED.**

- RED: `.venv/bin/pytest -q tests/test_sot_admin_model_categories.py tests/test_openapi_agent_surface.py` → **`9 failed, 36 passed, 22 warnings in 2.68s`**.
- **Honest labelling:** `test_replace_categories_unknown_category_404_no_side_effect`, `test_replace_categories_unknown_model_404` and `test_replace_categories_soft_deleted_model_404` PASSED at this RED checkpoint **by construction** (absent route → 404). Three pass-by-construction guards, load-bearing only after T5.3.
- GREEN after T5.2/T5.3: `.venv/bin/pytest -q tests/test_sot_admin_model_categories.py tests/test_sot_admin_categories.py tests/test_openapi_agent_surface.py tests/test_route_enforcement_gate.py` → **`93 passed, 96 warnings in 8.85s`**.

**T6 — OpenAPI honesty + non-regression.**

- T6.1 was **not** run as a separate isolated RED. As §5 T6.1 instructs, the `_GOVERNANCE_ROUTES` extension was folded into the T2/T3/T4/T5 RED batches — one `(METHOD, path)` tuple added per slice, so each checkpoint stayed runnable and each tuple had its own genuine RED (`KeyError: '<path>'`) before its route existed. Stated plainly here rather than presented as a standalone RED.
- T6.2/T6.3 witnesses, run byte-unmodified except where Completion Notes D-1/D-2 disclose otherwise: `.venv/bin/pytest -q tests/test_sot_categories.py tests/test_sot_models_category_scope.py tests/test_sot_admin_tag_groups.py tests/test_sot_admin_tags.py tests/test_sot_auth_boundary.py tests/test_openapi_agent_surface.py tests/test_route_enforcement_gate.py tests/test_csrf_middleware.py tests/test_orm_migration_parity.py tests/test_browse_category_entity.py` → **`196 passed, 255 warnings in 26.99s`**. `test_csrf_middleware.py` is inside that set and passes unmodified, which is the whole of this story's AC-5 / AC-24 / AC-33 CSRF evidence — no per-route CSRF test was authored (§6 F-8).
- Focused Story 49.5 coverage: `.venv/bin/pytest -q tests/test_sot_admin_categories.py tests/test_sot_admin_model_categories.py` → **`55 passed, 92 warnings in 7.47s`** (`--collect-only`: **45** in `test_sot_admin_categories.py`, **10** in `test_sot_admin_model_categories.py`).

**T7 — scope + quality.**

- T7.1 `.venv/bin/ruff format --check .` → **`281 files already formatted`**; `.venv/bin/ruff check .` → **`All checks passed!`**. (One `I001` import-ordering error was raised and fixed with `ruff check --fix` before this final clean run; `ruff format` reformatted 3 files.)
- T7.2 `.venv/bin/pytest -q` (full `apps/api` suite): first run **`2 failed, 1903 passed, 3 skipped, 2236 warnings in 418.94s`** — both failures were the `KNOWN_ENTITY_TYPES` drift guards described in Completion Note **D-2**. After that repair, final full-suite run → **`1905 passed, 3 skipped, 2236 warnings in 426.35s (0:07:06)`**.
- T7.3 `git diff --check` → **empty, exit 0**. `git diff --name-only` and `git status --porcelain` inventory is the File List below. Every path in §7's "asserted byte-unchanged" list is genuinely absent from that inventory — specifically `_entities.py`, `migrations/**`, `seed.py`, `scripts/seed_browse_categories.py`, `sot/router.py`, `sot/service.py`, `sot/admin_router.py`, `main.py`, `core/auth/csrf.py`, `core/auth/dependencies.py`, `tests/conftest.py`, `apps/web/**`, `workers/**`, `docs/operations.md`, `pyproject.toml`, `uv.lock`, `infra/scripts/check-all.sh` and all `_bmad-output/planning-artifacts/**`.
- Route inventory generated from the live OpenAPI doc: `POST /api/admin/categories`, `PATCH /api/admin/categories/{category_id}`, `DELETE /api/admin/categories/{category_id}`, `PUT /api/admin/models/{model_id}/categories` — all four carrying exactly `['sot-admin-governance']`, none carrying `agent-write`.

**T8 — deliberately left UNCHECKED.** `check-all.sh` 16/16, the NFR26-DETERMINISM-1 triple, native `bmad-code-review`, the independent Aider review and the commit/merge/deploy chain are controller-owned and were not run by this pass.

### Post-review repair pass (native `bmad-dev-story`, 2026-07-28)

**Pass:** native `bmad-dev-story` (Dev Story, menu **DS**), second invocation, fresh Claude Opus 5 (1M) session, model id `claude-opus-5[1m]`, native BMAD 6.10.0, same worktree `/home/ezop/worktrees/3d-portal-e49-5-dev`, same branch `feat/E49.5-admin-category-governance`, HEAD `b1a46a2`. The preserved uncommitted 13-path tree was repaired in place — nothing was reset, stashed or discarded. **NO human review of any kind** — no Ezop signature, no Ezop review, no Laura review is recorded, implied or claimable from this section. No `bmad-code-review`, no Aider, no Codex, no `check-all.sh`, no determinism triple, no commit, stage, push, merge, deploy, migration, seed, live-DB or network action.

**Routing.** `AGENTS.md`, `CLAUDE.md`, `_bmad-output/project-context.md`, the Laura Agent Rulebook, this full artifact and the CR pass's durable report `/home/ezop/repos/3d-portal/.hermes/run-logs/e49-5-native-review-20260728.md` were all read before any edit. Mandatory native `bmad-help` invoked FIRST via the `Skill` tool, then confirmed against `_bmad/_config/manifest.yaml` (BMad 6.10.0) and `_bmad/_config/bmad-help.csv`: the **CR** row reads *"Story cycle: If issues back to DS if approved then next CS or ER if epic complete"* and the **DS** row is `BMad Method,bmad-dev-story,Dev Story,DS,…,4-implementation,bmad-create-story:validate,,true,,` — so after a `REQUEST_CHANGES` the canonical next skill is **DS**, confirmed in `_bmad/_config/skill-manifest.csv` (`bmad-dev-story` → `_bmad/bmm/4-implementation/bmad-dev-story/SKILL.md`). Customization resolved with `python3 _bmad/scripts/resolve_customization.py --skill .claude/skills/bmad-dev-story --key workflow` → `activation_steps_prepend=[]`, `activation_steps_append=[]`, `on_complete=""`, `persistent_facts=["file:{project-root}/**/project-context.md"]` (loaded). No team override (`_bmad/custom/` holds only `.gitignore` + the installer's `config.toml` stub), no user override. Config via `_bmad/bmm/config.yaml` + `resolve_config.py` → `communication_language=Polish`, `document_output_language=English`, `user_skill_level=intermediate`. The DS workflow's step-3 review-continuation branch applied (this artifact carries a review record and unchecked review follow-ups); story and sprint were already `in-progress`, and the `baseline_commit: 0df663e…` frontmatter was **preserved, not overwritten**, per the skill's explicit preserve-existing rule.

**Genuine RED → GREEN (the two logic-bearing repairs, findings #3 and #4).** RED observed before any production edit, `.venv/bin/pytest -q tests/test_sot_admin_categories.py tests/test_sot_admin_model_categories.py` → **`4 failed, 61 passed, 109 warnings in 10.20s`**. Verbatim failures:

- `test_delete_category_child_race_is_409_not_an_unhandled_500` — `E sqlalchemy.exc.IntegrityError: (sqlite3.IntegrityError) FOREIGN KEY constraint failed` / `E [SQL: DELETE FROM browse_category WHERE browse_category.id = ?]`, i.e. the documented 409 escaping the router unhandled.
- `test_delete_category_assignment_race_is_409_not_an_unhandled_500` — `E AttributeError: module 'app.modules.sot.admin_service' has no attribute '_browse_category_assignments'. Did you mean: '_browse_category_children'?` (the symbol the safety net needs did not exist yet).
- `test_create_category_vanished_parent_is_404_not_a_slug_conflict_409` — `E AssertionError: {"detail":"slug already exists"}` / `E assert 409 == 404`.
- `test_patch_category_vanished_parent_is_404_not_a_slug_conflict_409` — identical `409 == 404` on the reparent path.

GREEN after the production edits: same command → **`65 passed, 110 warnings in 9.28s`** (55 → 65 tests: **54** in `test_sot_admin_categories.py` + **11** in `test_sot_admin_model_categories.py`, `--collect-only`).

**Coverage-only repairs (findings #1 arbitration, #2, #12, #13) — labelled honestly, non-vacuity proven by mutation probe.** These four exercise production behaviour that was already correct, so they pass on first run and **no genuine RED is possible** without deliberately breaking correct code. Per §5's ordering rule they are therefore recorded as coverage/characterisation guards, never dressed up as REDs. To prove they are not vacuous, each was run against a temporary single-line mutation of the production code, which was then restored and verified byte-identical (`sha256` of `admin_service.py` unchanged, `ef7d124c11a0896a…`):

| Mutation applied to `admin_service.py` | Test(s) run | Result |
|---|---|---|
| `_browse_category_snapshot` drops `inclusion_criterion` | `test_create_category_201_admin_read_shape` | **1 failed** |
| `browse_category_model_count` drops the `Model.deleted_at.is_(None)` filter | the two finding-#2 tests | **2 failed** |
| delete gate filtered to live models only (the naive finding-#1 "fix") | `test_soft_deleted_model_assignment_is_preserved_until_explicit_detach` | **1 failed** |
| replace-set `order_by` loses the `slug` tie-break | `test_replace_categories_orders_ties_by_slug` | **1 failed** |

**Gates actually executed in this pass** (all in `/home/ezop/worktrees/3d-portal-e49-5-dev/apps/api` unless noted; output verbatim, not inferred):

| Command | Result |
|---|---|
| `.venv/bin/pytest -q tests/test_sot_admin_categories.py tests/test_sot_admin_model_categories.py` (focused, GREEN) | `65 passed, 110 warnings in 9.28s` |
| the story's exact §14 non-regression witness set (10 modules) | `196 passed, 255 warnings in 26.53s` |
| `.venv/bin/pytest -q tests/test_audit.py tests/test_2fa_schema.py tests/test_sot_admin_models.py` (audit/entity-type + admin-model witnesses) | `51 passed, 56 warnings in 8.71s` |
| `.venv/bin/pytest -q tests/test_openapi_agent_surface.py` (after the finding-#11 rename) | `35 passed, 4 warnings in 1.04s` |
| `.venv/bin/pytest -q` (full `apps/api` suite) | **`1915 passed, 3 skipped, 2254 warnings in 420.10s (0:07:00)`** |
| `.venv/bin/python -m ruff format --check .` | `281 files already formatted` |
| `.venv/bin/python -m ruff check .` | `All checks passed!` |
| `git diff --check` (repo root) | empty, **exit 0** |
| `yaml.safe_load` on `sprint-status.yaml` | parses, 336 `development_status` keys, `epic-49` untouched |

The full-suite delta is exactly this pass's ten new tests: `1905 passed, 3 skipped` (CR pass, independently re-derived) → `1915 passed, 3 skipped`. No pre-existing test changed status.

**Production edits made (all inside this story's own new code — no existing pre-49.5 function body was touched).**

- `admin_service.py` — ADD `_browse_category_assignments` (the extracted delete pre-query, needed so both halves of the prequery→commit race are reproducible in-process); ADD `_raise_browse_category_flush_error` (`-> NoReturn`, the post-rollback cause re-derivation shared by create and update); `delete_browse_category`'s `session.delete` + `commit` wrapped in `except IntegrityError` with children-first re-derivation after rollback; both flush handlers routed through the new helper; `NoReturn` added to the `typing` import. Docstrings corrected in place: the delete docstring's "never by catching an IntegrityError" claim, and a new paragraph stating the controller-arbitrated preservation-first posture for soft-deleted assignments.
- No other production file changed. `browse_category_admin_router.py`, `admin_schemas.py`, `schemas.py`, `audit.py` and `router.py` are byte-unchanged by this pass — the router already maps `LookupError` → 404 and `ValueError` → 409/422, so both repairs surface correctly with **no** route-layer edit and **no** broadening of the API response shape.

**Disclosed limitation — §8's Dev Notes code sketches are now superseded and were deliberately left alone.** §8.2, §8.3 and §8.4 contain create-pass pseudocode that still shows the pre-repair `except IntegrityError: rollback; raise ValueError("slug_conflict")` and the pre-repair delete tail. They are **not** in the set of sections a `bmad-dev-story` pass may modify (frontmatter `baseline_commit`, Tasks/Subtasks checkboxes, Dev Agent Record, File List, Change Log, Status), so they were not edited. They remain accurate as the Create pass's historical record; the shipped behaviour is what this section and the source describe. Flagged rather than silently rewritten.

**Section-numbering / record-wording check.** §1…§20 re-scanned after the review pass's writes: numbering is intact, with no duplicate, gap or orphaned cross-reference (`§20`, `§15`, `§6 F-n` all resolve). The one genuinely stale sentence the review pass left behind is §20's "One Minor residual: the function name now asserts the opposite of what it tests (filed as a Patch item)" — true when written, closed by this pass's finding-#11 rename. §20 is the CR pass's own durable record and was **not** rewritten; the correction lives here and in the Review Findings checkbox. §15's Completion Notes sentence about `browse_category_model_count` was corrected in place (below) because it is inside this Dev Agent Record.

### Post-RE-REVIEW repair pass (native `bmad-dev-story`, 2026-07-28)

**Pass:** native `bmad-dev-story` (Dev Story, menu **DS**), third invocation, fresh Claude Opus 5 (1M) session, model id `claude-opus-5[1m]`, native BMAD 6.10.0, same worktree `/home/ezop/worktrees/3d-portal-e49-5-dev`, same branch `feat/E49.5-admin-category-governance`, HEAD `b1a46a2`. The preserved uncommitted 14-path tree was repaired in place — nothing was reset, stashed or discarded, and no path was added or removed. **NO human review of any kind** — no Ezop signature, no Ezop review, no Laura review is recorded, implied or claimable from this section. No `bmad-code-review`, no Aider, no Codex, no `check-all.sh`, no determinism triple, no commit, stage, push, merge, deploy, migration, seed, live-DB or network action.

**Routing.** `AGENTS.md`, `CLAUDE.md`, `_bmad-output/project-context.md`, the Laura Agent Rulebook, this artifact and the RE-REVIEW's durable report `/home/ezop/repos/3d-portal/.hermes/run-logs/e49-5-native-rereview-20260728.md` (344 lines, read in full) were all read before any edit. Mandatory native `bmad-help` invoked FIRST via the `Skill` tool, then confirmed against `_bmad/_config/bmad-help.csv`: the **CR** row (`:29`) reads *"Story cycle: If issues back to DS if approved then next CS or ER if epic complete"* and the **DS** row (`:28`) is `BMad Method,bmad-dev-story,Dev Story,DS,…,4-implementation,bmad-create-story:validate,,true,,` — so after a second `REQUEST_CHANGES` the canonical next skill is again **DS**, confirmed in `_bmad/_config/skill-manifest.csv:42` (`bmad-dev-story` → `_bmad/bmm/4-implementation/bmad-dev-story/SKILL.md`). Customization resolved with `python3 _bmad/scripts/resolve_customization.py --skill .claude/skills/bmad-dev-story --key workflow` → `activation_steps_prepend=[]`, `activation_steps_append=[]`, `on_complete=""`, `persistent_facts=["file:{project-root}/**/project-context.md"]` (loaded, 321 lines). No team or user override. Config via `resolve_config.py` → `communication_language=Polish`, `document_output_language=English`, `user_skill_level=intermediate`. The DS step-3 review-continuation branch applied; story and sprint were already `in-progress`; the `baseline_commit: 0df663e…` frontmatter was **preserved, not overwritten**.

**Genuine RED → GREEN (the two logic-bearing repairs, N-1 and N-2).** RED observed before any production edit, `.venv/bin/pytest -q tests/test_sot_admin_categories.py tests/test_sot_admin_model_categories.py` → **`4 failed, 67 passed, 120 warnings in 10.92s`**. Verbatim failures:

- `test_delete_category_unrelated_integrity_failure_is_not_a_false_409` — `E AssertionError: unrelated integrity failure reported as category_in_use` / `E assert 'http-409' != 'http-409'`, i.e. the re-review's reproduction reproduced independently: a category with zero children and zero assignments answering `409 category_in_use`.
- `test_replace_categories_vanished_category_race_is_404_not_an_unhandled_500` and `test_replace_categories_vanished_model_race_is_404_not_an_unhandled_500` — `E AttributeError: module 'app.modules.sot.admin_service' has no attribute '_validate_browse_category_ids'` / `'_get_model_active'` interleave: the extracted guard the safety net needs did not exist yet.
- `test_replace_categories_validates_every_id_before_any_delete_is_attempted` — same `AttributeError` on `_validate_browse_category_ids` (a symbol-absence RED; the ordering property itself was already true in the shipped code — stated plainly rather than claimed as a behaviour RED).

GREEN after the production edits: same command → **`71 passed, 122 warnings in 10.37s`** (65 → 71 tests, `--collect-only`: **56** in `test_sot_admin_categories.py` + **15** in `test_sot_admin_model_categories.py`).

**Coverage-only repairs (N-3, and both halves of N-4) — labelled honestly, non-vacuity proven by mutation probe.** These exercise production behaviour that was already correct, so they pass on first run and **no genuine RED is possible** without deliberately breaking correct code. Per §5's ordering rule they are recorded as coverage/characterisation guards, never dressed up as REDs. Each was run against a temporary mutation of `admin_service.py`, which was then restored and verified byte-identical (`sha256` = `eff4e930c2b90717c2018ef37e4630dad3d35daa3108b3afe3178e0adb669b09`, re-derived after the last probe):

| Mutation applied to `admin_service.py` | Test(s) run | Result |
|---|---|---|
| `after = _browse_category_snapshot(cat)` moved ABOVE the `setattr` loop (the re-review's own N-3 mutant, previously `70 passed`) | the two focused files + `test_audit.py` | **1 failed, 75 passed** — `test_patch_category_partial_update_200` |
| proactive children pre-query deleted from `delete_browse_category` (the re-review's own N-4a mutant, previously `1 failed, 64 passed`) | the two focused files | **2 failed, 69 passed** — the race probe *and* the new sentinel |
| existence loop moved AFTER the deletes with a compensating rollback (the Acceptance Auditor's own N-4b mutation, previously all 11 green) | `test_sot_admin_model_categories.py` | **1 failed, 14 passed** — only the new ordering test |

**Gates actually executed in this pass** (all in `/home/ezop/worktrees/3d-portal-e49-5-dev/apps/api` unless noted; output verbatim, not inferred):

| Command | Result |
|---|---|
| `.venv/bin/pytest -q tests/test_sot_admin_categories.py tests/test_sot_admin_model_categories.py` (RED) | `4 failed, 67 passed, 120 warnings in 10.92s` |
| the same (GREEN, after the production edits and after `ruff format`) | `71 passed, 122 warnings in 10.21s` |
| the story's exact §14 non-regression witness set (10 modules) | `196 passed, 256 warnings in 28.46s` |
| `.venv/bin/pytest -q` (full `apps/api` suite) | **`1921 passed, 3 skipped, 2266 warnings in 415.80s (0:06:55)`** |
| `.venv/bin/python -m ruff format --check .` | `281 files already formatted` (one new test file was reformatted by `ruff format` first, then re-verified) |
| `.venv/bin/python -m ruff check .` | `All checks passed!` |
| `git diff --check` (repo root) | empty, **exit 0** |

The full-suite delta is exactly this pass's six new tests: `1915 passed, 3 skipped` (the repair pass's number, which the re-review observed but deliberately did not claim) → `1921 passed, 3 skipped`. No pre-existing test changed status. **This pass DOES claim its own full-suite run:** it was executed on a quiescent tree with no concurrent subagent, no scratch test file and no in-place mutation outstanding (`admin_service.py` sha re-verified before the run), which is precisely the certification the re-review could not make for its own three attempts.

**Production edits made (all inside this story's own new code — no existing pre-49.5 function body was touched).**

- `admin_service.py` — (N-1) `delete_browse_category`'s `except IntegrityError` block now checks children, then assignments, then **re-raises** the original error instead of asserting `category_in_use`; (N-2) ADD `_validate_browse_category_ids` (the extracted per-id existence guard) and `_raise_model_categories_commit_error` (`-> NoReturn`, the post-rollback cause re-derivation), with `replace_model_categories`'s delete/insert/audit/commit block wrapped in `except IntegrityError` and its docstring extended to state the new safety net.
- No other production file changed. `browse_category_admin_router.py`, `admin_schemas.py`, `schemas.py`, `audit.py` and `router.py` are byte-unchanged by this pass — both repairs surface through the router's existing `LookupError` → 404 / `ValueError` → 409/400/422 mapping, with **no** route-layer edit, **no** new status code and **no** response-shape change.

**Explicitly NOT done.** No `deferred-work.md` edit (the seven new re-review defers remain recorded in § Re-review Findings and in the durable report; ledgering them is not part of this repair's authorised scope and is stated here rather than silently skipped). No T8 box ticked — `check-all.sh`, the determinism triple, `bmad-code-review`, the independent Aider review and the commit/merge/deploy chain are controller-owned and were not run. No test weakened, removed or narrowed: every new assertion is additive, and the one edited existing test (`test_patch_category_partial_update_200`) kept all four of its original assertions and gained six. No planning-artifact change, no `epic-49` change, no migration, seed or live-DB action.

**Disclosed limitation — §8's Dev Notes code sketches remain superseded and were again deliberately left alone**, for the same reason the prior repair pass gave: they are outside the sections a `bmad-dev-story` pass may modify. §8.4's delete tail and §8.5's replace-set sketch now differ from the shipped code by the two safety nets. Flagged, not silently rewritten.

### Post-FINAL-review repair pass (native `bmad-dev-story`, 2026-07-28)

**Pass:** native `bmad-dev-story` (Dev Story, menu **DS**), fourth invocation, Claude Opus 5 (1M) session, model id `claude-opus-5[1m]`, native BMAD 6.10.0, same worktree `/home/ezop/worktrees/3d-portal-e49-5-dev`, same branch `feat/E49.5-admin-category-governance`, HEAD `b1a46a2`. The preserved uncommitted 14-path tree was repaired in place — nothing reset, stashed or discarded, no path added or removed. **Scope: exactly the one remaining `patch` finding F-1 from the FINAL review, and the truthful story/status update.** **NO human review of any kind** — no Ezop signature, no Ezop review, no Laura review is recorded, implied or claimable from this section. No `bmad-code-review`, no Aider, no Codex, no `check-all.sh`, no determinism triple, no commit, stage, push, merge, deploy, migration, seed, live-DB or network action.

**Routing.** `AGENTS.md`, `CLAUDE.md`, `_bmad-output/project-context.md`, the Laura Agent Rulebook (`/home/ezop/repos/configs/agent-toolchain/rules/LAURA_AGENT_RULEBOOK.md`) and the FINAL review's durable report `.hermes/run-logs/e49-5-native-final-review-20260728.md` were read before any edit. Routing confirmed against the canonical config surface `_bmad/_config/bmad-help.csv`: the **CR** row (`:29`) reads *"Story cycle: If issues back to DS if approved then next CS or ER if epic complete"* and the **DS** row (`:28`) is `BMad Method,bmad-dev-story,Dev Story,DS,…,4-implementation,bmad-create-story:validate,,true,,` — so after the third `REQUEST_CHANGES` the canonical next step is again **DS**, exactly as after the first two. **Controller disposition of F-1:** repair, not defer. The FINAL review offered both (§ Final-review Findings: "a recorded deferral of F-1 is defensible… provided the deferral is written down"); the controller chose the ~4-line closure so E49.5 proceeds toward closeout with the delete contract honest.

**RED established by mutation, NOT by test-first ordering — stated plainly rather than dressed up as a strict RED → GREEN.** This pass wrote the production fix and the guard together and first ran them together: `.venv/bin/pytest -q tests/test_sot_admin_categories.py tests/test_sot_admin_model_categories.py` → **`72 passed, 124 warnings in 10.51s`** (71 → 72; `--collect-only` **72**). The RED was then proved by reverting the repaired guard to its exact pre-repair form (`if remaining:`) in `admin_service.py` and re-running the same two files → **`1 failed, 71 passed`**, the single failure being exactly `test_delete_category_detach_with_assignments_unrelated_integrity_failure_is_not_a_false_409` on `E AssertionError: restored-by-rollback assignments reported as category_in_use`. That is the FINAL review's reproduction, reproduced independently, and it proves the guard is non-vacuous and kills precisely the shipped defect — but it was observed *after* the edit, not before, and the record says so. `admin_service.py` was then restored from a byte-copy baseline and re-verified `sha256 3d393e846d8d0ca0c487870d3be0033ec13f91dd74468b0bedb50cccca998efe`; no suite run overlapped the mutation, and the baseline copy was deleted afterwards.

**Gates actually executed in this pass** (all in `/home/ezop/worktrees/3d-portal-e49-5-dev/apps/api` unless noted; output verbatim, not inferred):

| Command | Result |
|---|---|
| `.venv/bin/pytest -q tests/test_sot_admin_categories.py tests/test_sot_admin_model_categories.py` (GREEN, post-edit; re-run after the lint line-wrap) | `72 passed, 124 warnings in 10.51s` / `10.74s` |
| the same, with the repaired guard mutated back to `if remaining:` (post-hoc RED / non-vacuity probe) | `1 failed, 71 passed` — the new F-1 guard only |
| `--collect-only -q` on the two focused files | `72 tests collected` |
| the story's exact §14 non-regression witness set (10 modules) | `196 passed, 255 warnings in 27.73s` |
| `.venv/bin/pytest -q tests/test_audit.py tests/test_2fa_schema.py tests/test_sot_admin_models.py` | `51 passed, 56 warnings in 10.67s` |
| `.venv/bin/pytest -q` (full `apps/api` suite, re-run **after** the lint-driven line-wrap so the number matches the final tree) | **`1922 passed, 3 skipped, 2268 warnings in 426.34s (0:07:06)`** |
| `.venv/bin/ruff format --check .` | `281 files already formatted` |
| `.venv/bin/ruff check .` | `All checks passed!` (one `E501` in the new test was fixed by wrapping the call, then re-verified) |
| `git diff --check` (repo root) | empty, **exit 0** |
| `git status --porcelain -uall` | **still exactly 14 paths**, nothing staged, nothing committed |

The full-suite delta is exactly this pass's one new test: `1921 passed, 3 skipped` → `1922 passed, 3 skipped`. No pre-existing test changed status. **This pass DOES claim its own full-suite run:** it was executed on a quiescent tree with the mutation already restored and its baseline copy deleted, no scratch test file and no concurrent subagent.

**Production edit made (one file, inside this story's own new code).**

- `admin_service.py` — `delete_browse_category` only. `detached_ids: list[uuid.UUID] = []` is hoisted above the detach branch so it is in scope in the handler; the post-rollback re-derivation binds `remaining = _browse_category_assignments(session, category_id)` and raises `ValueError("category_in_use")` only when `{row.model_id for row in remaining}` is **not** a subset of `set(detached_ids)`. Children are still re-derived first, so the children-before-assignments precedence the N-4a sentinel pins is untouched. The docstring's safety-net paragraph gained one sentence stating the restored-rows caveat, and the handler's two comments were updated to match. **Behaviour is unchanged on every `detach=false` path** — `detached_ids` is empty there, so `remaining ⊄ ∅` for any row and the documented 409 fires exactly as before. No other production file changed: `browse_category_admin_router.py`, `admin_schemas.py`, `schemas.py`, `audit.py` and `router.py` are byte-unchanged by this pass; no route-layer edit, no new status code, no response-shape change.

**Test edit made (one file).**

- `tests/test_sot_admin_categories.py` — 56 → **57** tests (+1: `test_delete_category_detach_with_assignments_unrelated_integrity_failure_is_not_a_false_409`, placed with the other ghost-actor guards and re-using the existing `_ghost_actor_outcome` and `_seed_category_with_assignment` helpers). It asserts the outcome is not `http-409`, that it equals the sibling create route's answer to the same cause, that the category and its one assignment both survive the rollback, that there is **zero** `browse_category.delete` audit residue, that `detach=false` on the same category still returns the real `409`, and that a real-admin `detach=true` control returns **204** with `detached_model_ids` intact in the audit `before`. No existing test was weakened, narrowed, renamed or removed; the N-1 guard is untouched.

**Residual corner, named rather than hidden.** A racing INSERT that re-creates an assignment for a model this same request had just detached is indistinguishable, after rollback, from the rollback's own restoration of that row, so it re-raises the `IntegrityError` instead of answering `409 category_in_use`. Closing it would need row-level identity the ORM no longer has after the rollback. This is strictly narrower than the defect F-1 charged (which fired on *every* `detach=true`-with-assignments integrity failure), it is on the same near-zero-reachability path, and it loses no data — the rollback stays total and no audit row survives.

**Explicitly NOT done.** No `deferred-work.md` edit — F-2 and F-3 (and the seven earlier re-review defers) remain recorded in the findings sections and the durable reports; the ledger append is still the controller-owned bounded write F-3 itself asks for at T8 time, stated here rather than silently skipped. No T8 box ticked. No `check-all.sh`, no determinism triple, no frozen manifest, no `bmad-code-review`, no `laura-aider-review-diff`, no commit/ff-merge/push/deploy. No planning-artifact change, no `epic-49` change, no migration, seed or live-DB action. No unrelated file touched.

**Disclosed limitation — §8's Dev Notes code sketches remain superseded and were again deliberately left alone**, for the third time and for the same reason: they sit outside the sections a `bmad-dev-story` pass may modify. §8.4's delete tail now differs from the shipped code by both safety nets *and* this repair. Flagged, not silently rewritten.

### Completion Notes List

Implemented exactly the §4 AC set and the §5 T0–T7 task list. Four `current_admin`-only routes, all `tags=["sot-admin-governance"]`, all with non-empty summary + description. Initiative 26 invariants held: Model ↔ BrowseCategory stays M:N; zero categories remains valid and public (AC-27, no read/visibility path touched); Category stays independent of TagGroup; the depth-2 ceiling and self-cycle rejection are service-layer only (no DDL, no migration, no CHECK constraint); the replace-set is explicit auditable LWW with no invented concurrency claim; child-bearing deletion returns its own `409 category_has_children` checked first and unconditionally, which `detach=true` never bypasses; the replace-set validates the complete payload before any mutation, allows the empty set, rejects duplicates with 400, and 404s on unknown category or unknown/soft-deleted model.

**Two story-prediction misses were found and repaired. Both are disclosed here rather than absorbed silently; both mean §7's file list and AC-36's "byte-unmodified" claim were incomplete as written.**

- **D-1 — `test_openapi_agent_surface.py::test_retired_taxonomy_admin_crud_route_is_gone` had to be re-pointed (an existing test BODY changed, which §7 predicted would not happen).** That Story 47.5 guard asserts `PATCH /api/admin/categories/{id}` returns 404/405, i.e. that the retired Initiative 25 admin taxonomy CRUD surface is gone. Story 49.5 re-uses that exact path for the ratified new Initiative 26 governance write (Decision AY, AC-8, §7, §8.1), so the two cannot both hold; at T3 GREEN it failed with `AssertionError: expected 404/405 on retired admin CRUD route, got 401`. Resolved by the **already-ratified precedent for exactly this collision**: Story 49.3 hit the same thing on the read side under gate G26-ROUTE-PATH (recorded closed in §2) and re-pointed the sibling guard `test_retired_taxonomy_read_route_is_gone` at the retired *contract* rather than the retired *path*. This pass applied the identical treatment: the guard now authenticates as a real admin `User` row (the write needs one — `audit_log.actor_user_id` FKs `user.id` under `PRAGMA foreign_keys=ON`, unlike the read-side guard's bare minted JWT), performs the new admin write, and asserts the response is the flat Initiative 26 browse contract (exact ten-key `BrowseCategoryAdminRead` set, scalar `parent_id`, no `children`/`subcategories`) — coverage kept and re-pointed, never deleted or weakened. The runtime-assembled retired-path literal discipline (Story 47.5 §11 residual-symbol grep) is preserved. The alternative — moving the route off the ratified path — was rejected as a far larger unratified deviation.
- **D-2 — two `KNOWN_ENTITY_TYPES` drift guards had to be updated, which AC-34 predicted would not be necessary.** AC-34 claims "every pre-existing audit-touching test stays green"; the full suite proved otherwise. `tests/test_audit.py::test_known_entity_types_covers_all_call_site_resources` pins the exact expected set and `tests/test_2fa_schema.py::test_known_entity_types_count_reflects_taxonomy_retirement` pins its exact length — neither file appears in §7. Both are pure drift guards with an established convention of being updated by every story that adds an entity type (the count comment itself enumerates Story 6.1 → 7.1 → 33.2 → 42.4 → 47.5); Story 42.4 did exactly this for `tag_group`. Updated minimally and in the same in-file style: `"browse_category"` added to the expected set, the count `15 → 16` with a Story 49.5 clause appended to the existing comment chain. No assertion was weakened or removed.

Explicitly **not** done, per §3 / §10 / §8.6: no DDL, migration, seed change or seed re-run; no frontend; no category inference from tags; no boot auto-seed; no live DB, network, Docker or deploy; no cascading child-delete or auto-reparent; no truncation/cap on the audit payload; no `revision`/ETag/`If-Match` precondition; no retroactive duplicate-id guard on `replace_model_tags` (the gap is reported in §6 F-9, deliberately left in place); no edit to `docs/operations.md`'s stale "Admin write" enumeration (§6 F-11); no change to `GET /api/categories[/{slug}]`, `GET /api/models?category=` or `ModelDetail.categories`; no `_PUBLIC_ROUTES` or auth-dependency edit; no `agent-write` tag anywhere in this story's surface; no planning-artifact edit; no Epic 50 work.

Two implementation details worth the reviewer's attention. First, `BrowseCategoryAdminRead` is built field-by-field in the router rather than via `model_validate(cat)`, because `model_count` is an aggregate and not an ORM attribute — the same reason the read side hand-builds `BrowseCategoryRead`; `browse_category_model_count` is a scoped single-category query with the read side's live-model semantics (`Model.deleted_at IS NULL`), deliberately not a reuse of the whole-table GROUP BY. **Wording corrected by the 2026-07-28 repair pass (review finding #2):** "deliberately not a reuse" is accurate about the code but was an incomplete account of the obligation, because Decision AY asks for `model_count` to be *"computed by the same shared-helper approach … so the two can never disagree"*. The divergence from that letter is deliberate and stands — one definition is a whole-table `GROUP BY` feeding the public list, the other needs exactly one category on a write response — but it is no longer merely asserted. Decision AY's actual **requirement**, that the two can never disagree, is now enforced mechanically by `test_browse_category_model_count_agrees_with_read_side_for_every_category`, which compares both definitions across every category in the database on a fixture mixing non-zero, zero and soft-deleted-only counts. Decision AY's semantics are unchanged; only the proof obligation moved from prose to a test. Second, §18 **Q-3** (should `BrowseCategoryAdminRead` live in `schemas.py` or `admin_schemas.py`?) was carried into this pass unresolved; it is implemented in `sot/schemas.py` exactly as §7/T1.3 specify, following the existing file-role convention (`schemas.py` = response shapes, `admin_schemas.py` = request shapes). The observation stands but no deviation was taken.

### File List

**Modified — tracked (10):**

- `_bmad-output/implementation-artifacts/49-5-admin-category-governance.md` — workflow record (this artifact: status, task checkboxes, §15, §19).
- `_bmad-output/implementation-artifacts/sprint-status.yaml` — workflow record (story status transitions).
- `apps/api/app/core/audit.py` — `"browse_category"` added to `KNOWN_ENTITY_TYPES` + its doc-comment block (AC-34). No existing entry changed.
- `apps/api/app/modules/sot/admin_schemas.py` — ADD `BrowseCategoryCreate`, `BrowseCategoryPatch`, `ModelCategoriesReplace`.
- `apps/api/app/modules/sot/admin_service.py` — ADD `_browse_category_snapshot`, `browse_category_model_count`, `_browse_category_children`, `_validate_parent_is_root`, `create_browse_category`, `update_browse_category`, `delete_browse_category`, `replace_model_categories` + three import additions. No existing function body changed.
- `apps/api/app/modules/sot/schemas.py` — ADD `BrowseCategoryAdminRead(BrowseCategoryRead)`.
- `apps/api/app/router.py` — ADD one import + one `include_router` line, immediately after `sot_tag_group_admin_router`.
- `apps/api/tests/test_openapi_agent_surface.py` — extend `_GOVERNANCE_ROUTES` with the four new tuples (additive, as predicted) **and** re-point `test_retired_taxonomy_admin_crud_route_is_gone` + add the `User`/`UserRole` import it needs (Completion Note D-1 — **not** predicted by §7).
- `apps/api/tests/test_audit.py` — add `"browse_category"` to the pinned expected set (Completion Note D-2 — not predicted by §7).
- `apps/api/tests/test_2fa_schema.py` — `KNOWN_ENTITY_TYPES` count `15 → 16` + comment clause (Completion Note D-2 — not predicted by §7).

**Added — untracked (3):**

- `apps/api/app/modules/sot/browse_category_admin_router.py` — the four new routes.
- `apps/api/tests/test_sot_admin_categories.py` — 45 tests (AC-1 … AC-24, AC-34, AC-35, AC-37 category-scoped).
- `apps/api/tests/test_sot_admin_model_categories.py` — 10 tests (AC-25 … AC-33).

**Untracked local toolchain, deliberately NOT part of the story tree:** `apps/api/.venv/` (gitignored; bootstrapped in-worktree with `uv sync --frozen --all-extras`; zero git diff).

**Total: 13 product/workflow paths**  *(superseded by the post-review repair pass — see the re-derived 14-path inventory immediately below)* = 8 production/test files predicted by §7 (6 production + 2 new test files) + 1 predicted additive test extension + **3 unpredicted test-guard repairs** (D-1's body change counted inside the same `test_openapi_agent_surface.py` entry, plus `test_audit.py` and `test_2fa_schema.py`) + 2 workflow records. Nothing is staged and nothing is committed.

#### File List after the post-review repair pass (re-derived, 2026-07-28)

`git status --porcelain -uall` re-run at the end of the repair pass. **14 paths — the 13 above plus `deferred-work.md`.** Nothing is staged and nothing is committed.

**Modified — tracked (11):**

- `_bmad-output/implementation-artifacts/49-5-admin-category-governance.md` — this artifact (Status, Review Findings checkboxes, this Dev Agent Record subsection, File List, Change Log).
- `_bmad-output/implementation-artifacts/sprint-status.yaml` — story status transition + comment; `epic-49` deliberately unchanged.
- **`_bmad-output/implementation-artifacts/deferred-work.md` — NEW PATH for this story.** One appended heading, `## Deferred from: code review of 49-5-admin-category-governance (2026-07-28)`, carrying the review's eight `defer` findings. The sixteen pre-existing headings are byte-unchanged; nothing was rewritten or reordered. This closes the CR pass's disclosed deviation #2 (that pass was authorised to write only the story artifact and sprint-status).
- `apps/api/app/modules/sot/admin_service.py` — repair pass: ADD `_browse_category_assignments` + `_raise_browse_category_flush_error`; `IntegrityError` safety net on the delete commit with post-rollback children-first re-derivation; both flush handlers routed through the new helper; `NoReturn` import; three docstring corrections.
- `apps/api/tests/test_openapi_agent_surface.py` — repair pass: guard renamed to `test_admin_category_write_carries_no_recursive_taxonomy_contract` (finding #11), docstring extended; assertions unchanged. Earlier dev-pass changes unchanged.
- `apps/api/app/core/audit.py`, `apps/api/app/modules/sot/admin_schemas.py`, `apps/api/app/modules/sot/schemas.py`, `apps/api/app/router.py`, `apps/api/tests/test_audit.py`, `apps/api/tests/test_2fa_schema.py` — **byte-unchanged by the repair pass**; they carry only the original dev-pass edits described above.

**Added — untracked (3):**

- `apps/api/app/modules/sot/browse_category_admin_router.py` — **byte-unchanged by the repair pass.** Both repairs surface through the router's existing `LookupError` → 404 / `ValueError` → 409/422 mapping; no route-layer edit and no response-shape change were needed.
- `apps/api/tests/test_sot_admin_categories.py` — 45 → **54** tests (+9: two delete-race probes, two vanished-parent probes, one duplicate-slug non-regression, the finding-#1 lifecycle guard, two `model_count` guards, and the third `detach=false` parametrisation of the unknown-category 404). Plus the 8-key create-snapshot equality folded into the existing shape test.
- `apps/api/tests/test_sot_admin_model_categories.py` — 10 → **11** tests (+1: the `(position, slug)` tie-break).

**Byte-unchanged and re-verified green by this pass** (`git diff --name-only` returns nothing for them): every path on §7's asserted-unchanged list, and additionally `apps/api/app/modules/sot/service.py` — the read-side `_browse_category_model_counts` is only *read from a test*, never modified, so AC-36 holds.

**Untracked local toolchain, still deliberately out of the story tree:** `apps/api/.venv/` (gitignored, zero git diff).

#### File List after the post-RE-REVIEW repair pass (re-derived, 2026-07-28)

`git status --porcelain -uall` re-run at the end of this pass. **Still exactly 14 paths — no path added, removed or renamed.** Nothing is staged and nothing is committed.

**Written by this pass (5 of the 14) — the four below plus `sprint-status.yaml`, which changes only for the status transition:**

- `_bmad-output/implementation-artifacts/49-5-admin-category-governance.md` — this artifact (Status, the four Re-review Findings checkboxes, this Dev Agent Record subsection, this File List addendum, Change Log).
- `apps/api/app/modules/sot/admin_service.py` — N-1: the delete safety net re-derives both RESTRICT FKs and re-raises when neither is violated. N-2: ADD `_validate_browse_category_ids` + `_raise_model_categories_commit_error`; `replace_model_categories`'s mutate/commit block wrapped in `except IntegrityError`; docstring extended.
- `apps/api/tests/test_sot_admin_categories.py` — 54 → **56** tests (+2: the N-1 ghost-actor guard and the N-4a children-before-assignments sentinel), plus the N-3 audit-`after` assertions folded into the existing `test_patch_category_partial_update_200`, plus one import (`sqlalchemy.exc.IntegrityError`).
- `apps/api/tests/test_sot_admin_model_categories.py` — 11 → **15** tests (+4: the two N-2 race probes, the N-2 duplicate-payload non-regression, and the N-4b validate-before-delete ordering sentinel), plus two imports (`sqlalchemy.event`, `app.modules.sot.admin_service`) and the shared `_neutralised_first_call` helper.

**Byte-unchanged by this pass (9 of the 14):** `deferred-work.md`, `apps/api/app/core/audit.py`, `admin_schemas.py`, `sot/schemas.py`, `app/router.py`, `tests/test_2fa_schema.py`, `tests/test_audit.py`, `tests/test_openapi_agent_surface.py` and `apps/api/app/modules/sot/browse_category_admin_router.py` carry only their earlier edits. Every path on §7's asserted-byte-unchanged list is still absent from `git diff --name-only`, and `apps/api/app/modules/sot/service.py` remains read-from-a-test-only.

#### File List after the post-FINAL-review repair pass (re-derived, 2026-07-28)

`git status --porcelain -uall` re-run at the end of this pass. **Still exactly 14 paths — no path added, removed or renamed.** Nothing is staged and nothing is committed.

**Written by this pass (4 of the 14):**

- `_bmad-output/implementation-artifacts/49-5-admin-category-governance.md` — this artifact (Status `in-progress` → `review`, the Final-review Findings F-1 checkbox + repair note + repair-status paragraph, the §15 "Post-FINAL-review repair pass" subsection, this File List addendum, §19 Change Log).
- `_bmad-output/implementation-artifacts/sprint-status.yaml` — the story key's status transition and `last_updated` only. **`epic-49: in-progress` preserved unchanged.**
- `apps/api/app/modules/sot/admin_service.py` — F-1: `delete_browse_category` only — `detached_ids` hoisted, the post-rollback assignment re-derivation now subset-guarded against it, docstring + two comments updated.
- `apps/api/tests/test_sot_admin_categories.py` — 56 → **57** tests (+1: the F-1 detach-with-assignments ghost-actor guard). No import added; the existing `_ghost_actor_outcome`, `_seed_category_with_assignment`, `_category_audits`, `_assignment_count` and `_category_exists` helpers are re-used.

**Byte-unchanged by this pass (10 of the 14):** `deferred-work.md`, `apps/api/app/core/audit.py`, `admin_schemas.py`, `sot/schemas.py`, `app/router.py`, `browse_category_admin_router.py`, `tests/test_2fa_schema.py`, `tests/test_audit.py`, `tests/test_openapi_agent_surface.py` and `tests/test_sot_admin_model_categories.py` (still **15** tests) carry only their earlier edits.

## 16. Validation Record

**Verdict: PASS** — native `bmad-create-story` **action=validate** (menu VS), run 2026-07-28 by a fresh, independent Claude Sonnet 5 session in the same isolated worktree, per `checklist.md`'s "When Running in Fresh Context" protocol (loaded independently of, and with no memory of, the Create pass). **NO human review of any kind** — no Ezop signature, no Ezop review, no Laura review is recorded, implied, or claimable from this document.

**Routing.** `AGENTS.md`, `CLAUDE.md`, `_bmad-output/project-context.md`, and the Laura Agent Rulebook were read first. `_bmad/_config/manifest.yaml` (BMad 6.10.0), `bmad-help.csv` row 27 (`bmad-create-story` / Validate Story / VS / preceded-by `bmad-create-story:create`, followed-by `bmad-dev-story`, required=false), and `skill-manifest.csv` row 40 confirmed `bmad-create-story:validate` as the canonical post-Create action. `.claude/skills/bmad-create-story/` was present in this worktree (repaired by the Create pass, §17.6) and was used as-is — no second repair needed. `SKILL.md` contains only the Create-action workflow (steps 1–6); the Validate action's actual content is `checklist.md`, whose own "When Running in Fresh Context" section directs a fresh-context reviewer to load the story file and source documents directly and systematically re-derive rather than trust the Create pass — exactly the protocol this session followed. `resolve_customization.py` was not re-run (no team/user override files exist for this skill per the Create pass's own resolution, unchanged at this baseline).

**Re-derivation performed (independent of the Create pass's prose).** `epics.md:4375-4496` (Initiative 26 overview, FR matrix, Epic 49 + Story 49.5 sketch) and `architecture.md:3285-3386` (Decisions AX, AY, AZ) were read in full, not selectively. Source code was read directly at HEAD `0df663e`: `_entities.py:117-183` (`ModelTag`, `BrowseCategory`, `ModelBrowseCategory` — confirms F-2's RESTRICT/CASCADE claims verbatim, including `parent_id`'s `ondelete="RESTRICT"` at `_entities.py:158-161`); `app/core/audit.py:1-69` (`KNOWN_ENTITY_TYPES` — confirms `"browse_category"` is genuinely absent today, AC-34 is additive); `admin_service.py:807-859` (`replace_model_tags` — confirms F-9(b)'s no-duplicate-guard claim: the only guard is a docstring promise, not code) and `:1150,1192,1247` (`create_tag_group`/`update_tag_group`/`delete_tag_group` — confirms F-1's "real template" claim; the retired `create_category`/`admin_service.py:1150-1306` citation F-1 warns against is indeed absent, `grep` for `def create_category\|class Category\b` returns zero matches); `admin_router.py:98-120,147,571-598` (`_current_admin_or_agent_dep`/`_current_principal`, `agent-write` tags — confirms F-9(a)'s tags-endpoint auth claim); `tag_group_admin_router.py:1-40` (confirms F-7's `current_admin`/`_current_admin_dep` claim, and supplies the admin-curation-vs-agent-ingestion doctrine used to resolve Q-1, below); `test_openapi_agent_surface.py:61,257-269` (`TARGET_ROUTER_TAGS`, `_GOVERNANCE_ROUTES` — confirms F-5's exact set contents); `test_route_enforcement_gate.py:37-42` (`_AUTH_DEP_NAMES` — confirms F-7's exact set contents); `app/core/auth/dependencies.py:55-76` (`current_admin = Depends(_current_admin_dep)`); `app/core/auth/csrf.py:6-17` (`UNSAFE_METHODS`, prefix-based `csrf_guard` — confirms F-8). No test was run, no throwaway database was built, no live system was touched — this is source re-derivation only, consistent with a story that has not yet entered `bmad-dev-story`.

**Priority arbitration (task-mandated, not merely repeated from the Create pass):**

- **A — Child-category delete conflict (§6 F-4, AC-21/AC-22, §18 Q-2): RATIFIED, no amendment to the substantive contract.** The `409 category_has_children` unconditional-and-`detach`-independent design is confirmed the correct minimal, planning-compatible resolution — see Q-2 above for the full re-derivation. No planning artifact contradicts it; the RESTRICT FK it defends against is real; the rejected cascading alternative is correctly recorded, not adopted.
- **B — Admin-only replace-set rationale (§6 F-9, §18 Q-1): AMENDED for truthfulness, not rejected.** The Create pass's framing — "no ratified rationale beyond the task's own explicit instruction" — understated the evidence. `current_admin`-only is explicitly ratified by Decision AY (`architecture.md:3334-3341`, which places this exact route under the heading naming `current_admin`) and is not a novel asymmetry: it is the same admin-curation/agent-ingestion split `tag_group_admin_router.py` already applies to facet governance (FR25-ADMIN-1/Decision AW, D-ADMINONLY-1), verified verbatim in that router's own docstring. §6 F-9's addendum, §8.1's router docstring, and §18 Q-1 are corrected in place to state this truthfully. This was a documentation-accuracy defect in the Create pass's self-characterization, not a defect in the underlying design choice, which was correct all along.

**Other checks.** Traceability (§12) cross-checked against `epics.md`'s FR matrix (`4397-4411`) — no mismatch found. AC set (§4, AC-1…AC-37) re-read for contradictions, missing cases, and impossible test claims — none found; the depth-ceiling reparent logic (§8.3) was independently traced (a category with children cannot itself have a non-null `parent_id` under the depth-2 ceiling already enforced by AC-7/AC-14, so AC-15's check has no false-positive no-op case). §7's predicted file list and §11's branch/commit atomicity were read for internal consistency with §5's task list — consistent. No blocking finding survived re-derivation.

**Scope of this pass.** Story-artifact and `sprint-status.yaml` amendment only, per this pass's write policy. No code, test, config, or migration file touched; no live database, network, commit, push, merge, deploy, or human review of any kind. **`G26-DEVGO` remains NOT granted by this pass** — validation clears the story to `ready-for-dev`, which is a prerequisite for, not equivalent to, implementation authorization; the controller must still separately confirm this specific story under the standing initiative authorization before `bmad-dev-story` runs.

## 17. Disclosed deviations from the base workflow

1. **The Create pass stopped at `ready-for-validation`, and status advanced no further at that time — by explicit task scope, not by the base workflow's own logic.** *(Superseded 2026-07-28 by the separate Validate pass recorded in §16, which returned PASS and advanced the status to `ready-for-dev` per this same repo convention. The text below is preserved as the Create pass's honest record of its own scope, not as a current status claim.)* The base `bmad-create-story` workflow (`SKILL.md` step 5/6, `template.md:3`) literally sets `Status: ready-for-dev` at the end of a create pass. **This repo's established, repeatedly-disclosed convention** (Stories 49.1/49.2/49.3 all record this explicitly — verified this session by reading the committed initial-create text of Story 49.3, `git show 2d4fd75:_bmad-output/implementation-artifacts/49-3-category-read-api-and-model-scope.md:512` — *"The create pass ended at `ready-for-validation`, not the base workflow's `ready-for-dev`... 49.1/49.2 both used `ready-for-validation` at that stage"*) is that a Create pass alone lands at `ready-for-validation`, and only a subsequent, independent Validate pass with a `PASS` verdict advances it to `ready-for-dev`. This session follows that established repo convention rather than the base skill's literal instruction, and additionally — unlike 49.1/49.2/49.3, which bundled Create+Validate into one session/commit — this session runs **Create only**, per this pass's explicit operating scope ("Perform exactly ONE native BMAD lifecycle transition... Do not validate"). Both the status value and the stop-after-create scoping are disclosed, not silently applied.
2. **`epic-49` stays `in-progress`.** It was flipped at Story 49.1's dev-story pass; this story is the fifth in the epic, so the base workflow's first-story auto-promote does not apply and no change is warranted.
3. **The double-RESTRICT-on-delete resolution (§6 F-4, AC-21/AC-22) is a create-pass judgment call, not a transcription of any planning artifact.** No source document (`sprint-status.yaml`, `architecture.md`, `epics.md`) addresses the case of a category with children being deleted; this document adopts "children are an unconditional, `detach`-independent 409" as the minimal-diff resolution and states the reasoning and the rejected alternative explicitly (F-4) rather than picking silently or leaving it unspecified for the dev pass to improvise.
4. **Subagent use was partial, not full, relative to the base workflow's recommendation.** `SKILL.md`'s "On Activation" preamble recommends subagents/subprocesses for "thoroughly analyz[ing] different artifacts simultaneously." This session dispatched **one** `Explore` research subagent for the exhaustive current-code grounding in §6 (tag_group_admin_router.py, audit.py, admin_service.py, admin_schemas.py, test precedents — its full report is the basis for F-1 through F-11), then continued with direct in-session reads for verbatim code citation (§8's code blocks) and for the epics.md/architecture.md/sprint-status.yaml planning-artifact grounding, rather than fanning out multiple parallel subagents for every artifact category as the base workflow's ideal describes. This reflects the session's serial, single-orchestrator operating mode for this task, not a decision that parallel subagents were unnecessary.
5. **`checklist.md`'s "fresh independent context, competitive review" protocol was applied inline during authoring, not as a separate fresh-context pass.** `SKILL.md` step 6 directs the create pass to "Validate the newly created story file against `./checklist.md` and apply any required fixes before finalizing." `checklist.md` itself is written as a from-scratch, independent-context adversarial re-derivation — structurally the same exercise as the separate `bmad-create-story:validate` action this pass is explicitly scoped **not** to run. This session resolved that overlap by applying the checklist's disaster-prevention categories (reinvention, wrong file locations, regression, vague ACs, scope creep) as an authoring discipline throughout §4/§6/§8/§9 — every AC in this document was written against a real, cited code fact rather than an assumption, and every open judgment call is flagged in §6/§18 rather than silently resolved — plus one direct post-write arithmetic self-check (§7's file-count line, corrected in place before finalizing). It did **not** spin up a genuinely fresh, independent context to adversarially re-derive every citation the way the separate Validate action would. That full independent pass remains owed to the next `bmad-create-story:validate` session, per §16/§18.
6. **This worktree's `.claude/skills/` directory was missing at session start and was repaired before any skill invocation, rather than proceeding hand-authored.** `.claude/skills/` is gitignored (`.gitignore:143`) and regenerated by the `bmad-method` installer (per the durable memory note on this repo's install/surface drift); `git worktree add` — which created this isolated worktree — only materializes git-tracked content, so the 90 native skill directories (including `bmad-help` and `bmad-create-story` themselves) were simply absent here at session start, even though `_bmad/_config/manifest.yaml` (the git-tracked config surface) correctly reported BMad 6.10.0 installed. Rather than hand-authoring the story around the missing native skill (explicitly forbidden by this pass's operating instructions) or running the full non-interactive installer (which would have touched the git-tracked `_bmad/_config/` surface, violating the "no code/config change" scope of this pass), this session copied `.claude/skills/` verbatim from the sibling worktree `/home/ezop/repos/3d-portal` — confirmed to be the exact same repo at the exact same BMad version (`manifest.yaml` byte-identical, `diff` confirmed) — into this worktree. This is a purely local, gitignored filesystem repair with **zero** git diff (verified: `git status --short` unaffected by the copy) and is the smallest change that restores genuine native-skill invocability without touching any tracked file. Recorded here in full rather than silently worked around.

## 18. Open questions carried to Validate (no silent resolution claimed)

Three design decisions in this Create pass were explicitly flagged, not silently asserted as obviously correct. The Validate pass (2026-07-28) re-derived each against source: Q-1 and Q-2 are now closed (see below); Q-3 is a minor, non-blocking file-placement question carried forward for the dev pass.

- **Q-1 — CLOSED by Validate (2026-07-28).** Is `current_admin`-only (no `agent-write`) the right auth tier for `PUT /api/admin/models/{id}/categories`? **Yes — ratified, not merely task-instructed.** `architecture.md:3334-3341` (Decision AY) explicitly places this exact route under `current_admin` alongside the other three; the split follows the same admin-curation/agent-ingestion doctrine `tag_group_admin_router.py` already codifies for facet governance (FR25-ADMIN-1/Decision AW, D-ADMINONLY-1). See §6 F-9 addendum for the full re-derivation. Residual, non-blocking observation: `architecture.md` does not itself spell out prose reasoning for *why* the replace-set operation specifically (vs. the CRUD routes) falls on the admin-curation side — a documentation-completeness note for a future architecture-hygiene pass, not a gap in this story's authorization.
- **Q-2 — RATIFIED by Validate (2026-07-28), no override.** Is "children block delete unconditionally, `detach=true` never touches children" (§6 F-4) the right shape, versus an alternative where `detach=true` also orphans children to `parent_id=null`? **Confirmed as the correct minimal, planning-compatible contract.** Re-derived independently: `BrowseCategory.parent_id`'s `ondelete="RESTRICT"` is verified at `_entities.py:158-161`; no planning artifact (`epics.md:4491-4493`, `architecture.md:3337`) addresses the children-present case at all — both speak only of model-assignment conflicts — so this is genuinely an unfilled gap the Create pass had to resolve, not a contradiction of ratified text. The adopted shape is the more conservative of the two live options, does not silently redefine `detach=true`'s documented (model-only) scope, requires no new query parameter or cascading-delete semantics nothing in Decision AX/AY asked for, and is consistent with Decision AX's "MVP writes no child categories at all" framing (the path is rare by construction, but must not 500 when hit). The rejected alternative (auto-orphan children on `detach=true`) remains recorded in §6 F-4 for a future reader; it is not adopted.
- **Q-3 — Should the admin-write response schema (`BrowseCategoryAdminRead`) live in `sot/schemas.py` (response DTOs) as this document places it, or in `admin_schemas.py` alongside the request DTOs it's paired with?** This document follows the existing file-role convention (`schemas.py` = response/read shapes including `TagGroupSummary`; `admin_schemas.py` = request shapes including `TagGroupCreate`/`Patch`) exactly, but `BrowseCategoryAdminRead` is genuinely admin-specific in a way `BrowseCategorySummary`/`BrowseCategoryRead` are not — worth a second look at Validate.

## 19. Change Log

| Date | Pass | Result |
|---|---|---|
| 2026-07-28 | `bmad-create-story` (action=create, menu CS), Claude Sonnet 5, native BMAD | Story created at baseline `0df663e`; status `ready-for-validation`. `.claude/skills/` repair disclosed (§17.6). No human review; no Ezop or Laura sign-off recorded or implied. No Validate pass run this session (explicit task scope) — §16/§17.1/§18 record the resulting open items honestly rather than presenting the document as validated. |
| 2026-07-28 | `bmad-dev-story` (Dev Story, menu DS), Claude Opus 5 (1M) implementation author, native BMAD | **Implemented.** Status `ready-for-dev` → `in-progress` → `review`; `epic-49` unchanged at `in-progress`. Bounded RED → GREEN per slice with executed evidence and exact counts in §15 (T1 9 failed → 9 passed; T2 8 failed → 54 passed; T3 17 failed → 93 passed; T4 14 failed → 83 passed; T5 9 failed → 93 passed). Focused Story 49.5 coverage **55 tests** (45 + 10) all passing; non-regression witness set **196 passed**; full `apps/api` suite **1905 passed, 3 skipped**; `ruff format --check` 281 files formatted; `ruff check` all passed; `git diff --check` clean. Two story-prediction misses found, repaired and disclosed as §15 D-1 (the Story 47.5 retired-admin-path guard re-pointed per the ratified 49.3 / G26-ROUTE-PATH precedent) and D-2 (two `KNOWN_ENTITY_TYPES` drift guards updated, as Story 42.4 did for `tag_group`). Six pass-by-construction guards labelled honestly, never presented as REDs. NO human review of any kind; no independent `bmad-code-review` and no Aider run by the implementer. No commit, push, merge, deploy, migration, seed, live DB, network, or planning-artifact change. T8 controller-owned closeout left UNCHECKED. |
| 2026-07-28 | `bmad-create-story` (action=validate, menu VS), fresh independent Claude Sonnet 5 session, native BMAD | **PASS.** Independent re-derivation of every §6 finding against source at HEAD `0df663e` (see §16). Priority A (child-delete conflict, §18 Q-2) ratified unchanged. Priority B (admin-only replace-set rationale, §18 Q-1) amended for truthfulness — §6 F-9 addendum, §8.1 docstring, §18 Q-1 corrected to cite the ratified Decision AY basis instead of framing the choice as merely task-mandated. Status → `ready-for-dev`; `epic-49` stays `in-progress`. No human review of any kind. `G26-DEVGO` still NOT granted — controller confirmation of this specific story remains owed before `bmad-dev-story`. No code/test/config/migration/live change; no commit, push, merge, or deploy. |
| 2026-07-28 | `bmad-code-review` (Code Review, menu CR), fresh independent Claude Opus 5 (1M) reviewer session, native BMAD | **REQUEST_CHANGES.** All three adversarial layers ran (Blind Hunter, Edge Case Hunter, Acceptance Auditor; `failed_layers` empty) over the complete 13-path tracked+untracked diff at HEAD `b1a46a2`. Triage: **1 decision-needed, 6 patch, 8 defer, 6 dismissed** (see the Review Findings subsection and §20). The implementer's §15 numbers were re-executed rather than trusted and reproduced exactly: full suite `1905 passed, 3 skipped in 424.05s`, §15 T6.2 witness `196 passed`, focused coverage `55` collected, `281 files already formatted`, `All checks passed!`, `git diff --check` clean. No AC violated and no binding constraint breached; both disclosed prediction deltas arbitrated LEGITIMATE (A: retirement guard re-pointed, not weakened — the structural no-resurrection invariant lives in the untouched `test_no_category_schemas_in_components` / `test_no_category_properties_on_model_schemas`; B: the two drift-guard edits are minimal and truthful, exact set equality preserved), with AC-34's "every pre-existing audit-touching test stays green" sub-claim recorded as falsified. Status `review` → `in-progress` per the skill's step-04 §6 rule (patch findings left as action items); `epic-49` unchanged at `in-progress`. Nothing here is a security, authorization, data-corruption or merge-blocking safety defect. NO human review of any kind; no Aider and no Codex run — the independent external review is still owed, as is the T8 controller closeout. No fix applied, no product or test file touched, no commit, push, merge, deploy, migration, seed, live DB or network action. Durable report: `/home/ezop/repos/3d-portal/.hermes/run-logs/e49-5-native-review-20260728.md`. |

| 2026-07-28 | `bmad-dev-story` (Dev Story, menu DS) — **post-review repair pass**, fresh Claude Opus 5 (1M), native BMAD | **All 6 patch findings repaired, the 1 decision-needed finding arbitrated, 8 defer findings ledgered.** Addressed code review findings — **7 items resolved** (6 repaired + 1 arbitrated). Finding #1 arbitrated by **Laura/controller** as preservation-first under the standing Initiative 26 delegation (a controller product-default decision, **not** an Ezop signature and **not** a human review): all `model_browse_category` rows keep blocking an ordinary DELETE with 409, `model_count` stays the Decision-AY non-deleted count and may truthfully read 0, `detach=true` stays the explicit destructive opt-in; no implicit detach, no `deleted_at` filter on the gate, no response-shape change. Findings #3 and #4 are the only logic-bearing repairs and both ran a genuine RED → GREEN (`4 failed, 61 passed` → `65 passed`); #1/#2/#12/#13 are coverage guards over already-correct behaviour, labelled as such and proven non-vacuous by four mutation probes with the mutated file restored byte-identically. Gates: focused **65 passed**; §14 witness set **196 passed**; audit/entity-type witnesses **51 passed**; full `apps/api` suite **1915 passed, 3 skipped in 420.10s** (was 1905 + exactly this pass's 10 new tests); `ruff format --check` 281 files formatted; `ruff check` all passed; `git diff --check` clean exit 0; `sprint-status.yaml` parses. Path inventory re-derived: **14 paths** (13 + `deferred-work.md`). Production diff confined to `admin_service.py`; router, schemas, audit registration and `app/router.py` byte-unchanged by this pass. Status `in-progress` → `review`; `epic-49` unchanged at `in-progress`. §8's Dev Notes sketches are now superseded and were deliberately NOT edited (outside the sections a DS pass may modify) — disclosed in §15. NO human review of any kind; no `bmad-code-review`, no Aider, no Codex, no `check-all.sh`, no determinism triple. No commit, stage, push, merge, deploy, migration, seed, live DB, network or planning-artifact change. T8 controller-owned closeout still UNCHECKED. |

| 2026-07-28 | `bmad-code-review` (Code Review, menu CR) — **independent RE-REVIEW**, fresh Claude Opus 5 session, native BMAD | **REQUEST_CHANGES.** All three adversarial layers ran (`failed_layers` empty) over the complete **14-path** tracked+untracked diff at HEAD `b1a46a2` — 11 tracked M (incl. `deferred-work.md`) + 3 untracked, reconciling the prior pass's now-stale 13-path count. Neither the prior review nor the repair session's `subtype=success` was trusted. **All six prior patch findings (#2/#3/#4/#11/#12/#13) independently CLOSED**, mutation-proven; the #1 controller preservation-first arbitration **UPHELD** against Decision AY's ratified "409 while assignments exist" text; both prediction deltas re-arbitrate LEGITIMATE and MINIMAL; the eight ledgered defers are truthful (+29/-0, 16 → 17 headings, 8 entries) and conceal nothing. No AC violated, no binding constraint breached, no security/authorization/data-corruption/merge-blocking defect. Re-executed evidence: focused `65 passed`, §14 witness set `196 passed`, audit witnesses `51 passed`, `ruff format --check` 281 files, `ruff check` all passed, `git diff --check` exit 0, `KNOWN_ENTITY_TYPES` len 16, OpenAPI dump of all four routes `sot-admin-governance` with no `agent-write`. **The full `apps/api` suite is deliberately NOT claimed by this pass** — two runs were contaminated by concurrent review-layer scratch files/mutations and a third was killed; it stays controller-owned/owed. Triage: **4 patch (all `low`), 7 defer, 3 dismissed, 1 inherited-recorded.** Verdict rests on the findings, not the runs: one contract-honesty regression introduced by the finding-#3 repair (a false `409 category_in_use` on any non-RESTRICT integrity failure), two incomplete halves of the repairs under review, one traceability gap on two AC ordering claims, plus the skill's own step-04 §6 rule. Status `review` → `in-progress`; `epic-49` unchanged at `in-progress`. `deferred-work.md` deliberately NOT edited (write scope = story artifact + sprint-status). NO human review of any kind; no Aider, no Codex, no `check-all.sh`, no determinism triple, no commit/push/merge/deploy/migration/seed/live action; no fix applied. Durable report: `/home/ezop/repos/3d-portal/.hermes/run-logs/e49-5-native-rereview-20260728.md`. |

| 2026-07-28 | `bmad-dev-story` (Dev Story, menu DS) — **post-RE-REVIEW repair pass**, fresh Claude Opus 5 (1M), native BMAD | **All 4 re-review patch findings repaired.** Addressed code-review findings — **4 items resolved**. N-1 (the contract-honesty regression the finding-#3 repair introduced) and N-2 (the missing commit-time safety net on `replace_model_categories`) are the logic-bearing repairs and both ran a genuine RED → GREEN (`4 failed, 67 passed` → `71 passed`); N-3 and both halves of N-4 are coverage/ordering guards over already-correct behaviour, labelled as such and proven non-vacuous by re-running the re-review's and the Acceptance Auditor's **own** three mutants (`1 failed, 75 passed`; `2 failed, 69 passed`; `1 failed, 14 passed`), with `admin_service.py` restored byte-identically (`sha256 eff4e930c2b90717…`). Gates: focused **71 passed** (56 + 15); §14 witness set **196 passed**; full `apps/api` suite **1921 passed, 3 skipped in 415.80s** — claimed by this pass, run on a quiescent tree with no concurrent subagent, scratch file or outstanding mutation (was 1915 + exactly this pass's 6 new tests, no pre-existing test changing status); `ruff format --check` 281 files formatted; `ruff check` all passed; `git diff --check` clean exit 0. Path inventory re-derived: **still 14 paths**, none added or removed; production diff confined to `admin_service.py`, with the router, schemas, audit registration and `app/router.py` byte-unchanged and no new status code or response-shape change. No test weakened, removed or narrowed. Status `in-progress` → `review`; `epic-49` unchanged at `in-progress`. The 7 re-review `defer` findings stay deferred and were **not** ledgered into `deferred-work.md` by this pass — disclosed in §15, not silently skipped. NO human review of any kind; no `bmad-code-review`, no Aider, no Codex, no `check-all.sh`, no determinism triple. No commit, stage, push, merge, deploy, migration, seed, live DB, network or planning-artifact change. T8 controller-owned closeout still UNCHECKED. |

| 2026-07-28 | `bmad-code-review` (Code Review, menu CR) — **independent FINAL review, third CR pass**, fresh Claude Opus 5 (1M) session, native BMAD | **REQUEST_CHANGES.** Full **14-path** tracked+untracked audit at HEAD `b1a46a2`. Neither the repair session's `subtype=success` / `rc=0` nor any numeric claim was trusted. **All four re-review patch findings (N-1…N-4) independently CONFIRMED CLOSED and the tests proven non-vacuous by this pass's own five mutation probes** — N-1 → `1 failed, 55 passed`; N-2 → `2 failed, 13 passed`; N-3 → `1 failed, 55 passed`; N-4a → `2 failed, 54 passed`; N-4b → `2 failed, 13 passed` — each restore asserted against `sha256 eff4e930c2b90717…` inside the probe harness. Gates re-executed and reproduced exactly: focused `71 passed` (56 + 15), §14 witness `196 passed`, audit/OpenAPI/CSRF witness `92 passed`, **full `apps/api` `1921 passed, 3 skipped in 416.17s` — CLAIMED by this pass** (certified-quiescent tree, no concurrent mutation or scratch file, only read-only work overlapping), `ruff format --check` 281 files, `ruff check` all passed, `git diff --check` exit 0, runtime OpenAPI showing all four routes `sot-admin-governance` with **zero `agent-write`**. No AC violated, no binding constraint breached; nothing found is a security, authorization, data-corruption, data-loss or merge-blocking defect. Triage: **1 patch (`low`), 2 defer, 2 minor-recorded, 3 dismissed.** The verdict rests on one finding, not on any run: the N-1 repair closes only the zero-assignment branch — with `detach=true` on a category that HAS assignments, the post-rollback re-derivation always sees the rows its own rollback restored and still answers a false `409 category_in_use` (reproduced with a passing real-admin 204 control). Not a failed repair — N-1's prescribed fix was implemented literally and correctly; the prescription was incomplete. Status `review` → `in-progress` per the skill's step-04 §6 rule (patch finding left as an action item); `epic-49` unchanged at `in-progress`. `deferred-work.md` deliberately NOT edited (write scope = this artifact + sprint-status) — the ledger gap is itself filed as a controller-owned defer. **Disclosed methodological difference: the three review layers were executed inline by the orchestrator, not as three blind subagents**, under a standing harness constraint against unrequested subagent dispatch — less information asymmetry than the two prior passes. NO human review of any kind; no Aider, no Codex, no `check-all.sh`, no determinism triple, no commit/stage/push/merge/deploy/migration/seed/live action; no fix applied. Durable report: `.hermes/run-logs/e49-5-native-final-review-20260728.md`. |
| 2026-07-28 | `bmad-dev-story` (Dev Story, menu DS) — **post-FINAL-review repair pass**, Claude Opus 5 (1M), native BMAD | **The 1 remaining patch finding F-1 repaired.** Controller disposition: repair, not the defensible recorded deferral the FINAL review also offered — the ~4-line closure so E49.5 proceeds toward closeout with the delete contract honest. `delete_browse_category` now hoists `detached_ids` above the detach branch and, after `session.rollback()`, raises `category_in_use` only when the post-rollback assignment set is **not** a subset of the rows this request had already accounted for; children are still re-derived first, and every `detach=false` path is behaviourally identical (`detached_ids` is empty there). **RED established by mutation, not by test-first ordering, and labelled as such:** fix and guard were written together (`72 passed`), then the guard was collapsed back to `if remaining:` → **`1 failed, 71 passed`**, killed by exactly the new `test_delete_category_detach_with_assignments_unrelated_integrity_failure_is_not_a_false_409`; `admin_service.py` restored byte-identically (`sha256 3d393e846d8d0ca0…`). Gates: focused **72 passed** (57 + 15, `--collect-only` 72); §14 witness set **196 passed**; audit/2FA/admin-models witness **51 passed**; full `apps/api` suite **1922 passed, 3 skipped in 426.34s** — claimed by this pass, re-run after the lint line-wrap on a quiescent tree with the mutation already restored (was 1921 + exactly this pass's 1 new test, no pre-existing test changing status); `ruff format --check` `281 files already formatted`; `ruff check` `All checks passed!` (one `E501` in the new test wrapped, then re-verified); `git diff --check` clean exit 0. Path inventory re-derived: **still 14 paths**; production diff confined to `admin_service.py`, with the router, schemas, audit registration and `app/router.py` byte-unchanged and no new status code or response-shape change. No test weakened, narrowed, renamed or removed. One residual corner named rather than hidden: a racing INSERT re-creating an assignment for a model this same request just detached is indistinguishable after rollback and re-raises instead of 409 — strictly narrower than F-1, same near-zero reachability, no data loss. Status `in-progress` → `review`; `epic-49` unchanged at `in-progress`. F-2 and F-3 stay deferred and `deferred-work.md` was **not** edited — the ledger append is still the controller-owned bounded write F-3 asks for at T8 time. NO human review of any kind; no `bmad-code-review`, no Aider, no Codex, no `check-all.sh`, no determinism triple, no frozen manifest. No commit, stage, push, merge, deploy, migration, seed, live DB, network or planning-artifact change. No unrelated file touched. T8 controller-owned closeout still UNCHECKED. |
| 2026-07-28 | `bmad-code-review` (Code Review, menu CR) — **independent APPROVE review, fourth CR pass**, fresh Claude Opus 5 (1M) session, native BMAD | **APPROVE — native BMAD agent approval ONLY.** Full **14-path** tracked+untracked audit at HEAD `b1a46a2`. The repair pass's self-report was NOT trusted: its run log in fact ends `subtype=error_max_turns` / `terminal_reason: max_turns` with **no `RUN_EXIT` line**, so nothing may cite `rc=0` for it (the writes did land — re-confirmed from the tree, not the log — and every claim was re-derived here). **F-1 genuinely CLOSED in both directions** by this pass's own two mutation probes, each restored byte-identically with the restore sha-asserted inside the probe command: collapsing the guard back to `if remaining:` → **`1 failed, 71 passed`** killed by exactly `test_delete_category_detach_with_assignments_unrelated_integrity_failure_is_not_a_false_409` and nothing else; deleting the `category_in_use` mapping outright → **`1 failed, 56 passed`** killed by `test_delete_category_assignment_race_is_409_not_an_unhandled_500`, proving the repair did not hollow out the documented racing-insert 409. The one branch no test covers (`detach=true` + an unaccounted racing row → 409) was verified by out-of-tree execution — a probe living entirely in `/tmp`, creating no repo file — returning `ValueError('category_in_use')` with the category present, assignments intact and **0** audit rows. N-1…N-4 re-checked at source, all intact and non-vacuous; no test weakened, narrowed, renamed or removed. Gates all reproduced: full `apps/api` **1922 passed, 3 skipped in 419.39s** (claimed by this pass on a quiescent tree — the FINAL review's 1921 plus exactly this repair's 1 new test, no pre-existing test changing status); focused **72 passed** (`--collect-only` 72); `ruff format --check` `281 files already formatted`; `ruff check` `All checks passed!`; `git diff --check` exit 0; `sha256 3d393e846d8d0ca0…`; **14 paths**; runtime OpenAPI all four routes `sot-admin-governance` with **zero `agent-write`** against 25 agent-write routes repo-wide, none on any category path; `sprint-status.yaml` parses, 336 keys, `epic-49: in-progress`, no `49-6`. Binding contract clean: `current_admin` ×4, CSRF by construction (no allowlist), audit exactly-once (4 calls / 4 commits / no `record_event` added), children-first delete precedence and replace-set validate-before-mutate pinned by order, additive-only (the single production deletion in the whole diff is `-from typing import Any`), and no migration/DDL/seed/frontend/infra/live path anywhere. **Triage: 0 decision-needed, 0 patch, 3 defer, 3 minor-recorded, 3 dismissed.** New defer: the subset guard's true-positive branch has no test (verified working by execution instead). New minors: the repair log's `error_max_turns` provenance caveat, and the residual same-model-re-INSERT corner the repair itself named rather than hid. Carried unchanged: the N-4a sentinel note and the controller-owned ledger gap — **fifth consecutive pass** to disclose that `deferred-work.md` was not written for want of the authorization. **Status deliberately held at `review`, NOT advanced to `done`** despite step-04 §6's literal rule, because T8.1–T8.4 are unrun and marking `done` would be a false claim; deviation disclosed rather than silently applied. `epic-49` unchanged at `in-progress`. **NO human review of any kind**; no Aider, no Codex, no Gemini, no `check-all.sh`, no determinism triple, no frozen manifest. No commit, stage, push, merge, deploy, migration, seed, live-DB or network action; nothing staged, nothing committed, no fix applied, no repo scratch file created. T8 controller-owned closeout still UNCHECKED. Durable report: `.hermes/run-logs/e49-5-native-approve-review-20260728.md`. |

## 20. Code Review Record (native `bmad-code-review`, 2026-07-28)

**Verdict: `REQUEST_CHANGES`.** Fresh independent Claude Opus 5 (1M) reviewer session, model id `claude-opus-5[1m]`, native BMAD 6.10.0, in worktree `/home/ezop/worktrees/3d-portal-e49-5-dev` on branch `feat/E49.5-admin-category-governance` at HEAD/baseline `b1a46a27c3ab7862b4284df48892040a58fbfe18`. **NO human review of any kind** — no Ezop signature, no Ezop review, no Laura review is recorded, implied or claimable. No Aider and no Codex run; the independent external review (`laura-aider-review-diff`, Aider routine default per the Laura Agent Rulebook) is still owed. No `check-all.sh`, no determinism triple, no frozen manifest, no commit, push, merge, deploy, migration, seed, live-DB or network action. Nothing staged, nothing committed. Durable report: `/home/ezop/repos/3d-portal/.hermes/run-logs/e49-5-native-review-20260728.md`.

**Routing.** `AGENTS.md`, `CLAUDE.md`, `_bmad-output/project-context.md` and the Laura Agent Rulebook read first. Mandatory native `bmad-help` invoked FIRST, before any review work, then confirmed against `_bmad/_config/bmad-help.csv` (the `bmad-code-review` row: Code Review / **CR** / phase `4-implementation` / `preceded-by=bmad-dev-story`) and `_bmad/_config/skill-manifest.csv:38` (canonical id `bmad-code-review`, path `_bmad/bmm/4-implementation/bmad-code-review/SKILL.md`). Installed body `.claude/skills/bmad-code-review/` used as-is — no `.claude/skills` repair needed in this worktree. Customization resolved with `resolve_customization.py --skill .claude/skills/bmad-code-review --key workflow`: no team or user override, `activation_steps_prepend`/`append` empty, `on_complete` empty, `persistent_facts=["file:{project-root}/**/project-context.md"]` loaded. Executed through the skill's own step files 01→04 — no name-matching, no hand-rolling, no bypass. Step-01 Tier-1 resolution: uncommitted tracked+untracked tree vs `HEAD`, `{spec_file}` = this artifact, `{review_mode}="full"` (which is what enables the Acceptance Auditor).

**Scope audit.** `git status --porcelain -uall` re-derived: **13 paths** (10 tracked M + 3 untracked), all audited, including the untracked router (216 lines) and both new test modules (751 + 327 lines). `admin_service.py`'s +344 hunk was read directly from the file because the diff renderer truncated 239 lines. Diff stat +689/-59 tracked. `git diff --check` empty, exit 0.

**Three layer outcomes — all completed, none failed or returned empty (`failed_layers` empty).** Blind Hunter (no spec context, `bmad-review-adversarial-general`, probes executed against a throwaway SQLite) → 15 raw findings. Edge Case Hunter (`bmad-review-edge-case-hunter`, boundary enumeration, most findings confirmed by execution) → 9 raw findings plus an explicit correctly-handled list covering self-cycle, A→B→A and A→B→C→A, childful reparent, parent clearing, no-op self-rename, empty/one/many sets, both-conflicts-present, and `detach` at 0/1/many. Acceptance Auditor (`review_mode=full`, spec + `architecture.md` + `epics.md` loaded) → 6 findings plus a full AC-1…AC-37 coverage table and both delta arbitrations. After dedup and orchestrator-owned severity: **1 decision-needed, 6 patch, 8 defer, 6 dismissed** (§ Review Findings above).

**Independently re-derived gate evidence — the implementer's §15 numbers were NOT trusted, and all reproduced exactly.** `.venv/bin/pytest -q` full `apps/api` suite → **`1905 passed, 3 skipped, 2236 warnings in 424.05s (0:07:04)`**. The story's exact §15 T6.2 witness command → **`196 passed, 255 warnings in 28.93s`**. `--collect-only` on the two new files → **55** (45 + 10). `ruff format --check .` → **`281 files already formatted`**; `ruff check .` → **`All checks passed!`**; `git diff --check` clean, exit 0. One new negative result, reported rather than suppressed: `pytest tests/test_csrf_middleware.py tests/test_sot_categories.py` → `12 failed, 16 passed` on `401 {"detail":"invalid_access"}` — bisected to a **pre-existing** cross-file secret leak in two byte-unmodified files, not caused by this story, and not present in the default full-suite order.

**Binding contract verified clean.** BrowseCategory independent M:N with zero assignments valid and models public; all four routes `current_admin` with `tags=["sot-admin-governance"]` and zero `agent-write` (`browse_category_admin_router.py:44, 87, 126, 167, 204`, plus the non-vacuous extended `_GOVERNANCE_ROUTES` guards); exactly four `_audit_entity` calls, one per service function (`:1395, 1467, 1538, 1607`), one commit each; depth-2 ceiling and self-cycle service-layer only, no DDL/migration/seed/frontend; children checked at `:1516-1517` **before** the assignment query at `:1519` and before `detach` is read at `:1522`, parametrised over `""`/`?detach=true`/`?detach=false`; replace-set validates model, duplicates and every id (`:1581`, `:1583`, `:1586-1588`) all **before** the first `session.delete` at `:1595`, duplicates→400, unknown→404, `[]` allowed; bounded 8-key snapshot with `detached_model_ids` present only when rows were actually detached and always paired with a count; public reads and Tag/TagGroup governance byte-unchanged; no Story 49.6 and no tag→category inference. Router registered at `app/router.py:34`, immediately after `sot_tag_group_admin_router` at `:33`, exactly as §7 predicted, with no path collision against `admin_router.py`'s `/models/{model_id}/*` routes.

**Arbitration — delta A (re-pointed Story 47.5 retired-route guard): LEGITIMATE, coverage re-pointed not weakened.** The collision is unavoidable (Decision AY ratifies `PATCH /api/admin/categories/{id}`; the old `assert status in (404, 405)` and that route are mutually exclusive). The precedent is ratified, same file and same closed gate G26-ROUTE-PATH that Story 49.3 used for the sibling read guard, and Decision AY's own "Recorded tradeoff — re-using the retired `/api/categories` path" paragraph ratifies the pattern. The new assertion is non-vacuous: exact 10-key set **equality** (catches additions as well as removals), `children`/`subcategories` absence, scalar `parent_id`, and `position == 1` proving the write took effect. The stronger claim that other recursive-CRUD coverage was deleted is **rejected** — the baseline body was a single `client.patch` against one path and never carried it. Critically, the broader no-resurrection contract does not rest on this test: `test_no_category_schemas_in_components` and `test_no_category_properties_on_model_schemas` are byte-unmodified and green, and that is where the structural invariant lives. Runtime-assembled path literal preserved (47.5 §11 grep discipline intact); authenticating as a real `User` row is necessary, not loosening, because the write inserts an `audit_log` row FK'd to `user.id`. One Minor residual: the function name now asserts the opposite of what it tests (filed as a Patch item).

**Arbitration — delta B (two `KNOWN_ENTITY_TYPES` drift guards): MINIMAL and TRUTHFUL.** `test_audit.py` is +1/-0 — one string inserted into the `expected` literal, with the assertion still **exact bidirectional set equality**, so it still fails on an extra type as well as a missing one; nothing relaxed, nothing removed. `test_2fa_schema.py` is +4/-2 — `15 → 16` plus two comment lines appended in-style to the existing 6.1→7.1→33.2→42.4→47.5 chain, and the new count is independently correct. This is the established convention the comment chain itself records; Story 42.4 did exactly this for `tag_group`. No invariant rests on the count alone, since the set-equality guard already pins the length. **AC-34's sub-claim "every pre-existing audit-touching test stays green" is falsified** — correctly disclosed as D-2 rather than absorbed, and now recorded here as known-wrong AC text. Honest pre-existing caveat, neither introduced nor worsened by this story: `test_known_entity_types_covers_all_call_site_resources` is a pinned literal rather than a call-site scan despite its name, and the new code audits via `_audit_entity`'s direct `session.add(AuditLog(...))` (`:114-135`), bypassing `record_event`'s `ValueError` guard entirely — so for everything on that path (all tag-group governance since 42.4, and now all category governance) the call-site/resource invariant is protected by convention, not mechanism. Filed for a future audit-hygiene pass.

**AC outcome.** No AC is VIOLATED and no binding constraint is breached. Exceptions named honestly: AC-5/AC-24/AC-33's CSRF halves are satisfied **by construction** (prefix middleware, `csrf.py:12-19`) but proven by no route-specific test — authorised by §6 F-8; AC-35 is partially proven (create path); AC-25 and AC-23 are narrower than their text; AC-34's sub-claim is falsified as above; AC-36 holds for the paths it actually lists (§7's prediction about `test_openapi_agent_surface.py` is falsified by D-1, but that file is not on AC-36's list). All six pass-by-construction guards the implementer labelled honestly are **load-bearing now** that the routes exist.

**Why `REQUEST_CHANGES` and not `APPROVE`.** Not inferred from any exit code — every gate reproduced green, which is exactly why the verdict rests on findings rather than runs. One decision-needed medium finding whose obvious fix would itself introduce a 500; three unresolved medium findings; and the native workflow's own rule (`steps/step-04-present.md` §6) that patch findings left as action items mean `in-progress`, not `done`. **Nothing here is a security, authorization, data-corruption or merge-blocking safety defect** — auth, CSRF, the `agent-write` fence, audit exactly-once, validate-before-mutate, children-first 409 and the additive-only invariant all verified clean, and both disclosed deltas arbitrate as legitimate. A single repair pass on the six Patch items plus a decision on the one Decision item would close this.

**Disclosed deviations from the base workflow.** (1) Step-01's CHECKPOINT and step-04's §4/§5/§7 HALTs were not honoured interactively — non-interactive automation session, no human present; the step-01 target was fully determined by the Tier-1 explicit argument so nothing was guessed, and step-04 §5 took the non-destructive default (option 2, "leave as action items"), which is also what the task mandates. (2) **`deferred-work.md` was NOT written** — step-04 §2 directs appending every defer finding there, but this pass is authorised to write only this artifact and `sprint-status.yaml`; the eight defer items are recorded in full in § Review Findings and in the durable report instead. (3) Review layers ran as `general-purpose` subagents, each re-deriving the 13-path diff itself and invoking its own canonical review skill. (4) Subagent severities were overridden per step-03 instruction 4; three subagent claims were rejected after independent re-derivation. (5) A full-suite `pytest -q` was run (424s) because re-deriving the `1905 passed` headline claim required it; the forbidden gates (`check-all.sh`, determinism triple, frozen manifest) were not run. (6) `epic-49` left unchanged at `in-progress`.

## 21. Code Re-review Record (native `bmad-code-review`, independent RE-REVIEW, 2026-07-28)

**Verdict: `REQUEST_CHANGES`.** Fresh, independent Claude Opus 5 reviewer session, native BMAD 6.10.0, in worktree `/home/ezop/worktrees/3d-portal-e49-5-dev` on branch `feat/E49.5-admin-category-governance` at HEAD/baseline `b1a46a27c3ab7862b4284df48892040a58fbfe18`. **NO human review of any kind** — no Ezop signature, no Ezop review, no Laura review is recorded, implied or claimable. No Aider and no Codex run; the independent external review (`laura-aider-review-diff`, Aider routine default per the Laura Agent Rulebook; Codex fallback/high-stakes only; Gemini not a default reviewer) is still owed. No `check-all.sh`, no determinism triple, no frozen manifest, no commit, stage, push, merge, deploy, migration, seed, live-DB or network action. Nothing staged, nothing committed, no fix applied. Durable report: `/home/ezop/repos/3d-portal/.hermes/run-logs/e49-5-native-rereview-20260728.md`.

**Routing.** `AGENTS.md`, `CLAUDE.md`, `_bmad-output/project-context.md`, the Laura Agent Rulebook, this complete artifact (777 lines) and the prior CR pass's durable report read first. Mandatory native `bmad-help` invoked FIRST via the `Skill` tool, then confirmed against `_bmad/_config/bmad-help.csv:29` (CR row: `preceded-by=bmad-dev-story`, phase `4-implementation`) and `_bmad/_config/skill-manifest.csv:38` (canonical id `bmad-code-review`, path `_bmad/bmm/4-implementation/bmad-code-review/SKILL.md`). Customization resolved: no team or user override, `activation_steps_prepend`/`append` empty, `on_complete` empty, `persistent_facts=["file:{project-root}/**/project-context.md"]` loaded. Executed through the skill's own step files 01→04 — no name-matching, no hand-rolling, no bypass. Step-01 Tier-1: uncommitted tracked+untracked tree vs `HEAD`, `{spec_file}` = this artifact, `{review_mode}="full"`, `{story_key}=49-5-admin-category-governance`.

**Scope audit — 14 paths, reconciling the stale 13.** `git status --porcelain -uall` re-derived: **11 tracked M + 3 untracked**. The prior review's 13-path count was correct for the tree it saw; the repair pass added `deferred-work.md` as an 11th tracked path, exactly as §15's re-derived File List states. All 14 audited; the three untracked files read in FULL (router 217 lines, `test_sot_admin_categories.py` 1036 lines / 54 tests, `test_sot_admin_model_categories.py` 363 lines / 11 tests), and `admin_service.py:1285-1684` read directly from the file rather than through a truncating diff renderer.

**Three layer outcomes — all completed, `failed_layers` empty.** Blind Hunter (`bmad-review-adversarial-general`, no spec context, probes executed against throwaway SQLite plus a restored in-place mutation) → 9 confirmed-by-execution findings, 4 read-only, a 10-item negative-space list and a 15-item correctly-handled list. Edge Case Hunter (`bmad-review-edge-case-hunter`, systematic boundary walk with forced in-process interleaves) → 9 new boundaries (8 confirmed by execution), 6 independently re-confirmed ledger items, a 16-item correctly-handled list. Acceptance Auditor (`review_mode=full`, spec + `architecture.md` + `epics.md` loaded) → a full AC-1…AC-37 table, a §3.1-§3.12 constraint sweep, a six-probe closure matrix and a truthfulness audit of §15/§20. All severities re-assigned by the orchestrator per step-03 rule 4; three subagent claims dismissed after independent re-derivation.

**Prior-finding closure — independently re-derived, not trusted.** #2, #3, #4, #11, #12, #13 all **CLOSED**, each mutation-proven (the auditor layer ran six single-line mutations; this session ran two more of its own). The #1 decision-needed finding's controller preservation-first arbitration is **UPHELD**: Decision AY's ratified delete policy reads "409 Conflict while assignments exist" with no live/soft-deleted qualifier, `model_count`'s "distinct non-deleted models" definition is separately truthful, AC-18's literal precondition is satisfied, and the alternative would drive `session.delete(cat)` into the RESTRICT FK. Pinned end to end by `test_soft_deleted_model_assignment_is_preserved_until_explicit_detach`.

**Prediction deltas re-audited.** Delta A (re-pointed Story 47.5 guard) remains **load-bearing**: exact 10-key set equality catches additions as well as removals, the `children`/`subcategories` negatives and the scalar-`parent_id` assertion hold, `position == 1` proves the write took effect, the runtime-assembled retired-path literal is preserved, and the structural no-resurrection invariant still lives in the byte-unmodified `test_no_category_schemas_in_components` / `test_no_category_properties_on_model_schemas`. Delta B remains **minimal and truthful**: `test_audit.py` +1/-0 with exact bidirectional set equality intact, `test_2fa_schema.py` 15 → 16 with the count independently verified at runtime as 16.

**Evidence actually executed by this pass** (verbatim): focused `65 passed, 110 warnings in 9.59s` (`--collect-only`: 54 + 11 = 65); the story's exact §14 witness set `196 passed, 255 warnings in 28.24s`; `51 passed` on the audit/entity-type + admin-model witnesses; `ruff format --check .` → `281 files already formatted`; `ruff check .` → `All checks passed!`; `git diff --check` empty, exit 0; `yaml.safe_load` on `sprint-status.yaml` → 336 `development_status` keys, `epic-49: in-progress`, no `49-6` key; `deferred-work.md` diff `29 0` with 16 → 17 headings and exactly 8 entries; runtime `len(KNOWN_ENTITY_TYPES)` = 16; runtime OpenAPI dump of all four routes carrying `sot-admin-governance`, non-empty summary+description, zero `agent-write`. Own mutation probes: update-audit `after` snapshot taken before the `setattr` loop → **`70 passed`** (surviving mutant); children pre-query deleted → **`1 failed, 64 passed`** (both children-409 tests stay green). `admin_service.py` restored after each, `sha256 ef7d124c11a0896a…` re-verified.

**The full `apps/api` suite is deliberately NOT claimed by this pass.** Three runs were attempted: one completed at `1915 passed, 3 skipped` but ran concurrently with the layers' in-place mutations and cannot be certified; one returned `3 failed, 1961 passed` where every failure was in a review layer's temporary `test_zz_probe*` scratch file (since removed — `git status -uall` shows exactly the 14 story paths); one was killed at ~11%. The full suite, `check-all.sh` 16/16 and the determinism triple all remain controller-owned and owed.

**Triage: 4 patch (all `low`), 7 defer, 3 dismissed, 1 inherited-recorded** (see § Re-review Findings). **No AC is violated and no binding constraint is breached**, and nothing found is a security, authorization, data-corruption or merge-blocking safety defect. The verdict rests on findings, not runs: one contract-honesty regression the finding-#3 repair itself introduced (a false `409 category_in_use` for any integrity failure outside the two RESTRICT FKs, reproduced with a passing control), two incomplete halves of the repairs under review (`replace_model_categories` left without the safety net its sibling got; the update path left without the snapshot assertion the create path got), a traceability gap on two AC ordering claims, and the skill's own step-04 §6 rule that patch findings left as action items mean `in-progress`. All four are `low`; N-1 and N-3 are the minimum honest closure (≈2 lines and one assertion), and N-2/N-4 are defensible defers if the controller prefers to hold the frozen delta, provided the deferral is recorded rather than silent.

**Disclosed deviations.** (1) Step-01's CHECKPOINT and step-04's §4/§5/§7 HALTs were not honoured interactively — non-interactive automation session; the Tier-1 argument fully determined the target and step-04 §5 took the non-destructive default (option 2), which is what the task mandates. (2) **`deferred-work.md` was NOT written** — step-04 §2 directs it, but this pass's write scope is this artifact plus `sprint-status.yaml`; the seven new defers are recorded in § Re-review Findings and in the durable report. (3) Review layers ran as `general-purpose` subagents, each re-deriving the path inventory itself and invoking its own canonical skill. (4) Subagent severities overridden per step-03 instruction 4. (5) The layers and this session mutated `admin_service.py` temporarily and concurrently; all mutations restored, final `sha256` corroborated three independent ways, and the two consequences (a contaminated mid-flight baseline-hash record of mine, and two contaminated full-suite runs) are disclosed rather than dropped. (6) The full suite is not claimed as evidence (above). (7) `epic-49` left unchanged at `in-progress`; the story was never marked `done`. (8) The task prompt's 13-path inventory is stale; 14 were audited and the reconciliation reported. (9) This record and the durable report are English per `CLAUDE.md`; the conversational surface is Polish.

## 22. Code Review Record (native `bmad-code-review`, independent FINAL review / third CR pass, 2026-07-28)

**Verdict: `REQUEST_CHANGES`.** Fresh, independent Claude Opus 5 (1M) reviewer session, model id `claude-opus-5[1m]`, native BMAD 6.10.0, in worktree `/home/ezop/worktrees/3d-portal-e49-5-dev` on branch `feat/E49.5-admin-category-governance` at HEAD/baseline `b1a46a27c3ab7862b4284df48892040a58fbfe18`. **NO human review of any kind** — no Ezop signature, no Ezop review, no Laura review is recorded, implied or claimable. No Aider and no Codex run; the independent external review (`laura-aider-review-diff`, Aider routine default per the Laura Agent Rulebook; Codex fallback/high-stakes only; Gemini is not a default reviewer) is still owed. No `check-all.sh`, no determinism triple, no frozen manifest, no commit, stage, push, merge, deploy, migration, seed, live-DB or network action. Nothing staged, nothing committed, no fix applied. Durable report: `.hermes/run-logs/e49-5-native-final-review-20260728.md`.

**Routing.** `AGENTS.md`, `CLAUDE.md`, `_bmad-output/project-context.md`, the Laura Agent Rulebook, this artifact, the prior re-review's durable report (344 lines) and the post-re-review repair run log (`.hermes/run-logs/e49-5-bmad-repair-20260728_151258.jsonl`, 404 lines — preflight, `RESULT subtype=success turns=76`, `RUN_EXIT rc=0`, result payload read verbatim) all read before any review work. Canonical routing confirmed from `_bmad/_config/bmad-help.csv` (CR row: menu **CR**, phase `4-implementation`, `preceded-by=bmad-dev-story` — the story sits at `review` after a completed DS repair pass, so CR is the canonical next skill) and `_bmad/_config/skill-manifest.csv` (canonical id `bmad-code-review`, module `bmm`, path `_bmad/bmm/4-implementation/bmad-code-review/SKILL.md`). Installed body `.claude/skills/bmad-code-review/` used as-is — no repair needed. Customization resolved via `resolve_customization.py`: no team or user override, `activation_steps_prepend`/`append` empty, `on_complete` empty, `persistent_facts=["file:{project-root}/**/project-context.md"]` loaded. Executed through the skill's own step files 01→04. Step-01 Tier-1: uncommitted tracked+untracked tree vs `HEAD`, `{spec_file}` = this artifact, `{review_mode}="full"`, `{story_key}=49-5-admin-category-governance`.

**Scope.** `git status --porcelain -uall` re-derived four times (session start, after the mutation-probe block, after the scratch-probe run, at close) — **identical every time: 11 tracked M + 3 untracked = 14 paths**, all audited, the three untracked files read directly. `git diff --check` empty, exit 0. No path under `migrations/`, `apps/web/`, `app/core/db/`, `scripts/seed_*`, `_entities.py` or any planning artifact appears anywhere in the tree.

**Prior-finding closure — re-proved, not trusted.** All four re-review patch findings CONFIRMED CLOSED with non-vacuous tests, by five of this pass's own mutation probes (details in § Final-review Findings). The repair pass's own numeric claims were re-executed and reproduced exactly, including the full `apps/api` suite at `1921 passed, 3 skipped`, which **this pass claims** on a certified-quiescent tree.

**Triage: 1 patch (`low`), 2 defer, 2 minor-recorded, 3 dismissed.** No AC violated, no binding constraint breached, and nothing found is a security, authorization, data-corruption, data-loss or merge-blocking defect. Auth, the `agent-write` fence, CSRF-by-construction, audit exactly-once, children-first delete precedence, replace-set validate-before-mutate and the additive-only invariant all verified clean by direct reading and execution.

**Why `REQUEST_CHANGES` and not `APPROVE`.** Not inferred from any exit code — every gate reproduced green, which is exactly why the verdict rests on a finding. (1) The single `patch` finding is a this-story-introduced contract defect reproduced with a passing control: the N-1 repair closes only the zero-assignment branch, so `detach=true` on a category with assignments still returns a false `409 category_in_use` that the client cannot falsify or resolve — the un-closed remainder of the very finding this review was convened to verify, at the same severity the prior pass charged it. (2) The native workflow's own rule (`steps/step-04-present.md` §6): a patch finding left as an action item means `in-progress`, not `done`, and this pass is forbidden to apply fixes. **Scoping guidance:** the fix is ~4 lines plus one assertion; alternatively a *recorded* deferral is defensible given near-zero reachability and zero data loss, provided it is written down rather than silent.

**Disclosed deviations.** (1) Step-01's CHECKPOINT and step-04's §4/§5/§7 HALTs were not honoured interactively — non-interactive automation session; the Tier-1 argument fully determined the target and step-04 §5 took the non-destructive default (option 2, "leave as action items"), which is what the task mandates. No `decision-needed` findings arose, so §4 did not apply. (2) **Step-02's three layers were executed inline by the orchestrator rather than as three blind subagents**, under a standing harness constraint against dispatching subagents the user did not request. All three methods were applied to the full 14-path diff — adversarial hunting (which produced the patch finding and its reproduction), systematic boundary walking, and acceptance auditing under `{review_mode}="full"` — but they were **not independently blind**, which is a real methodological difference from the two prior passes and is stated so it can be weighed rather than assumed equivalent. (3) **`deferred-work.md` was NOT written** — step-04 §2 directs it, but this pass's write scope is this artifact plus `sprint-status.yaml`; the ledger gap is itself filed as a controller-owned defer. (4) One temporary scratch test file was created for the patch finding's reproduction, run in isolation (never during a suite run) and deleted in the same command; the tree was verified at exactly 14 paths immediately afterwards. (5) Five mutation probes were applied to `admin_service.py` and restored, each restore asserted against `sha256 eff4e930c2b90717…` inside the probe harness; no suite run overlapped any mutation. (6) `epic-49` left unchanged at `in-progress`; the story was never marked `done`. (7) This record and the durable report are English per `CLAUDE.md` and `document_output_language`; the conversational surface is Polish per `communication_language`.


## 23. Code Review Record (native `bmad-code-review`, independent APPROVE review / fourth CR pass, 2026-07-28)

**Verdict: `APPROVE` — native BMAD agent approval ONLY.** Fresh, independent Claude Opus 5 (1M) reviewer session, model id `claude-opus-5[1m]`, native BMAD 6.10.0, in worktree `/home/ezop/worktrees/3d-portal-e49-5-dev` on branch `feat/E49.5-admin-category-governance` at HEAD/baseline `b1a46a27c3ab7862b4284df48892040a58fbfe18`. **NO human review of any kind** — no Ezop signature, no Ezop review, no Laura review is recorded, implied or claimable. **Still owed and all controller-owned:** `infra/scripts/check-all.sh` 16/16 standalone with teed evidence (T8.1), the NFR26-DETERMINISM-1 determinism triple (T8.2), the frozen manifest, the independent external review `laura-aider-review-diff` / **Aider** (routine default per the Laura Agent Rulebook §2; Codex fallback/high-stakes/explicit-operator-request only; **Gemini is not a default reviewer**) (T8.3), and the T8.4 commit / ff-only merge / push / deploy chain. No commit, stage, push, merge, deploy, migration, seed, live-DB or network action; nothing staged, nothing committed, no fix applied. Durable report: `.hermes/run-logs/e49-5-native-approve-review-20260728.md`.

**Routing.** `AGENTS.md`, `CLAUDE.md`, `_bmad-output/project-context.md`, the Laura Agent Rulebook, this artifact, the prior FINAL review's durable report (262 lines) and the post-FINAL F-1 repair run log (261 lines, fully parsed) all read before any review work. Canonical routing confirmed from `_bmad/_config/bmad-help.csv` (CR row: menu **CR**, phase `4-implementation`, `preceded-by=bmad-dev-story` — the story sits at `review` after a completed DS repair pass, so CR is the canonical next skill) and `_bmad/_config/skill-manifest.csv` (canonical id `bmad-code-review`, module `bmm`, path `_bmad/bmm/4-implementation/bmad-code-review/SKILL.md`). Installed body `.claude/skills/bmad-code-review/` used as-is. Customization resolved via `resolve_customization.py`: no team or user override, `activation_steps_prepend`/`append` empty, `on_complete` empty, `persistent_facts=["file:{project-root}/**/project-context.md"]` loaded. Executed through the skill's own step files 01→04. Step-01 Tier-1: uncommitted tracked+untracked tree vs `HEAD`, `{spec_file}` = this artifact, `{review_mode}="full"`, `{story_key}=49-5-admin-category-governance`.

**Provenance caveat on the repair run.** The post-FINAL F-1 repair log terminates `subtype=error_max_turns` / `terminal_reason: max_turns` / `errors: ["Reached maximum number of turns (50)"]` with **no `RUN_EXIT` line**. Its writes did land — verified from the tree and from its own final `tool_result` payloads — but its self-report was not trusted and **no `rc=0` may be cited for it.** Every claim was re-derived here. Recorded as a minor finding in § Approve-review Findings.

**Scope.** `git status --porcelain -uall` re-derived at session start, after each mutation probe, after the out-of-tree branch probe and at close — **identical every time: 11 tracked M + 3 untracked = 14 paths**, all audited, the three untracked files read directly. `git diff --check` empty, exit 0. The destructive-path inventory (`migrations/`, `apps/web/`, `core/db/`, `seed`, `_entities`, `planning-artifacts`, `infra/`) returns nothing.

**F-1 closure — proved, not trusted.** Two of this pass's own mutation probes, restores sha-asserted inside the probe command: collapsing the guard to `if remaining:` → `1 failed, 71 passed` killed by exactly the new F-1 test; deleting the `category_in_use` mapping → `1 failed, 56 passed` killed by the assignment-race test. The untested true-positive branch was verified by out-of-tree execution (409 returned, category preserved, zero audit rows) rather than left to reading. N-1…N-4 re-checked at source and intact.

**Gates.** Full `apps/api` suite **`1922 passed, 3 skipped in 419.39s`**, claimed by this pass on a quiescent tree; focused `72 passed` / `--collect-only` 72; `ruff format --check` `281 files already formatted`; `ruff check` `All checks passed!`; `git diff --check` exit 0; `sha256 3d393e846d8d0ca0…`; runtime OpenAPI zero `agent-write` on all four routes and on every category path.

**Triage: 0 decision-needed, 0 patch, 3 defer, 3 minor-recorded, 3 dismissed.** No AC violated, no binding constraint breached, nothing security / authorization / data-corruption / data-loss / merge-blocking.

**Why `APPROVE`.** Not inferred from any exit code — least of all the repair's, which was an error. F-1 is closed in both directions with mutation and execution evidence; all prior closures are intact and non-vacuous; the 14-path re-audit is clean on auth, the `agent-write` fence, CSRF-by-construction, audit exactly-once, delete precedence, replace-set ordering and the additive-only invariant; every gate reproduces; and the records are truthful. The three remaining open items are one coverage gap on a branch verified working by execution and two carried record-keeping defers, of which the ledger gap is explicitly controller-owned — none is `patch`-class within this story's frozen scope, and no unresolved `high`/`medium` finding remains.

**Disclosed deviations.** (1) Step-01's CHECKPOINT and step-04's §4/§5/§7 HALTs were not honoured interactively — non-interactive automation session; the Tier-1 argument fully determined the target, and with zero `decision-needed` and zero `patch` findings §4 and §5 did not apply. (2) **Step-04 §6's status rule was deliberately NOT followed to `done`.** Its literal text sets `done` on this triage, but T8.1–T8.4 are unrun and §2's gate posture reserves closeout for the controller, so marking `done` would be a false claim. `Status: review` preserved — disclosed rather than silently applied. (3) **Step-02's three layers were executed inline by the orchestrator rather than as three blind subagents**, under a standing harness constraint against dispatching subagents the user did not request; all three methods were applied to the full 14-path diff, but they were **not independently blind** — second consecutive pass with this constraint, stated so it can be weighed. (4) **`deferred-work.md` was NOT written** — fifth consecutive pass disclosing this identical gap, itself filed as a controller-owned defer. (5) Two mutation probes applied to `admin_service.py` and restored, each restore asserted against `sha256 3d393e846d8d0ca0…` inside the probe command; no suite run overlapped either. (6) **No scratch file was created inside the repo** — the branch probe lives entirely in `/tmp` and imports the app via `PYTHONPATH`; the tree was verified at exactly 14 paths and baseline sha immediately afterwards. (7) `epic-49` left unchanged at `in-progress`; the story was never marked `done`. (8) This record and the durable report are English per `CLAUDE.md` and `document_output_language`; the conversational surface is Polish per `communication_language`.

## 24. Controller closeout evidence before commit (Laura/ITCM, 2026-07-28)

**Status: review, not done.** This controller pass closes T8.1–T8.3 only. T8.4 remains unchecked until the one commit is created, fast-forward merged to `main`, pushed, deployed and post-verified. No human review of any kind is claimed.

**Independent review gates.** Native BMAD review is literal `APPROVE` in `.hermes/run-logs/e49-5-native-approve-review-20260728.md`. Routine independent external Aider review is literal `APPROVE` in `.hermes/run-logs/e49-5-aider-review-20260728_164015.log` (`rc=0`), with no Critical/Important findings; the only notes are already-recorded minor/documentation and coverage defers.

**Full closeout gate.** First `infra/scripts/check-all.sh` run failed from missing local worktree toolchains only (`workers/render/.venv` absent; `apps/web/node_modules` absent). The controller provisioned ignored local dependencies via `uv sync --extra dev` in `workers/render` and `npm ci` in `apps/web`, restored the repo hook config from npm's `apps/web/.husky` side effect back to `core.hooksPath=.githooks`, removed generated `apps/web/vitest*.d.ts` residue, and reran the gate. Final evidence: `.hermes/run-logs/check-all-e49-5-rerun-20260728_164803.log` → all **16/16** stages green and `CHECK_ALL_RC=0`.

**Determinism.** `.hermes/run-logs/e49-5-determinism-triple-20260728_170039.log` → the focused Story 49.5 backend suite passed three consecutive times with identical counts: `72 passed, 124 warnings` ×3.

**Frozen manifest.** `.hermes/run-logs/e49-5-frozen-manifest-20260728_170219.log` captured branch `feat/E49.5-admin-category-governance`, HEAD `b1a46a2`, `core.hooksPath=.githooks`, the exact 14 story paths, ignored local tool dirs (`apps/web/node_modules`, `workers/render/.venv`, `.hermes/run-logs`), and `git diff --check` clean. The two generated TypeScript declaration files noted in that manifest were removed immediately after the manifest because they were local test/build residue and are absent from the final status.

## 25. Post-merge deploy closeout (Laura/ITCM, 2026-07-28)

**Status: done.** The single implementation commit `52519d851444828371b987eb2ab7cb6f2c778078` (`feat(api): add admin category governance (Story 49.5, Init 26)`) contains the final 14-path story diff and T8.1–T8.3 evidence. It was fast-forward merged from `feat/E49.5-admin-category-governance` into `main`, pushed to GitHub, and deployed immediately per repo policy.

**Push/deploy evidence.** Lean pre-push hook passed 11/11 (`git diff --check`, API/worker ruff, web typecheck/lint, settings/env/compose drift, both uv lock checks, local-env secret check). `main`, `origin/main`, remote `refs/heads/main`, and `infra/.last-deploy-sha` all resolve to `52519d851444828371b987eb2ab7cb6f2c778078`. Deploy log `.hermes/run-logs/deploy-e49-5-20260728_170709.log` ended `DEPLOY_RC=0`, rebuilt base images plus the slicer-worker overlay, ran `SLICER_WORKER_SMOKE_OK`, verified GlitchTip symbolication for release `0.1.0+52519d8`, verified the agent-runbook fingerprint, and wrote the deploy state file.

**Runtime smoke.** On `.190`, `3d-portal-api-1`, `3d-portal-arq-worker-1`, `3d-portal-redis-1`, `3d-portal-slicer-worker-1`, `3d-portal-web-1`, and `3d-portal-worker-1` were all running after deploy. `GET https://3d.ezop.ddns.net/api/health` returned `200 {"status":"ok","version":"0.1.0"}`. `GET https://3d.ezop.ddns.net/api/openapi.json` exposed the three new Story 49.5 paths with tag `sot-admin-governance`: `POST /api/admin/categories`, `PATCH/DELETE /api/admin/categories/{category_id}`, and `PUT /api/admin/models/{model_id}/categories`. Unauthenticated `GET /api/categories` returned `401`, consistent with the existing default-deny auth posture.
