import type { Viewer3DProps } from "./types";

type Props = Viewer3DProps & { onExpand?: (id: string) => void };

// Placeholder — full implementation lands in a later task.
export default function Viewer3DInline(_props: Props): null {
  void _props;
  return null;
}
