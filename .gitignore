# Dependencies
node_modules/
__pycache__/
*.pyc
.venv/
.pnpm-store/

# Build artifacts
dist/
build/
*.egg-info/
*.tsbuildinfo
# tsc composite / declaration emit artifacts (Vite handles actual bundling)
apps/web/vite.config.js
apps/web/vite.config.d.ts
apps/web/src/**/*.js
apps/web/tests/**/*.js
apps/web/tests/**/*.d.ts

# Local env
.env
.env.local
.env.production

# Editor
.vscode/
.idea/

# Worktrees
.worktrees/

# Test
coverage/
.pytest_cache/
test-results/
playwright-report/
.playwright-mcp/

# OS
.DS_Store
Thumbs.db

# 3D Portal — runtime artifacts
*.db
*.db-journal
htpasswd-*
.env.production.local
.env.dev.local

# Verify ritual — runtime tripwire (Story 3.1)
infra/.last-verify

# Visual regression
apps/web/tests/visual/test-results/
apps/web/tests/visual/__snapshots__/**/*.actual.png
apps/web/tests/visual/__snapshots__/**/*.diff.png

# Render artifacts (local fixtures only — production renders live on .190 volume)
workers/render/.local-renders/

# Implementation plans — kept local-only on purpose (may reference internal hosts/paths)
docs/plans/

# BMAD planning + implementation artifacts — local-only (may reference internal hosts/paths)
_bmad-output/

tmp/

# Orchestration logs (local-only operational state)
.codex/

# Brainstorming companion (local mockups + state)
.superpowers/

# Playwright MCP scratch screenshots from QA subagents (working dir leaks)
.playwright-mcp/
/*.png

# Claude scheduled task locks (session-only)
.claude/scheduled_tasks.lock
